---
context: default
phase: null
task: null
total_tasks: null
status: paused
last_updated: 2026-09-01T14:34:45.090Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

- [ ] CONSTRAINT: `gsd-sdk` PATH shadowing — the shell's `gsd-sdk` resolves to
  `/run/user/1000/fnm_multishells/.../bin/gsd-sdk` (v0.1.0, no `query` subcommand,
  fails with "Unknown option" / usage errors). The working binary with `query`
  support is `/mnt/c/Users/ak/.npm-global/gsd-sdk` (same version string, different
  build). Any GSD workflow that shells out to `gsd-sdk query ...` will break until
  this is fixed. Structural mitigation: either call the npm-global binary by full
  path, or fix fish PATH ordering (npm-global before the fnm shim) before running
  any further GSD skill.

**Do not proceed until this is checked.**

<current_state>
No active phase/spike/sketch/deliberation. This was ad-hoc investigation
(triggered by `/gsd:quick`, not yet a real quick-task — no `.planning/quick/`
directory was created, no PLAN.md written, no code changed). Paused before
Step 2 of the quick.md workflow (gsd-sdk init call) due to context exhaustion.
</current_state>

<completed_work>

Diagnosed why 4 open Renovate PRs (#42-45) in `akentner/homeassistant-addons`
show red CI. Confirmed via `gh run list --branch main` that Lint has been
failing on `main` for multiple commits already — unrelated to Renovate.
Full root-cause list (verified against CI logs + source, not yet fixed):

1. **end-of-file-fixer** — several files missing trailing newline. Trivial
   auto-fix (pre-commit run / `make fix`).

2. **shellcheck** — SC2029 (info) in `internal/spike-h1-token-rotation.sh`
   (lines 57, 71) and `internal/spike-pitfalls10-backup-addon-config.sh`
   (lines 48, 69, 89, 91, 92); SC2012 (info) in the latter, line 140.
   `.pre-commit-config.yaml:30` only ignores SC1091/SC2034 — no severity
   floor, so info-level findings fail the hook. The SC2029 hits look
   intentional (client-side var expansion into a remote ssh command string,
   by design) — plan was to add inline `# shellcheck disable=SC2029` /
   `SC2012` comments with a short reason rather than rewrite the scripts.

3. **prettier** — 5 markdown files need reformatting: `.github/RELEASE.md`,
   `README.md`, `docs/DEVELOPMENT.md`, `docs/WEBHOOK_SETUP.md`,
   `terraform-bridge/DOCS.md`. Trivial auto-fix.

4. **actionlint** — SC2086 (unquoted var) in
   `.github/workflows/test-install-provider.yml` around line 56, inside a
   `run: |` block. Needs proper quoting.

5. **validate-addon-config — REAL BUG.** Commit `1195028` renamed
   `terraform-bridge/config.yaml`'s `map: type:` from `addon_config` to
   `app_config` (HA deprecated the old name). But
   `internal/validate-addon-config.py:12` `VALID_MAP_TYPES` was never
   updated — still only
   `{"addon_config","backup","config","data","media","share","ssl"}`.
   Fix: add `"app_config"` to the set, **keep `addon_config` too** (per
   repo CLAUDE.md, `phone-logger` add-on still uses the legacy type).

6. **terraform-bridge scaffold verify — REAL BUG.**
   `internal/verify-bridge-scaffold.sh:66` parses
   `docker images --format '{{.Size}}'` via a naive Python one-liner
   (`s.split()[0]` for the number, `s.split()[1]` for the unit), assuming
   a space like `"55.1 MB"`. Current Docker emits no space (`"55.1MB"`),
   so `.split()` yields one token and `float()` throws
   `ValueError: could not convert string to float: '55.1MB'`. Fix: parse
   with a regex, e.g. `re.match(r'([\d.]+)\s*([A-Za-z]+)', s)`, instead of
   `.split()`.

7. **terraform-bridge no-token-leak — REAL BUG, deferred to /gsd:debug.**
   `internal/verify-bridge-no-token-leak.sh` fails with
   `FAIL: could not read /data/initial-token from running container` /
   `No such container: terraform-bridge-leak-test`. Root cause: commit
   `680a1bf` added `WriteInitialTokenFile` (writes plaintext token to
   `/data/initial-token`, see `terraform-bridge/internal/auth/token.go`)
   and `main.go:75` hardcodes `NewFileTokenStore("/data")`, but nothing
   creates `/data` (no `mkdir` in `terraform-bridge/Dockerfile`, no
   `MkdirAll` in the token store). The test's plain `docker run` (no `-v`
   volume) has no `/data` dir → `os.CreateTemp` fails → bridge calls
   `os.Exit(1)` on `token_file_write_failed` → container (`--rm`) is gone
   before the script's `docker exec`/`docker logs` can run. **User
   confirmed the test's fake/leaked token was already rotated — no live
   secret exposure** — but the bridge crashing on every fresh install
   without a supervisor-mounted `/data` is a real bug worth verifying
   against actual HA Supervisor behavior (which normally auto-mounts
   `/data` for every add-on, so this may only bite in this standalone
   test harness — unconfirmed, no docker available locally in this WSL2
   env to repro).

User's explicit split: fix 1-6 via `/gsd:quick`, fix 7 separately via
`/gsd:debug` (needs more careful investigation, possible prod impact).

No files were edited, no commits made — diagnosis only.
</completed_work>

<remaining_work>

1. Run `/gsd:quick` with the following task description (pass verbatim,
   it was already the argument used when this session started that flow):

   > Fix 6 pre-existing lint failures on main branch (found while
   > investigating why open Renovate PRs #42-45 show red CI — confirmed
   > pre-existing, unrelated to those PRs): [items 1-6 as detailed above,
   > exact file paths/lines/fixes]. Do NOT touch item 7. Verify locally
   > where possible (no docker in this WSL2 env, so item 6's fix can only
   > be sanity-checked with a standalone python snippet, not the real
   > hook).

2. Then run `/gsd:debug` for item 7 — investigate whether the
   `/data`-mkdir gap is a real HA Supervisor production risk (Supervisor
   normally auto-provisions `/data`) or purely a test-harness gap (no `-v`
   in the CI script's `docker run`). Fix whichever is confirmed — likely
   both: `mkdir -p /data` (or equivalent) in the Dockerfile/entrypoint for
   defense-in-depth, and/or add a volume mount to
   `verify-bridge-no-token-leak.sh`'s `docker run` if the test itself is
   the gap.

3. Once both fixed and green locally, re-run the 4 Renovate PRs' CI
   (rebase/re-trigger) — they were never blocked by anything Renovate
   introduced, so they should go green once `main` is fixed and merged
   forward into them.
</remaining_work>

<decisions_made>

- Split fix work: mechanical/lint (1-6) via `/gsd:quick`, the
  production-risk bug (7) via `/gsd:debug` — user's explicit call.
- For shellcheck SC2029 findings: preserve the intentional client-side var
  expansion, suppress via inline disable comments rather than rewriting
  the spike scripts (they're throwaway Phase 9/10 spike scripts, not
  touching runtime behavior).
- For validate-addon-config: keep both `addon_config` and `app_config` as
  valid map types (phone-logger still uses the legacy name).
</decisions_made>

<blockers>
- gsd-sdk PATH shadowing (see BLOCKING CONSTRAINTS above) — must resolve
  before any GSD skill that shells out to `gsd-sdk query` will work
  reliably.
</blockers>

## Required Reading (in order)
1. This file — full diagnosis for all 7 issues, so no re-investigation needed.
2. `terraform-bridge/config.yaml`, `internal/validate-addon-config.py` — for item 5.
3. `internal/verify-bridge-scaffold.sh` (line 66) — for item 6.
4. `internal/verify-bridge-no-token-leak.sh`, `terraform-bridge/cmd/bridge/main.go`,
   `terraform-bridge/internal/auth/token.go` — for item 7 (do this in the
   `/gsd:debug` session, not now).

## Critical Anti-Patterns (do NOT repeat these)
- Do not assume Renovate PRs #42-45 caused the CI failures — verified via
  `gh run list --branch main` that Lint was already red on plain `main`
  commits before/unrelated to those PRs.
- Do not run local `docker build`/`docker run` to verify item 6/7 fixes —
  no docker binary in this WSL2 distro (Docker Desktop WSL integration not
  enabled here). Verify item 6's parsing logic with a standalone Python
  snippet instead; item 7 needs either CI re-run or manual docker check
  from a shell where docker is available.

## Infrastructure State
- No background processes running.
- `.planning/STATE.md` has an uncommitted diff (completed_phases 3→2,
  percent field added, session-continuity line changed) that appeared
  during this session without any explicit action by this session doing
  it — likely an automatic GSD recompute unrelated to the lint work.
  Do not assume it reflects real milestone progress changes; verify
  before trusting it in the next session.

<context>
This was purely diagnostic work triggered by "welche PRs habe ich" →
"was muss ich da machen" → deep dive into why homeassistant-addons CI is
red. All findings are backed by actual CI log reads (`gh run view --log-failed`)
and source reads — no speculation. Ready to execute fixes immediately in
the next session, no further research needed for items 1-6. Item 7 needs
one more round of investigation (confirm HA Supervisor's real `/data`
mounting behavior) before deciding the exact fix.
</context>

<next_action>
Start with: `/gsd:quick` using the task description in <remaining_work>
item 1 above (items 1-6). After that completes and commits, run
`/gsd:debug` for item 7.
</next_action>
