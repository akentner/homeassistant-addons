---
phase: 01-quality-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .pre-commit-config.yaml
autonomous: true
requirements:
  - FIX-03

must_haves:
  truths:
    - "Running make lint invokes hadolint on both Dockerfiles"
    - "hadolint produces no errors on fritz-callmonitor2mqtt/Dockerfile and phone-logger/Dockerfile"
  artifacts:
    - path: ".pre-commit-config.yaml"
      provides: "Active hadolint hook entry (not commented out)"
      contains: "id: hadolint"
  key_links:
    - from: ".pre-commit-config.yaml"
      to: "fritz-callmonitor2mqtt/Dockerfile"
      via: "hadolint hook with --ignore flags for HA base-image patterns"
      pattern: "id: hadolint"
    - from: ".pre-commit-config.yaml"
      to: "phone-logger/Dockerfile"
      via: "hadolint hook with --ignore flags for HA base-image patterns"
      pattern: "id: hadolint"
---

<objective>
Re-enable hadolint in .pre-commit-config.yaml so Dockerfiles are linted on every commit (FIX-03).

Purpose: hadolint is currently disabled via a commented-out block. Enabling it with appropriate ignore rules makes the
linting gate complete. Both Dockerfiles use HA-pattern dynamic `FROM $BUILD_FROM` (DL3006) and unpinned apk packages
(DL3018) — these must be explicitly ignored so the hook passes cleanly.

Output: .pre-commit-config.yaml with active hadolint hook using rev v2.14.0 and ignore rules DL3006, DL3018.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-enable hadolint hook with correct ignore rules (FIX-03)</name>
  <files>.pre-commit-config.yaml</files>
  <read_first>
    - .pre-commit-config.yaml (current content — especially the commented hadolint block around lines 45-50)
    - fritz-callmonitor2mqtt/Dockerfile (to understand DL3006 ARG/FROM pattern)
    - phone-logger/Dockerfile (to understand DL3018 apk-without-version pattern and DL3006 ARG/FROM)
  </read_first>
  <action>
    In `.pre-commit-config.yaml`, find the commented-out hadolint block (lines 45-50):

    ```yaml
    # Dockerfile linting (disabled - hadolint not available)
    # - repo: https://github.com/hadolint/hadolint
    #   rev: v2.12.0
    #   hooks:
    #     - id: hadolint
    #       args: [--ignore, DL3008, --ignore, DL3009]  # Ignore apt pinning
    ```

    Replace those 6 commented lines entirely with the active block below. Place it in the same location
    (between the prettier hook and the JSON formatting hook):

    ```yaml
    # Dockerfile linting
    - repo: https://github.com/hadolint/hadolint
      rev: v2.14.0
      hooks:
        - id: hadolint
          args:
            - --ignore
            - DL3006  # ARG before FROM is the HA base-image pattern
            - --ignore
            - DL3018  # apk packages are not version-pinned in HA add-on Dockerfiles
    ```

    Rules being ignored and why:
    - DL3006: "Always tag the version of the image explicitly" — both Dockerfiles use `ARG BUILD_FROM` /
      `FROM $BUILD_FROM` as required by Home Assistant Supervisor; the base image version is set by HA.
    - DL3018: "Pin versions in apk add" — HA add-on Dockerfiles intentionally use unpinned packages;
      the base Alpine version in the HA image pins the package universe.

    Do NOT add DL3008 or DL3009 (those are apt rules; these Dockerfiles use apk).
    Do NOT add DL3059 (multiple RUN) unless hadolint actually fires on it — only ignore what is needed.

  </action>
  <verify>
    <automated>grep -A8 'id: hadolint' /home/akentner/Projects/homeassistant-addons/.pre-commit-config.yaml</automated>
  </verify>
  <acceptance_criteria>
    - `grep '# - repo: https://github.com/hadolint' .pre-commit-config.yaml` returns no match (block is no longer commented out)
    - `grep 'id: hadolint' .pre-commit-config.yaml` returns exactly one uncommented match
    - `grep 'rev: v2.14.0' .pre-commit-config.yaml` returns a match
    - `grep 'DL3006' .pre-commit-config.yaml` returns a match in the args list
    - `grep 'DL3018' .pre-commit-config.yaml` returns a match in the args list
    - `grep -c '^    # - repo:' .pre-commit-config.yaml` returns 0 (no remaining commented-out repo blocks)
    - `pre-commit run hadolint --all-files` exits 0 (run after installing pre-commit hooks)
  </acceptance_criteria>
  <done>hadolint runs on every commit via pre-commit, lints both Dockerfiles, and exits 0 with DL3006 and DL3018 suppressed</done>
</task>

</tasks>

<verification>
After task completes:

```bash
cd /home/akentner/Projects/homeassistant-addons
pre-commit run hadolint --all-files
```

Must exit 0. If hadolint is not yet installed locally, `pre-commit` will install it automatically from the hook repo.

Also verify YAML validity:

```bash
yamllint .pre-commit-config.yaml
```

</verification>

<success_criteria>

- `.pre-commit-config.yaml` contains an active (uncommented) hadolint hook at rev v2.14.0
- args include `--ignore DL3006` and `--ignore DL3018`
- `pre-commit run hadolint --all-files` exits 0
- `make lint` passes without hadolint errors </success_criteria>

<output>
After completion, create `.planning/phases/01-quality-fixes/01-02-SUMMARY.md`
</output>
