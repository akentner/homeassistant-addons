---
phase: 17-git-integration-apply-job-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - iac-runner/config.yaml
  - iac-runner/Dockerfile
autonomous: true
requirements:
  - GIT-01

must_haves:
  truths:
    - "The operator can declare a list of git repos in the add-on Options UI; each entry carries name, url, branch and
      ref, and a malformed name (spaces, slashes, empty) is rejected by the HA Supervisor schema before the add-on
      starts"
    - "The operator can bound total concurrent tofu jobs via max_parallel_jobs (default 4, accepted range 1..32) and cap
      a single run's wall-clock time via apply_timeout_minutes (default 60, accepted range 5..1440)"
    - "The operator can control run-output retention via runs_retention_hours (default 24)"
    - "The running container has the `tofu`, `git` and `ssh` binaries on PATH, so GET /healthz can return 200 instead of
      503 and /v1/plan + /v1/apply + /v1/repos/{name}/pull have executables to invoke"
    - "The tofu binary in the image is a pinned, checksum-verified upstream release — not an unversioned download"
  artifacts:
    - path: "iac-runner/config.yaml"
      provides: "repos list-of-dict option + max_parallel_jobs + apply_timeout_minutes + runs_retention_hours options
        and their HA schema entries"
      contains: "repos:"
    - path: "iac-runner/Dockerfile"
      provides: "Pinned checksum-verified OpenTofu download stage + git/openssh-client in the runtime image"
      contains: "TOFU_VERSION"
  key_links:
    - from: "iac-runner/config.yaml"
      to: "iac-runner/cmd/runner/main.go"
      via: "HA Supervisor renders options into /data/options.json; main.go unmarshals repos/max_parallel_jobs/
        apply_timeout_minutes/runs_retention_hours (wired in plan 17-07)"
      pattern: "repos"
    - from: "iac-runner/Dockerfile"
      to: "iac-runner/internal/httpapi/handlers/healthz.go"
      via: "COPY --from=tofu /usr/local/bin/tofu /usr/bin/tofu satisfies exec.LookPath(\"tofu\")"
      pattern: "tofu"
---

<objective>
Add the four Phase 17 Options fields (`repos`, `max_parallel_jobs`, `apply_timeout_minutes`, `runs_retention_hours`) to
`iac-runner/config.yaml` with HA Supervisor schema validation, and add the three runtime binaries the phase depends on
(`tofu`, `git`, `ssh`) to `iac-runner/Dockerfile`.

Purpose: GIT-01 is literally an Options-schema requirement — the operator declares repos in the HA UI, and Supervisor
rejects malformed entries before the runner ever sees them. The Dockerfile half is a prerequisite discovered during
planning: Phase 16 shipped a runtime image with **no `apk add` at all**, so `tofu`, `git` and `ssh` are all absent.
`handlers/healthz.go:75` calls `exec.LookPath("tofu")` and returns 503 when it fails, and every Phase 17 endpoint shells
out to `git` or `tofu`. Without this task the entire phase is unreachable at runtime, and Phase 16 SC#7 (`/healthz`
returns 200) stays unsatisfied.

Output: 2 files modified. `python3 internal/validate-addon-config.py iac-runner` exits 0, `yamllint iac-runner/config.yaml`
exits 0, `hadolint iac-runner/Dockerfile` reports no errors, `./internal/validate-dockerfile-args.sh
iac-runner/Dockerfile` exits 0, and `docker build` produces an image where `tofu version`, `git --version` and `ssh -V`
all succeed.

Requirements satisfied by this plan:

- GIT-01: `repos` list-of-dict Options + schema (name / url / branch / ref)

Locked decisions implemented here:

- D-04: `max_parallel_jobs` — default 4, range 1..32, integer
- D-15: `apply_timeout_minutes` — default 60, range 5..1440, integer
- D-25: `runs_retention_hours` — default 24 (disk-usage bound for the D-01 run directories)

Explicitly OUT of scope for this plan (owned elsewhere): Go-side option parsing and startup clone (17-07), git
operations (17-03), run storage (17-04), job queue (17-05), HTTP handlers (17-06). Version bump happens once at the end
of the phase in 17-07 — do NOT bump versions here.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-git-integration-apply-job-system/17-CONTEXT.md
@.planning/codebase/CONVENTIONS.md

# Pattern sources — read these BEFORE editing:

@markdown-renderer/config.yaml (the ONLY proven list-of-dict Options pattern in this repo — `directories` mixes required
`name: str` / `path: str` with optional `git_url: str?` / `css: str?`; the inline comment above `plugins` records the
empirical HA constraint that an *optional* list-of-dicts with mixed optional fields breaks Supervisor's schema parser)
@gatus/Dockerfile (lines 9-16 — the proven "alpine download stage + curl + unzip + chmod, then COPY --from into the HA
base" pattern this plan mirrors for the tofu binary)
@network-tools/Dockerfile (lines 1-10 — the proven `apk add --no-cache` multi-package runtime pattern; unpinned apk
versions are the repo-wide convention, hadolint DL3018 is tolerated)
@iac-runner/config.yaml (current state — 7 options, 7 schema entries)
@iac-runner/Dockerfile (current state — two stages, zero apk installs)
</context>

<interfaces>
Phase 16 committed these contracts that this plan extends:

- `iac-runner/config.yaml` current options block (all 7 fields stay untouched):
  `bind_address: "auto"`, `bind_allowed_subnets: []`, `state_backend: "r2"`, `r2_bucket: ""`, `s3_endpoint: ""`,
  `s3_bucket: ""`, `s3_region: ""`

- `iac-runner/config.yaml` current schema block (all 7 entries stay untouched):
  `bind_address: "match(^auto$|^(([0-9]{1,3}\\.){3}[0-9]{1,3})$)?"`, `bind_allowed_subnets: ["str?"]`,
  `state_backend: "list(match(^(r2|s3|local)$))"`, `r2_bucket/s3_endpoint/s3_bucket/s3_region: "str"`

- `iac-runner/internal/httpapi/handlers/healthz.go:75` — `_, lookErr := exec.LookPath("tofu")`; a missing tofu binary
  is what currently forces every `/healthz` response to 503

- `iac-runner/Dockerfile` current structure — `ARG BUILD_FROM` + `ARG RUNNER_VERSION` before the first FROM (enforced by
  `internal/validate-dockerfile-args.sh` via pre-commit), `FROM golang:1.25-alpine AS builder`, then
  `FROM ${BUILD_FROM}` runtime stage that only copies the runner binary and run.sh

NEW contracts this plan publishes (consumed by 17-07's option parsing):

- Options keys, exact names and types: `repos` (list of objects with keys `name`, `url`, `branch`, `ref`),
  `max_parallel_jobs` (int), `apply_timeout_minutes` (int), `runs_retention_hours` (int)
- `tofu` is at `/usr/bin/tofu`, `git` and `ssh` come from Alpine packages on the default PATH
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add repos + max_parallel_jobs + apply_timeout_minutes + runs_retention_hours to config.yaml</name>
  <files>
    - iac-runner/config.yaml
  </files>
  <read_first>
    - iac-runner/config.yaml (current 7 options + 7 schema entries — all must survive unchanged)
    - markdown-renderer/config.yaml (proven list-of-dict schema shape + the HA schema-parser constraint comment)
    - .planning/phases/17-git-integration-apply-job-system/17-CONTEXT.md (D-04, D-15, D-25)
    - .planning/REQUIREMENTS.md (GIT-01)
    - .planning/codebase/CONVENTIONS.md (YAML 2-space indent, quoted strings, snake_case option names)
  </read_first>
  <acceptance_criteria>
    - `options:` block contains `repos: []` (verify: `grep -E '^  repos: \[\]$' iac-runner/config.yaml`)
    - `options:` block contains `max_parallel_jobs: 4`, `apply_timeout_minutes: 60`, `runs_retention_hours: 24`
      (verify: `grep -cE '^  (max_parallel_jobs: 4|apply_timeout_minutes: 60|runs_retention_hours: 24)$'
      iac-runner/config.yaml` returns 3)
    - `schema:` block declares the repos list-of-dict with 4 keys (verify:
      `grep -cE '^    - name: "match\(\^\[a-zA-Z0-9\]|^      url: "match\(|^      branch: "str\?"|^      ref: "str\?"'
      iac-runner/config.yaml` returns 4)
    - `schema:` block declares the three int ranges (verify:
      `grep -cE '^  max_parallel_jobs: "int\(1,32\)"|^  apply_timeout_minutes: "int\(5,1440\)"|^  runs_retention_hours: "int\(1,720\)"'
      iac-runner/config.yaml` returns 3)
    - All 7 pre-existing options survive (verify: `grep -cE '^  (bind_address|bind_allowed_subnets|state_backend|r2_bucket|s3_endpoint|s3_bucket|s3_region):'
      iac-runner/config.yaml` returns 7)
    - No `ingress:` and no `hassio_api:` key was introduced (verify:
      `! grep -E '^(ingress|hassio_api):' iac-runner/config.yaml`)
    - version key is untouched at `0.1.0-0` (verify: `grep -E '^version: "0\.1\.0-0"$' iac-runner/config.yaml`)
    - `python3 internal/validate-addon-config.py iac-runner` exits 0
    - `yamllint iac-runner/config.yaml` exits 0
  </acceptance_criteria>
  <action>
    Edit `iac-runner/config.yaml`. Keep every existing key, ordering, and quoting style. Append the four new keys to the
    END of the `options:` block and their four counterparts to the END of the `schema:` block.

    A. `options:` block — append exactly these four entries after `s3_region: ""`:

    ```yaml
      repos: []
      max_parallel_jobs: 4
      apply_timeout_minutes: 60
      runs_retention_hours: 24
    ```

    Rationale for the defaults (do NOT change them):
    - `repos: []` — empty by default; the add-on starts with zero repos and no clone attempt (GIT-02 is a no-op loop).
    - `max_parallel_jobs: 4` — D-04 default.
    - `apply_timeout_minutes: 60` — D-15 default.
    - `runs_retention_hours: 24` — D-25 / ROADMAP SC-11 default.

    B. `schema:` block — append exactly these four entries after `s3_region: "str"`:

    ```yaml
      repos:
        - name: "match(^[a-zA-Z0-9][a-zA-Z0-9._-]{0,63}$)"
          url: "match(^(git@|ssh://).+$)"
          branch: "str?"
          ref: "str?"
      max_parallel_jobs: "int(1,32)"
      apply_timeout_minutes: "int(5,1440)"
      runs_retention_hours: "int(1,720)"
    ```

    Field-by-field rationale (this is the GIT-01 contract — keep the regexes verbatim):
    - `name` — URI-safe identifier per GIT-01. The regex forces a leading alphanumeric then up to 63 more chars from
      `[a-zA-Z0-9._-]`. This rejects spaces, `/`, `..`, and the empty string, which is what makes
      `/data/repos/<name>/` and the `/v1/repos/{name}/pull` path segment safe by construction.
    - `url` — SSH-only per GIT-01 (`git@github.com:akentner/homelab-infra.git`). The regex accepts the `git@host:path`
      scp-like form and the `ssh://` URL form and rejects `https://` — the runner authenticates with the deploy key at
      `/data/keys/<name>.key`, and an https URL would silently prompt for credentials and hang.
    - `branch` — `str?` (optional). GIT-01 says "default `main`"; HA schema has no default-value syntax for
      list-of-dict members, so the DEFAULT IS APPLIED IN GO: 17-03 treats an empty branch as `"main"`. Do not try to
      express the default here.
    - `ref` — `str?` (optional commit/tag pin per GIT-01, semantics locked by D-10/D-11/D-12 and implemented in 17-03).

    C. HA schema-parser constraint — READ THIS BEFORE ASSUMING THE ABOVE WORKS.
    `markdown-renderer/config.yaml` documents an empirically-hit Supervisor limitation: an optional list-of-dicts with
    mixed optional/required member fields breaks the schema parser for users whose existing `options.json` lacks the
    key. The shape used above is the SAFE variant, identical in structure to markdown-renderer's working `directories`
    list (required `name`/`url` + optional `branch?`/`ref?`), and `repos` is declared with an explicit `[]` default so
    the key is always present in `options.json`.

    If — and only if — `python3 internal/validate-addon-config.py iac-runner` or a local Supervisor schema check
    rejects the block, apply this fallback WITHOUT reducing behavior: change `branch` and `ref` from `"str?"` to
    `"str"` and add them to the `options:` example default as empty strings, i.e.

    ```yaml
      repos: []
    # schema
      repos:
        - name: "match(^[a-zA-Z0-9][a-zA-Z0-9._-]{0,63}$)"
          url: "match(^(git@|ssh://).+$)"
          branch: "str"
          ref: "str"
    ```

    Empty-string `branch`/`ref` keep the exact same Go-side semantics (empty branch → `main`, empty ref → no pin), so
    GIT-01 is still fully delivered. Record which variant shipped in the SUMMARY.

    D. Do NOT touch: `version`, `name`, `slug`, `host_network`, `homeassistant_api`, `services`, `ports`,
    `ports_description`, `arch`, `startup`, `boot`, `init`, `url`. Do NOT add a `map:` block — the runner reads
    everything from `/data`, and `/data/keys/` + `/data/repos/` + `/data/runs/` all live inside the add-on's own data
    volume.

    E. Validate:
    ```
    python3 internal/validate-addon-config.py iac-runner
    yamllint iac-runner/config.yaml
    ```
    Both must exit 0.
  </action>
  <verify>
    <automated>cd /home/akentner/Projects/homeassistant-addons && grep -E '^  repos: \[\]$' iac-runner/config.yaml && test "$(grep -cE '^  (max_parallel_jobs: 4|apply_timeout_minutes: 60|runs_retention_hours: 24)$' iac-runner/config.yaml)" = "3" && test "$(grep -cE '^    - name: "match\(\^\[a-zA-Z0-9\]|^      url: "match\(|^      branch: "str\??"|^      ref: "str\??"' iac-runner/config.yaml)" = "4" && test "$(grep -cE '^  max_parallel_jobs: "int\(1,32\)"|^  apply_timeout_minutes: "int\(5,1440\)"|^  runs_retention_hours: "int\(1,720\)"' iac-runner/config.yaml)" = "3" && test "$(grep -cE '^  (bind_address|bind_allowed_subnets|state_backend|r2_bucket|s3_endpoint|s3_bucket|s3_region):' iac-runner/config.yaml)" = "7" && ! grep -E '^(ingress|hassio_api):' iac-runner/config.yaml && grep -E '^version: "0\.1\.0-0"$' iac-runner/config.yaml && python3 internal/validate-addon-config.py iac-runner && yamllint iac-runner/config.yaml</automated>
  </verify>
  <done>iac-runner/config.yaml exposes `repos` (list of {name, url, branch, ref} with Supervisor-side regex validation on name and SSH-only url), `max_parallel_jobs` (default 4, int(1,32)), `apply_timeout_minutes` (default 60, int(5,1440)), and `runs_retention_hours` (default 24, int(1,720)). All seven Phase 16 options and their schema entries are unchanged, the version key is untouched, and both the add-on config validator and yamllint pass.</done>
</task>

<task type="auto">
  <name>Task 2: Add pinned checksum-verified tofu + git + openssh-client to the runtime image</name>
  <files>
    - iac-runner/Dockerfile
  </files>
  <read_first>
    - iac-runner/Dockerfile (current two-stage build; ARG-before-FROM ordering is enforced by pre-commit)
    - gatus/Dockerfile (lines 1-20 — proven alpine download stage with curl+unzip, then COPY --from)
    - network-tools/Dockerfile (lines 1-10 — proven `apk add --no-cache` runtime install)
    - iac-runner/internal/httpapi/handlers/healthz.go (the exec.LookPath("tofu") probe this task unblocks)
    - internal/validate-dockerfile-args.sh (the ARG-before-FROM rule this file must keep satisfying)
    - iac-runner/internal/version/version.go (MinSupportedOpenTofu = "1.6.0", MaxSupportedOpenTofu = "1.999.0" — the
      pinned TOFU_VERSION must fall inside this window)
  </read_first>
  <acceptance_criteria>
    - `ARG TOFU_VERSION=` is declared before the first FROM (verify:
      `awk '/^FROM/{exit} /^ARG TOFU_VERSION=/{found=1} END{exit !found}' iac-runner/Dockerfile`)
    - A dedicated download stage exists (verify: `grep -E '^FROM alpine:[0-9.]+ AS tofu$' iac-runner/Dockerfile`)
    - The download stage verifies a checksum (verify:
      `grep -cE 'SHA256SUMS|sha256sum -c' iac-runner/Dockerfile` returns 2)
    - The tofu binary is copied into the runtime image at /usr/bin/tofu (verify:
      `grep -E '^COPY --from=tofu .*/usr/bin/tofu$' iac-runner/Dockerfile`)
    - git and openssh-client are installed in the runtime stage (verify:
      `grep -E 'apk add --no-cache.*(git|openssh-client)' iac-runner/Dockerfile`)
    - The builder stage and the runner binary COPY are unchanged (verify:
      `grep -cE '^FROM golang:1\.25-alpine AS builder$|^COPY --from=builder /workspace/runner /usr/bin/runner$'
      iac-runner/Dockerfile` returns 2)
    - EXPOSE 8125 and CMD ["/run.sh"] survive (verify:
      `grep -cE '^EXPOSE 8125$|^CMD \["/run\.sh"\]$' iac-runner/Dockerfile` returns 2)
    - `./internal/validate-dockerfile-args.sh iac-runner/Dockerfile` exits 0
    - `hadolint iac-runner/Dockerfile` produces no error-level findings (DL3018 unpinned-apk warnings are the repo-wide
      accepted convention — see network-tools/Dockerfile)
    - The built image has all three binaries (verify:
      `docker run --rm --entrypoint sh <image> -c 'tofu version && git --version && ssh -V'` exits 0)
  </acceptance_criteria>
  <action>
    Edit `iac-runner/Dockerfile`. Three concrete changes; everything else stays byte-identical.

    A. Add `ARG TOFU_VERSION=1.10.6` to the global ARG block, directly after `ARG RUNNER_VERSION=dev` and BEFORE the
    first `FROM`. The ARG-before-FROM placement is mandatory — `internal/validate-dockerfile-args.sh` runs on this exact
    file via pre-commit (`files: ^(phone-logger|meridian|iac-runner)/Dockerfile$`) and fails the commit otherwise.

    VERSION PIN CONTINGENCY: `1.10.6` is the pinned target. If the release asset 404s during the build, resolve the
    newest available `v1.x.y` tag from
    `https://api.github.com/repos/opentofu/opentofu/releases/latest`, use that version instead, and record the actual
    pinned version in the SUMMARY. The pin MUST stay inside the
    `internal/version/version.go` window (>= MinSupportedOpenTofu "1.6.0", < MaxSupportedOpenTofu "1.999.0"). Never
    replace the pin with `latest` — an unpinned binary breaks build reproducibility.

    B. Insert a new download stage between the builder stage and the runtime `FROM ${BUILD_FROM}` stage, mirroring
    gatus/Dockerfile's stage-2 pattern:

    ```dockerfile
    # Stage 2: OpenTofu download — pinned version + upstream SHA256 verification. Kept in its own
    # alpine stage so curl/unzip never land in the runtime image (mirror gatus/Dockerfile stage 2).
    FROM alpine:3.22 AS tofu

    ARG TOFU_VERSION

    RUN apk add --no-cache curl unzip && \
        curl -fsSL -o /tmp/tofu.zip \
            "https://github.com/opentofu/opentofu/releases/download/v${TOFU_VERSION}/tofu_${TOFU_VERSION}_linux_amd64.zip" && \
        curl -fsSL -o /tmp/SHA256SUMS \
            "https://github.com/opentofu/opentofu/releases/download/v${TOFU_VERSION}/tofu_${TOFU_VERSION}_SHA256SUMS" && \
        cd /tmp && \
        grep " tofu_${TOFU_VERSION}_linux_amd64.zip\$" SHA256SUMS | sha256sum -c - && \
        unzip -j /tmp/tofu.zip tofu -d /usr/local/bin && \
        chmod +x /usr/local/bin/tofu && \
        /usr/local/bin/tofu version
    ```

    The trailing `tofu version` is a build-time smoke test: a corrupt or wrong-arch binary fails the build instead of
    shipping an image whose `/healthz` silently 503s.

    C. In the runtime stage (`FROM ${BUILD_FROM}`), add the binary copy and the two Alpine packages BEFORE the existing
    `COPY run.sh /run.sh` line:

    ```dockerfile
    # Phase 17 runtime dependencies:
    #   git + openssh-client — GIT-02/GIT-03 clone + `git pull --ff-only` over SSH using the deploy key at
    #     /data/keys/<name>.key with /data/keys/known_hosts (invoked via GIT_SSH_COMMAND).
    #   tofu — RUN-02/RUN-03 `tofu init/plan/apply`. handlers/healthz.go probes exec.LookPath("tofu") and returns
    #     503 without it, so this COPY is what makes Phase 16 SC#7 satisfiable.
    RUN apk add --no-cache git openssh-client

    COPY --from=tofu /usr/local/bin/tofu /usr/bin/tofu
    RUN chmod +x /usr/bin/tofu
    ```

    Unpinned `apk add --no-cache` matches every other Dockerfile in this repo (network-tools, markdown-renderer,
    meridian, gatus) — do not introduce version pins here and do not add a `.hadolint.yaml`; the repo has none and
    DL3018 is tolerated project-wide.

    D. Do NOT change: the `ARG BUILD_FROM=ghcr.io/home-assistant/amd64-base:3.24` default, the builder stage
    (`FROM golang:1.25-alpine AS builder`, `go mod download`, the `CGO_ENABLED=0 ... -X main.runnerVersion=` build),
    `COPY --from=builder /workspace/runner /usr/bin/runner`, `EXPOSE 8125`, `CMD ["/run.sh"]`, or the LABEL block.

    E. Validate:
    ```
    ./internal/validate-dockerfile-args.sh iac-runner/Dockerfile
    hadolint iac-runner/Dockerfile
    docker build --build-arg BUILD_FROM=ghcr.io/home-assistant/amd64-base:3.24 -t iac-runner:phase17-01 iac-runner/
    docker run --rm --entrypoint sh iac-runner:phase17-01 -c 'tofu version && git --version && ssh -V'
    ```
    The `docker run` line must print a tofu version, a git version, and an OpenSSH version, and exit 0. If the local
    environment has no Docker daemon, run `make docker-build-check` instead and record in the SUMMARY that the
    binary-presence assertion is deferred to CI — do NOT mark the task done on a skipped build without recording it.
  </action>
  <verify>
    <automated>cd /home/akentner/Projects/homeassistant-addons && awk '/^FROM/{exit} /^ARG TOFU_VERSION=/{found=1} END{exit !found}' iac-runner/Dockerfile && grep -E '^FROM alpine:[0-9.]+ AS tofu$' iac-runner/Dockerfile && test "$(grep -cE 'SHA256SUMS|sha256sum -c' iac-runner/Dockerfile)" -ge 2 && grep -E '^COPY --from=tofu .*/usr/bin/tofu$' iac-runner/Dockerfile && grep -E 'apk add --no-cache.*git' iac-runner/Dockerfile && grep -E 'apk add --no-cache.*openssh-client' iac-runner/Dockerfile && test "$(grep -cE '^FROM golang:1\.25-alpine AS builder$|^COPY --from=builder /workspace/runner /usr/bin/runner$' iac-runner/Dockerfile)" = "2" && test "$(grep -cE '^EXPOSE 8125$|^CMD \["/run\.sh"\]$' iac-runner/Dockerfile)" = "2" && ./internal/validate-dockerfile-args.sh iac-runner/Dockerfile && hadolint iac-runner/Dockerfile</automated>
  </verify>
  <done>iac-runner/Dockerfile declares ARG TOFU_VERSION before the first FROM, downloads the pinned OpenTofu release in a dedicated alpine stage, verifies it against the upstream SHA256SUMS file, smoke-tests `tofu version` at build time, copies the binary to /usr/bin/tofu in the runtime stage, and installs git + openssh-client via apk. The builder stage, runner-binary COPY, EXPOSE 8125, CMD and LABEL block are untouched. validate-dockerfile-args.sh and hadolint pass, and the built image runs `tofu version`, `git --version` and `ssh -V` successfully.</done>
</task>

</tasks>

<verification>
1. `python3 internal/validate-addon-config.py iac-runner` exits 0
2. `yamllint iac-runner/config.yaml` exits 0
3. `./internal/validate-dockerfile-args.sh iac-runner/Dockerfile` exits 0
4. `hadolint iac-runner/Dockerfile` — no error-level findings
5. `docker build -t iac-runner:phase17-01 iac-runner/` succeeds; `tofu version && git --version && ssh -V` inside the
   image exits 0
6. `grep -c 'repos' iac-runner/config.yaml` >= 2 (options + schema)
7. No version files were touched: `git diff --name-only` lists exactly `iac-runner/config.yaml` and
   `iac-runner/Dockerfile`
</verification>

<success_criteria>
- The four Phase 17 Options exist with the exact defaults from D-04 (4), D-15 (60), D-25 (24) and an empty `repos` list
- The `repos` schema validates `name` as a URI-safe identifier and `url` as SSH-only, per GIT-01
- `tofu`, `git` and `ssh` are on PATH in the built image, unblocking `/healthz` 200 and all Phase 17 endpoints
- The OpenTofu binary is pinned and checksum-verified, and the pin sits inside the version.go support window
- Every Phase 16 option, schema entry and Dockerfile stage is preserved
</success_criteria>

<output>
After completion, create `.planning/phases/17-git-integration-apply-job-system/17-01-SUMMARY.md`.

Record explicitly: which `repos` schema variant shipped (optional `str?` or fallback `str`), the actual pinned
TOFU_VERSION, and whether the docker-build binary assertion ran locally or was deferred to CI.
</output>
