---
phase: 21-cups-print-server-addon-airprint-mdns-fixes
plan: 03
type: execute
wave: 3
depends_on: ["21-01", "21-02"]
files_modified:
  - internal/cups-migration-suggestion.sh
  - cups/DOCS.md
autonomous: false
requirements: [D-12, D-13]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "A read-only script exists that queries f1c878cb_cups's live configuration on haos-op3050-1 and prints a suggested printers: YAML snippet (D-13)"
    - "The cups add-on is installed and running on haos-op3050-1, verified via ha apps info cups reporting a started state (D-12)"
    - "avahi-resolve -a <haos-op3050-1 IP> resolves to the fixed avahi_hostname with no auto-renamed -2 suffix, and the add-on's own logs contain zero occurrences of the slot-exhaustion message — both checked automatically, matching the exact symptoms cups/DIAGNOSIS.md documented against the old add-on"
    - "f1c878cb_cups is uninstalled from haos-op3050-1 ONLY after explicit human confirmation that AirPrint works against the new add-on (D-12, and this repo's CLAUDE.md ask-first-for-removals rule)"
  artifacts:
    - internal/cups-migration-suggestion.sh
  key_links:
    - "internal/cups-migration-suggestion.sh's `ha apps info f1c878cb_cups` read -> printed printers: suggestion -> human pastes it into the cups add-on's options via the HA UI (manual, per D-13's explicit no-automated-migration requirement)"
    - "checkpoint:human-action confirmation -> ha apps uninstall f1c878cb_cups (the one destructive call in this plan, gated behind explicit confirmation)"
---

<objective>
Complete the phase's rollout scope (D-12, D-13): install the new `cups` add-on on the real HAOS host `haos-op3050-1`,
empirically prove the AirPrint/mDNS fixes hold against the live host (not just the Plan 21-01 docker smoke test),
give the operator a ready-to-paste `printers` option suggestion derived from the old add-on's live config, and only
then — after explicit human confirmation that AirPrint actually works — remove `f1c878cb_cups`.

Purpose: this phase does not just produce a repo artifact; the user's real AirPrint bug is only fixed once the new
add-on replaces the old one on the host that was actually failing.
Output: `cups` running on haos-op3050-1, `f1c878cb_cups` removed, a reusable migration-suggestion script, and
cups/DOCS.md's migration placeholder filled in.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-cups-print-server-addon-airprint-mdns-fixes/21-CONTEXT.md
@cups/DIAGNOSIS.md
@cups/config.yaml
@cups/DOCS.md
@internal/verify-image-availability.sh
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces (full phase list — cumulative across 21-01, 21-02, 21-03)

Created by Plan 21-01 (not this plan): `cups/config.yaml`, `cups/build.yaml`, `cups/Dockerfile`, `cups/run.sh`,
`cups/generate_config.py`, `internal/verify-cups-scaffold.sh`.

Created by Plan 21-02 (not this plan): `internal/base-image-config.yaml` (modified), `cups/README.md`,
`cups/DOCS.md`, root `README.md` (modified).

Created/modified by **this plan (21-03)**:
- `internal/cups-migration-suggestion.sh` — read-only SSH query against haos-op3050-1 + printed `printers:`
  suggestion (D-13)
- `cups/DOCS.md` — the "Migrating from f1c878cb_cups" placeholder section (created empty by Plan 21-02) is filled in
  with the real, executed procedure

No functional code artifact beyond the script above — the rest of this plan's work is a live-host operational
rollout, not a repo file.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Read f1c878cb_cups's live config and print a printers: migration suggestion (D-13)</name>
  <precondition>haos-op3050-1 is reachable via `ssh haos-op3050-1 true` and the `ha` CLI wrapper resolves (per
  cups/DIAGNOSIS.md's "Access &amp; Tooling" section — this has already been proven reachable during the diagnosis
  session)</precondition>
  <read_first>
    cups/DIAGNOSIS.md ("Access &amp; Tooling" section — the exact `ha apps info f1c878cb_cups` command and its
    sample output are the input this script parses; "Raw evidence" section shows the real field shapes), cups/config.yaml
    (the exact printers schema shape from Plan 21-01 — name/uri/enabled — the suggestion's output must match it
    field-for-field)
  </read_first>
  <files>internal/cups-migration-suggestion.sh</files>
  <action>
    Create `internal/cups-migration-suggestion.sh` (bash, mirrors the internal/spike-*.sh scripts' shape — a
    read-only, single-purpose operational script, not a pre-commit hook). It runs `ssh haos-op3050-1 ha apps info
    f1c878cb_cups` (read-only Supervisor query — no `ha apps restart`/`uninstall`/any mutating verb anywhere in this
    script) and extracts whatever printer queue information is present (CUPS printer name + device URI — if
    `ha apps info` does not itself expose per-printer detail, additionally SSH-run `lpstat -v` against the OLD
    add-on's container, e.g. `ssh haos-op3050-1 docker exec addon_f1c878cb_cups lpstat -v`, to enumerate registered
    printers and their device URIs). For each printer found, print one YAML entry matching cups/config.yaml's exact
    schema shape: `- name: "<name>"` / `  uri: "<uri>"` / `  enabled: true`, wrapped under a `printers:` key, ready to
    paste directly into the new add-on's Options UI. If no printers are found (or the old add-on is already gone),
    print a clear message and exit 0 rather than emitting an empty/misleading suggestion.
  </action>
  <verify>
    <automated>shellcheck -e SC1091 -e SC2034 internal/cups-migration-suggestion.sh</automated>
    <fails_when>shellcheck exits non-zero</fails_when>
  </verify>
  <acceptance_criteria>
    - The script contains no `ha apps install`, `ha apps uninstall`, `ha apps restart`, or any other mutating
      Supervisor verb — grep confirms only `ha apps info` / `lpstat` (read-only)
    - Output is valid YAML that parses under `python3 -c "import yaml,sys; yaml.safe_load(sys.stdin)"` when the old
      add-on has at least one registered printer
  </acceptance_criteria>
  <done>A reusable, read-only script exists and is committed that produces a ready-to-paste printers: suggestion
  from the live f1c878cb_cups configuration (D-13).</done>
</task>

<task type="auto">
  <name>Install and start cups on haos-op3050-1; verify the mDNS fixes empirically</name>
  <precondition>the `cups` image is published to ghcr.io — confirm via `./internal/verify-image-availability.sh`
  reporting `ok` for the `akentner/homeassistant-addons/amd64-cups` repo path at the version in cups/config.yaml; if
  it reports `missing`, the Wave 1/2 commits have not yet been built by CI — wait for the build.yml run triggered by
  the push and retry, do not proceed</precondition>
  <read_first>
    cups/DIAGNOSIS.md (the exact `avahi-resolve -a`, `avahi-browse -rp`, and log-grep commands used to diagnose the
    original bug — reuse these verbatim as the empirical proof this task's fix actually holds on the real host),
    internal/verify-image-availability.sh (ghcr availability probe this task's precondition reuses)
  </read_first>
  <files>(no repo files — live-host operational task only)</files>
  <action>
    Over SSH to haos-op3050-1: confirm this repository is already added as a custom add-on repository (it must be,
    since `f1c878cb_cups`'s replacement lives in this same repo alongside gatus/meridian/network-tools, which are
    already installed there per STATE.md's deploy history); refresh the add-on store if needed; `ha apps install
    cups`; start it with the default (empty) `printers` list — do NOT paste the Task 1 suggestion into it yet, that
    step is explicitly the human's job per D-13. Once running, empirically re-run the exact diagnostic commands
    cups/DIAGNOSIS.md used against the OLD add-on, now against the NEW one: `avahi-resolve -a 192.168.178.3` must
    return the add-on's `avahi_hostname` value with NO auto-renamed `-2` suffix (proves D-11); `ha apps logs cups`
    must contain zero occurrences of "No slot available for legacy unicast reflection" (proves D-07); confirm
    `avahi-resolve -n <avahi_hostname>.local` (not the `-2` variant) resolves without timing out. Capture this
    output verbatim for the SUMMARY, mirroring how Phase 9's spike scripts pasted their transcripts into
    `09-SUMMARY.md`.
  </action>
  <verify>
    <automated>ssh haos-op3050-1 ha apps info cups</automated>
    <fails_when>the SSH command fails, or the returned info does not show state: started</fails_when>
  </verify>
  <acceptance_criteria>
    - `ha apps info cups` on haos-op3050-1 reports `state: started`
    - `avahi-resolve -a 192.168.178.3` returns the configured `avahi_hostname` with no `-2` suffix
    - `ha apps logs cups` contains zero occurrences of "No slot available for legacy unicast reflection"
  </acceptance_criteria>
  <done>cups is installed, running, and the core mDNS fixes (D-07, D-11) are empirically proven against the real
  host that originally exhibited the bug — captured as a transcript for the SUMMARY.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Confirm AirPrint works before removing the old add-on</name>
  <read_first>
    cups/DOCS.md ("Migrating from f1c878cb_cups" placeholder section — this checkpoint's instructions become that
    section's real content once resolved), the Task 1 script's printed suggestion, the Task 2 diagnostic transcript
  </read_first>
  <action>
    Present: (1) the Task 1 script's printed `printers:` suggestion; (2) the Task 2 automated diagnostic results
    (hostname resolves correctly, zero slot-exhaustion log lines). Ask the human to: review/edit the suggested
    `printers:` snippet and paste it into the new `cups` add-on's Options via the HA UI, restart the add-on, then
    physically attempt to print from an iOS or macOS device against the shared printer — this is the one step in
    the phase that genuinely cannot be automated (no CLI/API exists for "does AirPrint actually work from a real
    device"). Do not proceed to Task 4 (uninstalling `f1c878cb_cups`) until the human confirms.
  </action>
  <instructions>
    I have installed and started `cups` on haos-op3050-1 and confirmed the mDNS fixes hold (hostname resolves
    without a -2 suffix, zero reflector-slot-exhaustion log lines). I have NOT configured your printers yet — per
    the phase's design, only you should paste the printers option (I only suggest it, per D-13).

    1. Review the printers: suggestion above (edit names/URIs if anything looks wrong)
    2. Paste it into the cups add-on's Options in the HA UI, save, restart the add-on
    3. From an iPhone/iPad/Mac, try to print to the shared printer via AirPrint
    4. Confirm it works
  </instructions>
  <verification>Once you confirm, I will remove f1c878cb_cups and confirm its removal via `ha apps list`.</verification>
  <acceptance_criteria>
    - The human has explicitly confirmed AirPrint prints successfully from a real device against the new add-on
    - No automated step in this plan attempts to substitute for that physical confirmation
  </acceptance_criteria>
  <resume-signal>Type "confirmed" once AirPrint has printed successfully from a real device, or describe what went
  wrong</resume-signal>
</task>

<task type="auto">
  <name>Remove f1c878cb_cups now that the replacement is confirmed working</name>
  <precondition>the Task 3 checkpoint has been explicitly confirmed by the human</precondition>
  <read_first>
    CLAUDE.md (repo root — "Before taking action on critical tasks (version bumps, deployment changes, add-on
    removals, CI/CD modifications): Ask first, act later" — this task is exactly that ask-first-then-act sequence,
    already satisfied by Task 3's checkpoint)
  </read_first>
  <files>cups/DOCS.md</files>
  <action>
    Run `ssh haos-op3050-1 ha apps uninstall f1c878cb_cups`; confirm via `ssh haos-op3050-1 ha apps list` that
    `f1c878cb_cups` no longer appears and `cups` (the new add-on) still shows `state: started`. Then replace
    cups/DOCS.md's "Migrating from f1c878cb_cups" placeholder (left by Plan 21-02) with the real procedure just
    executed: read the migration-suggestion script's output, paste into the new add-on's Options, restart, verify
    AirPrint from a real device, then uninstall the old add-on — written as a reusable runbook for anyone repeating
    this migration on a different host.
  </action>
  <verify>
    <automated>ssh haos-op3050-1 ha apps list</automated>
    <fails_when>the SSH command fails, or f1c878cb_cups still appears in the list, or cups does not show state: started</fails_when>
  </verify>
  <acceptance_criteria>
    - `f1c878cb_cups` is absent from `ha apps list` on haos-op3050-1
    - `cups` shows `state: started` on haos-op3050-1
    - cups/DOCS.md's migration section no longer contains the Plan 21-02 placeholder text
  </acceptance_criteria>
  <done>The rollout is complete: the new add-on replaces the old one on the host that had the original bug, removed
  only after explicit human confirmation (D-12).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| This workstation (operator terminal) -> haos-op3050-1 (live production Supervisor host) via SSH | Every command in this plan crosses this boundary; Tasks 2 and 4 mutate a production host's running add-on set |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-06 | Elevation of Privilege / Tampering | `ha apps uninstall f1c878cb_cups` — destructive Supervisor mutation on a live production host | high | mitigate | Gated behind an explicit `checkpoint:human-action` with `gate="blocking-human"` (never auto-approved, even in auto-mode) per this repo's CLAUDE.md ask-first-for-removals rule; the uninstall only runs in Task 4, after Task 3's confirmation |
| T-21-07 | Information Disclosure | internal/cups-migration-suggestion.sh reads f1c878cb_cups's live printer config via `ha apps info`/`lpstat` over SSH | low | accept | Read-only Supervisor/CUPS queries (the same command class cups/DIAGNOSIS.md already ran non-interactively); output is printer names/URIs already visible via `ha apps info`, not credentials |
</threat_model>

<verification>
Task 1's shellcheck + read-only-verb grep run first and are independent of the live host. Tasks 2-4 are strictly
sequential against the real haos-op3050-1 host and cannot be parallelized or re-ordered — Task 3's checkpoint is the
hard gate between "new add-on running" and "old add-on removed".
</verification>

<success_criteria>
- internal/cups-migration-suggestion.sh exists, is read-only, and produces a schema-correct printers: suggestion
- cups is installed and running on haos-op3050-1 with the D-07/D-11 fixes empirically proven against the real host
- f1c878cb_cups is removed only after explicit human confirmation that AirPrint works
- cups/DOCS.md's migration section documents the real, executed procedure (no placeholder left)
</success_criteria>

<output>
Create `.planning/phases/21-cups-print-server-addon-airprint-mdns-fixes/21-03-SUMMARY.md` when done
</output>
