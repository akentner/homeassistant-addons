---
phase: 21-cups-print-server-addon-airprint-mdns-fixes
plan: 02
type: execute
wave: 2
depends_on: ["21-01"]
files_modified:
  - internal/base-image-config.yaml
  - cups/README.md
  - cups/DOCS.md
  - README.md
autonomous: true
requirements: [D-06, D-08]

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "internal/base-image-config.yaml lists a cups: entry using the same amd64-base + home-assistant/docker-base tracking pattern as gatus/meridian/network-tools (D-06), so the existing daily base-image-update.yml workflow picks it up with no workflow edits"
    - "cups/ has no .upstream.yaml (D-06 — there is no external upstream project to sync a version tag from; CUPS/Avahi come from apk packages baked into the base image)"
    - "cups/README.md and cups/DOCS.md document every D-05 option with its default and purpose, including an explicit, findable note that avahi_reflector defaults to no and why (D-07), and that no slot-exhaustion watchdog exists by design (D-08)"
    - "Root README.md lists the cups add-on in the repository's Add-ons section alongside every other add-on"
  artifacts:
    - internal/base-image-config.yaml
    - cups/README.md
    - cups/DOCS.md
    - README.md
  key_links:
    - "internal/base-image-config.yaml's cups: entry -> .github/workflows/base-image-update.yml's addon-name loop (auto-discovered from the YAML map's keys, no workflow file edit needed)"
    - "cups/DOCS.md options table -> cups/config.yaml's actual option names/defaults from Plan 21-01 (must stay in sync)"
---

<objective>
Wire the new `cups/` add-on into this repo's cross-cutting conventions that Plan 21-01 deliberately left out of the
tracer: base-image version tracking (D-06), operator-facing documentation of the full options surface (D-05, D-07,
D-08, D-10, D-11), and the root README's add-on listing. No functional code changes — this plan makes the add-on
discoverable and documented, not more capable.

Purpose: close the "no .upstream.yaml, use base-image-config.yaml instead" decision (D-06) and give an operator
enough documentation to actually configure the add-on without reading cups/DIAGNOSIS.md or the source.
Output: internal/base-image-config.yaml (modified), cups/README.md, cups/DOCS.md, root README.md (modified).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-cups-print-server-addon-airprint-mdns-fixes/21-CONTEXT.md
@cups/DIAGNOSIS.md
@internal/base-image-config.yaml
@gatus/README.md
@gatus/DOCS.md
@network-tools/README.md
@network-tools/DOCS.md
@README.md
@cups/config.yaml
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces (full phase list — cumulative across 21-01, 21-02, 21-03)

Created by Plan 21-01 (not this plan): `cups/config.yaml`, `cups/build.yaml`, `cups/Dockerfile`, `cups/run.sh`,
`cups/generate_config.py`, `internal/verify-cups-scaffold.sh`.

Created/modified by **this plan (21-02)**:
- `internal/base-image-config.yaml` — adds a `cups:` entry (modified)
- `cups/README.md` — badges, About, Features, Configuration skeleton (mirrors gatus/README.md)
- `cups/DOCS.md` — full options table + D-07/D-08 rationale section (mirrors gatus/DOCS.md)
- `README.md` — adds a `### [CUPS Print Server](./cups)` entry (modified)

Created by Plan 21-03 (not this plan): `internal/cups-migration-suggestion.sh`; also appends a "Migrating from
f1c878cb_cups" section to `cups/DOCS.md` (created by this plan).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Track cups' base image via internal/base-image-config.yaml (D-06)</name>
  <read_first>
    internal/base-image-config.yaml (full file — gatus:/meridian:/network-tools: entries are the exact 4-line block
    to copy), .github/workflows/base-image-update.yml (confirms the addon-name loop reads
    `internal/base-image-config.yaml`'s `addons:` map keys directly via a small inline Python block — adding a
    `cups:` key is sufficient, no workflow YAML edit required), cups/config.yaml (from Plan 21-01 — confirms the
    base image tag currently pinned, must match)
  </read_first>
  <files>internal/base-image-config.yaml</files>
  <action>
    Add a `cups:` entry to `internal/base-image-config.yaml`'s `addons:` map, placed after the existing
    `phone-logger:` entry (end of the map), with the identical 4 keys the `gatus:`/`meridian:`/`network-tools:`
    entries already use: `image: "ghcr.io/home-assistant/amd64-base"`, `source_repo: "home-assistant/docker-base"`,
    `source_file: "alpine/Dockerfile"`, `version_pattern: 'ARG ALPINE_VERSION=(?P<version>\d+\.\d+)'` — byte-identical
    to those three entries (D-06: cups uses the plain `amd64-base` family, not `amd64-base-python`, because
    `python3` here is just an apk package for generate_config.py and does not change which base-image family this
    add-on tracks). Do NOT create `cups/.upstream.yaml` — D-06 explicitly rejects the upstream-tag-tracking model for
    this add-on since CUPS/Avahi ship as Alpine packages baked into the base image, not a downloadable release
    tarball.
  </action>
  <verify>
    <automated>python3 -c "import yaml; cfg = yaml.safe_load(open('internal/base-image-config.yaml')); assert 'cups' in cfg['addons'], 'cups entry missing'; e = cfg['addons']['cups']; assert e['image'] == 'ghcr.io/home-assistant/amd64-base'; assert e['source_repo'] == 'home-assistant/docker-base'; assert e['source_file'] == 'alpine/Dockerfile'; assert 'ALPINE_VERSION' in e['version_pattern']; print('OK')" && test ! -f cups/.upstream.yaml && echo "no-upstream-yaml-confirmed"</automated>
    <fails_when>the python3 assertion raises (missing key or wrong value), or cups/.upstream.yaml exists (test ! -f fails)</fails_when>
  </verify>
  <acceptance_criteria>
    - `internal/base-image-config.yaml`'s `addons.cups` block has all 4 keys, matching gatus/meridian/network-tools byte-for-byte except the map key name
    - No `cups/.upstream.yaml` file exists anywhere in the tree
    - `.github/workflows/base-image-update.yml` is NOT modified by this task (its addon loop already derives the list from the YAML map's keys)
  </acceptance_criteria>
  <done>cups is tracked by the daily base-image-update.yml workflow via internal/base-image-config.yaml, with no .upstream.yaml and no workflow file changes (D-06).</done>
</task>

<task type="auto">
  <name>Write cups/README.md and cups/DOCS.md (D-05, D-07, D-08, D-10, D-11)</name>
  <read_first>
    gatus/README.md (badges/About/Features/Configuration skeleton to mirror), gatus/DOCS.md (options-table shape,
    `| Option | Default | Description |` format), network-tools/README.md, network-tools/DOCS.md (German-language
    precedent exists in this repo — cups/README.md and cups/DOCS.md should stay English to match gatus/meridian,
    since DIAGNOSIS.md and CONTEXT.md are both English), cups/config.yaml (from Plan 21-01 — the authoritative
    source for every option name/default this task documents; do not invent option names not present there),
    cups/DIAGNOSIS.md ("Findings" + "Ideas for a permanent fix" sections — the root-cause narrative this README's
    About section summarizes for an operator who has never seen the diagnosis)
  </read_first>
  <files>cups/README.md, cups/DOCS.md</files>
  <action>
    Create `cups/README.md` mirroring gatus/README.md's exact section order: version/license badge line (`v0.1.0`,
    matching cups/config.yaml's version from Plan 21-01), `## About` (2-3 sentences: this add-on shares printers via
    CUPS with working AirPrint/mDNS discovery, forked from f1c878cb_cups after diagnosing a specific Avahi
    reflector-slot-exhaustion bug — link to DIAGNOSIS.md is optional since it is a working-notes file, not shipped
    doc), `## Features` (bullet list: AirPrint-compatible printer sharing via CUPS + driverless `everywhere` IPP,
    Avahi mDNS reflector disabled by default to prevent the legacy-unicast slot-exhaustion bug that this add-on
    exists to fix, fixed/stable mDNS hostname independent of container restarts, IPv6 mDNS resolution disabled by
    default, multiple printers configurable from a single options list), `## Configuration` (a minimal example
    `printers:` YAML snippet using the exact schema shape Plan 21-01 shipped — one `{name, uri, enabled}` entry with
    a realistic `ipp://` URI, matching whichever schema type — str or url — the Task 1 checkpoint in Plan 21-01
    resolved to). Close with the same shield-link reference block gatus/README.md uses at the bottom
    ([release-shield], [release], [maintenance-shield], [project-stage-shield]) — reuse the shared `[amd64-shield]`
    definition already at the bottom of root README.md, do not redefine it per-add-on (gatus/README.md defines its
    own copy; either approach is acceptable, match gatus's self-contained pattern for consistency within this file).

    Create `cups/DOCS.md` mirroring gatus/DOCS.md's `## Add-on Options` table shape exactly
    (`| Option | Default | Description |`), one row per D-05 option: `avahi_reflector` (default `false`) —
    "Disabled by default. Avahi's legacy-unicast reflector keeps a fixed-size in-memory slot table that fills up
    under sustained legacy-unicast mDNS traffic (e.g. from a mesh Wi-Fi repeater) and silently drops all further
    mDNS queries once full, including resolves of this add-on's own advertised printers — the exact bug this add-on
    exists to fix. Only re-enable this if the add-on is run WITHOUT host_network: true, where reflection across
    network namespaces would actually be needed."; `avahi_hostname` (default `cups`) — "Fixed Avahi host-name,
    independent of the container's transient hostname. Prevents the auto-rename-on-conflict behavior
    (`<hostname>-2`) that made the printer's advertised mDNS name diverge from its actual resolvable address.";
    `avahi_use_ipv6` (default `false`) — "Disabled by default. Avahi may resolve the add-on's mDNS hostname to an
    IPv6 ULA address that is unreachable/unrouted for some client devices, independent of the reflector bug.";
    `printers` (default `[]`) — full sub-table of `name`/`uri`/`enabled` fields, with 3-4 example `uri` values
    covering the schemes this add-on supports (`ipp://<host>:631/ipp/print`, `socket://<host>:9100`,
    `usb://<vendor>/<model>?serial=...`), and an explicit note on which scheme validation applies (str + runtime
    allowlist, or url — matching whatever Plan 21-01's checkpoint resolved to); `log_level` (default `info`). Add a
    `## Design notes` section documenting D-08 explicitly and findably: "No watchdog or log-monitoring exists for
    the 'No slot available for legacy unicast reflection' message. With `avahi_reflector: false` as the shipped
    default this failure class cannot occur at all, so a watchdog for it would be dead code; if you re-enable
    `avahi_reflector`, you re-inherit the original bug and are responsible for monitoring it yourself." Leave a
    `## Migrating from f1c878cb_cups` heading with a one-line placeholder ("See the rollout runbook — filled in by a
    later plan.") — Plan 21-03 replaces this placeholder with the real migration procedure; do not write the
    migration steps here.
  </action>
  <verify>
    <automated>test -s cups/README.md && test -s cups/DOCS.md && grep -c 'avahi_reflector' cups/DOCS.md && grep -c 'avahi_hostname' cups/DOCS.md && grep -c 'avahi_use_ipv6' cups/DOCS.md && grep -c 'printers' cups/DOCS.md && grep -c 'log_level' cups/DOCS.md && grep -ci 'no slot available for legacy unicast reflection' cups/DOCS.md && grep -c 'Migrating from f1c878cb_cups' cups/DOCS.md</automated>
    <fails_when>either file is empty/missing, or any grep -c returns 0 (an option is undocumented, or the D-08
    no-watchdog rationale is missing, or the migration-section placeholder heading is missing)</fails_when>
  </verify>
  <acceptance_criteria>
    - Every option name in cups/config.yaml's `options:` block from Plan 21-01 appears as a row in cups/DOCS.md's
      table
    - cups/DOCS.md contains the literal D-08 rationale sentence explaining why no watchdog exists
    - cups/README.md's example `printers:` snippet uses field names that exactly match cups/config.yaml's schema
  </acceptance_criteria>
  <done>cups/README.md and cups/DOCS.md fully document the D-05 option surface and the D-07/D-08 design rationale;
  the migration section is a placeholder Plan 21-03 will fill in.</done>
</task>

<task type="auto">
  <name>Add cups entry to root README.md's Add-ons list</name>
  <read_first>
    README.md (full "## Add-ons" section — the four existing entries are the exact shape to mirror; the
    `[amd64-shield]` reference definition at the bottom is already shared/reusable, do not redefine it)
  </read_first>
  <files>README.md</files>
  <action>
    Using a scoped Edit (never a whole-file Write on this file — see repo convention), insert a new
    `### [CUPS Print Server](./cups)` entry into README.md's "## Add-ons" section, after the existing
    "### [Network Tools](./network-tools)" entry's Features list and before the next `##` heading (or before the
    end of the section if Network Tools is currently last), matching the exact structure of the other four entries:
    `![Supports amd64 Architecture][amd64-shield]` line, an italicized one-line tagline, a short descriptive
    paragraph (2-3 sentences referencing the AirPrint/mDNS reliability fix as the add-on's reason for existing), and
    a `**Features:**` bullet list (reuse the same bullet content drafted for cups/README.md's Features section in
    Task 2, condensed to match this section's brevity).
  </action>
  <verify>
    <automated>grep -c 'CUPS Print Server' README.md</automated>
    <fails_when>grep -c returns 0 (the entry was not inserted)</fails_when>
  </verify>
  <acceptance_criteria>
    - README.md's Add-ons section contains a CUPS Print Server entry structurally identical to the other four
    - No other content in README.md is altered (scoped Edit only, per repo convention for this file)
  </acceptance_criteria>
  <done>Root README.md advertises the cups add-on alongside every other add-on in the repo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions cron (base-image-update.yml) -> internal/base-image-config.yaml | The workflow already has `contents: write` + `actions: write`; this task adds a data row, not a new privilege |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-05 | Tampering | internal/base-image-config.yaml `cups:` entry | low | accept | The entry only names a public GHCR image and a public GitHub source repo/file/regex to scrape for the next Alpine version tag — no secret, no new write path beyond the existing daily-cron bump workflow every other add-on already goes through identically; verified structurally correct by Task 1's own assertion script |
</threat_model>

<verification>
Run all three tasks' verify commands. This plan has no runtime/functional surface — verification is purely
structural (YAML parse assertions, grep for required documentation content).
</verification>

<success_criteria>
- internal/base-image-config.yaml tracks cups the same way it tracks gatus/meridian/network-tools, no workflow edits
- cups/README.md and cups/DOCS.md fully document the D-05 option surface and the D-07/D-08 rationale
- Root README.md lists cups among the repo's add-ons
</success_criteria>

<output>
Create `.planning/phases/21-cups-print-server-addon-airprint-mdns-fixes/21-02-SUMMARY.md` when done
</output>
