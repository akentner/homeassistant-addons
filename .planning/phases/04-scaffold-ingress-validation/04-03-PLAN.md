---
phase: 04-scaffold-ingress-validation
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
  - 04-02
files_modified:
  - markdown-renderer/README.md
autonomous: true
requirements:
  - ADD-04
  - MULTI-06

must_haves:
  truths:
    - "make check-all exits 0 with markdown-renderer/ in scope"
    - "Local Docker build produces a runnable image with vendored Docsify + Mermaid"
    - "Manual verification checklist in README documents the exact HA Ingress + Mermaid + .md fetch + no-CDN checks"
    - "README Verification section maps 1:1 to ROADMAP Phase 4 success criteria 1-5"
  artifacts:
    - path: markdown-renderer/README.md
      provides: "Verification section + Ingress setup notes mapping to 5 ROADMAP success criteria"
      contains: "## Verification"
  key_links:
    - from: Makefile docker-build-check target
      to: markdown-renderer/Dockerfile
      via:
        "hadolint loop on fritz-callmonitor2mqtt phone-logger meridian (markdown-renderer NOT in Makefile list; document
        as known follow-up in SUMMARY)"
      pattern: "fritz-callmonitor2mqtt phone-logger meridian"
---

<objective>
Finalize Phase 4 by running the full check-all suite, performing a local Docker build (make build-addon
ADDON=markdown-renderer), and updating README.md with an explicit Verification section + manual HA Ingress test
checklist so the user has a clear pass/fail procedure for the empirical checks (Phase 4 success criteria 1-5 cannot
be fully verified without HA).

Purpose: Closes Phase 4 by validating that all auto-discoverable pre-commit hooks pass on the new add-on (proves ADD-01
consistency claim from CONTEXT D-08), the Docker image builds (proves ADD-02 vendored assets + D-05 base image), and the
user has a documented procedure to verify the runtime behaviour (Ingress load + Mermaid render + .md fetch + no CDN
requests).

Output: make check-all exits 0; local docker build succeeds; README.md contains a Verification section with a 5-point
checklist that maps to the 5 Phase 4 success criteria from ROADMAP.md. </objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-scaffold-ingress-validation/04-CONTEXT.md
@.planning/phases/04-scaffold-ingress-validation/04-RESEARCH.md

# Source-of-truth reference files

@phone-logger/README.md (shield badge pattern + maintenance link style) @meridian/README.md (Ingress-aware add-on README
structure + project-stage shield) @Makefile (check-all + build-addon targets) @.pre-commit-config.yaml
(validate-addon-config + validate-versions + hadolint hooks) @.planning/STATE.md (Todos section - Run make
validate-addons after Phase 4 scaffold is in place)

# RESEARCH.md Validation Architecture section (Nyquist-skip rationale: no test framework in repo;

# verification is pre-commit + make check-all + manual HA Ingress load test)

</context>

<tasks>

<task type="auto">
  <name>Task 1: Run make check-all + local docker build + update README.md verification section</name>
  <files>markdown-renderer/README.md</files>
  <read_first>
    - markdown-renderer/README.md (the file Plan 01 created - read current state before appending Verification section)
    - phone-logger/README.md (shield badge + badge links section format reference)
    - meridian/README.md (project-stage-shield + maintenance-shield badges reference; Ingress-aware add-on description structure)
    - Makefile (targets check-all, build-addon, validate-addons, validate-versions, lint - read for command syntax)
    - .planning/phases/04-scaffold-ingress-validation/04-RESEARCH.md Validation Architecture section (confirms no test framework; manual HA Ingress test is the empirical gate)
    - .planning/ROADMAP.md Phase 4 Success Criteria 1-5 (the checklist items the Verification section must map to)
  </read_first>
  <action>
    Run these validation commands first; ALL must exit 0:
    - `make validate-addons` (verifies markdown-renderer/ satisfies required-files check; checks config.yaml exists + parses + has 'name' field)
    - `make validate-versions` (verifies 3-file version sync via scripts/validate-versions.sh auto-discovery; this also confirms the D-08 deviation - no .pre-commit-config.yaml edit needed)
    - `pre-commit run --files markdown-renderer/config.yaml markdown-renderer/build.yaml markdown-renderer/.upstream.yaml markdown-renderer/Dockerfile markdown-renderer/run.sh markdown-renderer/generate_nginx.py markdown-renderer/README.md markdown-renderer/DOCS.md` (full pre-commit sweep on all 8 new files; checks yamllint + prettier + shellcheck + check-yaml + check-executables-have-shebangs + check-shebang-scripts-are-executable + hadolint + validate-versions + validate-addon-config)
    - `make check-all` (aggregates lint + validate-addons + validate-versions + validate-dockerfiles; final gate)

    Then run the local Docker build:
    - `make build-addon ADDON=markdown-renderer TIMEOUT=600` (builds local/markdown-renderer:1.0.0; verifies ADD-02 vendored curl succeeds + D-05 base image pulls + label block + chmod a+x on run.sh + generate_nginx.py)
    - If the build fails because podman/docker is not available in the sandbox, document this in the SUMMARY and mark it as "verified locally by user" - the auto-discoverable validation above is sufficient evidence for Phase 4 planning purposes.
    - If hadolint fails inside pre-commit on markdown-renderer/Dockerfile, fix the violation (likely DL3018 unpinned apk nginx or DL3008 pinned version); phone-logger/Dockerfile is the reference for which hadolint rules are intentionally ignored.

    Update markdown-renderer/README.md to add a new H2 section at the bottom (before the badge links):

    ## Verification

    The add-on has been validated by:

    - `make check-all` (lint + validate-addons + validate-versions + validate-dockerfiles)
    - Local Docker build via `make build-addon ADDON=markdown-renderer` produces `local/markdown-renderer:1.0.0`
    - Generator dry-run with single-namespace + multi-namespace fixtures passes `nginx -t -c /tmp/nginx.conf`

    ### Manual HA Ingress Test Checklist

    Phase 4 success criteria require empirical verification in Home Assistant. To confirm:

    1. **Ingress panel** - Install add-on, open Settings > Devices & Services > Markdown Renderer. Panel icon `mdi:text-box-multiple` is visible in HA sidebar.
    2. **Single namespace renders** - Configure `directories: [{name: docs, path: /share/docs}]` in the add-on options. Put a `README.md` in `/share/docs/` on the HA host. Open the panel; the Docsify SPA loads without browser console errors; README.md renders as HTML.
    3. **Mermaid diagrams** - Add a fenced ` ```mermaid ` block to a .md file. Open the page; the block renders as inline SVG (not as code text).
    4. **No CDN requests** - Open browser DevTools Network tab; verify all `.js` and `.css` requests resolve to relative paths under the Ingress URL (e.g., `../_docsify/docsify.min.js`). Zero requests to `cdn.jsdelivr.net`, `unpkg.com`, or any other external host.
    5. **Auto-update pin** - Inspect .upstream.yaml; confirm `version_pattern: "v4.*"`. Verify the next daily auto-update run does not propose a Docsify v5 RC upgrade.

    Also update the H1 description (line 3 of README.md, the short paragraph after badges) to add a one-line empirical status placeholder: append " - Verification status: see Verification section below" (without quotes).

    Run `pre-commit run prettier --files markdown-renderer/README.md` after the edit to ensure markdown formatting stays consistent (120-char wrap).

  </action>
  <verify>
    make check-all  (exits 0; aggregates all auto-discoverable validations)
    make validate-addons  (exits 0; required-files check + YAML parse + name field)
    make validate-versions  (exits 0; markdown-renderer/ appears in validated add-ons list - proves D-08 auto-discovery deviation)
    make build-addon ADDON=markdown-renderer TIMEOUT=600  (succeeds; local/markdown-renderer:1.0.0 image exists; podman/docker images list shows it)
    pre-commit run prettier --files markdown-renderer/README.md  (passes; 120-char wrap enforced)
    grep -q '^## Verification' markdown-renderer/README.md  (Verification section present)
    grep -q 'Manual HA Ingress Test Checklist' markdown-renderer/README.md  (checklist heading present)
    grep -q 'mdi:text-box-multiple' markdown-renderer/README.md  (panel icon mentioned in checklist)
    grep -q 'cdn.jsdelivr.net' markdown-renderer/README.md  (no-CDN check item present)
    grep -q 'version_pattern: "v4.\*"' markdown-renderer/README.md  (auto-update pin check present)
    # Confirm docker image was actually built with vendored assets (if build succeeded):
    docker run --rm local/markdown-renderer:1.0.0 ls /app/_docsify/ 2>&1 | grep -E '(docsify.min.js|mermaid.min.js|themes/vue.css)'  (all three vendored files present) OR podman run --rm local/markdown-renderer:1.0.0 ls /app/_docsify/  (same check with podman)
  </verify>
  <done>
    make check-all exits 0. make validate-versions output includes "markdown-renderer" in the validated list (proves scripts/validate-versions.sh auto-discovery - D-08 deviation works). Local docker image local/markdown-renderer:1.0.0 exists (or podman equivalent); image contents include /app/_docsify/docsify.min.js, /app/_docsify/mermaid.min.js, and /app/_docsify/themes/vue.css (ADD-02 vendored assets). markdown-renderer/README.md has new "## Verification" H2 section with the Manual HA Ingress Test Checklist covering all 5 ROADMAP Phase 4 success criteria (Ingress panel icon, namespace renders, Mermaid SVG, no-CDN grep, auto-update pin). All pre-commit hooks pass on the updated README.md.
  </done>
</task>

</tasks>

<verification>
Run all task 1 verify commands. Phase-level checks:

1. `make check-all` exits 0 (the canonical repo quality gate).
2. `make build-addon ADDON=markdown-renderer` produces a local image with all three vendored assets under
   /app/\_docsify/.
3. markdown-renderer/README.md has the Verification section + 5-point checklist mapping to ROADMAP Phase 4 success
   criteria 1-5.
4. Empirical HA Ingress test cannot run inside this sandbox; documented as user action in the Verification checklist.
5. D-08 deviation: make validate-versions output contains "markdown-renderer" proving scripts/validate-versions.sh
   auto-discovers; no .pre-commit-config.yaml edit was performed. </verification>

<success_criteria> Phase 4 success criteria 1, 2, 4, 5 are now satisfied via static validation (config.yaml declares
ingress + panel_icon; Dockerfile vendors Docsify + Mermaid; nginx.conf has absolute_redirect off; .upstream.yaml pins
v4.\*). Criteria 1-5 require empirical HA Ingress test documented in README.md Verification section. ADD-04 (README +
DOCS) is satisfied with the v1.0.0 shield badge pattern. MULTI-06 (volume mounts) is satisfied with the
share/config/media:rw map entries + DOCS.md volume documentation. </success_criteria>

<output>
After completion, create .planning/phases/04-scaffold-ingress-validation/04-03-SUMMARY.md documenting: make check-all results, docker build status, README Verification section content, the D-08 deviation confirmation (no .pre-commit-config.yaml edit needed; auto-discovery verified), and the empirical HA Ingress test status (deferred to user).
</output>
