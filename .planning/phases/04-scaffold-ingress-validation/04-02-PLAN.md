---
phase: 04-scaffold-ingress-validation
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - markdown-renderer/generate_nginx.py
autonomous: true
requirements:
  - INGRESS-04
  - INGRESS-05
  - MULTI-02
  - MULTI-03
  - MULTI-04
  - MULTI-05

must_haves:
  truths:
    - "Generator iterates ALL directories entries and writes one nginx location block + one index.html per namespace"
    - "Generator validates namespace names against ^[a-z0-9][a-z0-9-]{0,62}$ regex"
    - "Generator rejects names that conflict with reserved list (_docsify, api, data, share, config, media)"
    - "Generator writes /tmp/landing/index.html listing all configured namespaces as clickable cards"
    - "nginx.conf contains absolute_redirect off at http level"
    - "nginx.conf contains per-namespace trailing-slash redirect (location = /<ns> { return 301 /<ns>/; })"
    - "Generator runs nginx -t -c /tmp/nginx.conf and exits 1 on config error before run.sh can exec nginx"
    - "INDEX_HTML_TEMPLATE per-namespace format() call preserves Kroki dispatcher + kroki_url injection (KROKI-02..05
      unchanged from Plan 01)"
    - "Each per-namespace index.html contains MARKDOWN_RENDERER.krokiUrl = '<kroki_url>' inline config (KROKI-02)"
  artifacts:
    - path: markdown-renderer/generate_nginx.py
      provides:
        "Multi-namespace generator with name validation + per-ns index.html + landing page + nginx -t validation"
      contains: "validate_namespace"
  key_links:
    - from: markdown-renderer/generate_nginx.py
      to: /tmp/nginx.conf
      via: "f-string loop over validated namespaces rendered into NGINX_TEMPLATE"
      pattern: "for ns in validated:"
    - from: markdown-renderer/generate_nginx.py
      to: /tmp/docroots/<ns>/index.html
      via: "INDEX_HTML_TEMPLATE.format(name=ns.name) in loop"
      pattern: "DOCROOTS_DIR / ns.name / .index.html."
    - from: markdown-renderer/generate_nginx.py
      to: /tmp/landing/index.html
      via: "LANDING_HTML_TEMPLATE with names + paths joined from validated list"
      pattern: "LANDING_DIR / .index.html."
    - from: markdown-renderer/generate_nginx.py
      to: /tmp/nginx.conf
      via: "subprocess.run nginx -t -c validation in main()"
      pattern: "nginx.*-t.*NGINX_CONF_PATH"
---

<objective>
Extend markdown-renderer/generate_nginx.py (single-namespace skeleton from Plan 01) to the full MULTI-01..06 scope
defined in CONTEXT D-03: iterate ALL directories entries, validate each namespace name, write one index.html per
namespace under /tmp/docroots/<name>/, write a landing page at /tmp/landing/index.html listing every namespace as a
clickable card, and render the per-namespace nginx location blocks (including trailing-slash redirect) into
/tmp/nginx.conf. Validates the rendered config with `nginx -t -c` so run.sh execs nginx only against a valid config.

Purpose: Satisfies INGRESS-04 (absolute_redirect off + per-ns trailing-slash redirect), INGRESS-05 (doneEach Mermaid
hook — already in INDEX_HTML_TEMPLATE from Plan 01, reused here per namespace), and MULTI-02..05. MULTI-01 + MULTI-06
are config.yaml concerns addressed in Plan 01; this plan extends the runtime to honor both single and multiple
directories.

Output: A generate_nginx.py that handles 1..N namespaces end-to-end with name validation, landing page, and nginx config
validation. Run against the two-namespace fixture produces a valid /tmp/nginx.conf (passes nginx -t) plus two
per-namespace index.html files plus a landing page. </objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/codebase/CONVENTIONS.md
@.planning/phases/04-scaffold-ingress-validation/04-CONTEXT.md
@.planning/phases/04-scaffold-ingress-validation/04-RESEARCH.md

# Source-of-truth reference files (read before editing)

@phone-logger/generate_config.py (canonical Python style; type hints; pathlib; flush=True; sys.exit)
@meridian/nginx.conf (nginx config structure - http block, server block, listen 8099, location alias pattern)
@.planning/phases/04-scaffold-ingress-validation/04-01-PLAN.md (Plan 01 task 3 defines INDEX_HTML_TEMPLATE +
NGINX_TEMPLATE structure to extend)

# RESEARCH.md sections to apply:

# - section 1 (basePath window.location.pathname - template already correct from Plan 01)

# - section 2 (doneEach mermaid.run() hook - already in template from Plan 01)

# - section 3 (nginx absolute_redirect off + per-ns trailing-slash redirect - EXTEND NGINX_TEMPLATE loop)

# - section 5 (multiline namespace validation regex + reserved name set)

# - section 7 (landing page strategy: /tmp/landing/index.html with <a href="/<ns>/"> cards)

# - section 8 (Pitfall 7: nginx -t validation step before exiting 0)

# python3 stdlib only - no third-party deps in base image

</context>

<tasks>

<task type="auto">
  <name>Task 1: Add multi-namespace iteration + name validation to generate_nginx.py</name>
  <files>markdown-renderer/generate_nginx.py</files>
  <read_first>
    - markdown-renderer/generate_nginx.py (the skeleton from Plan 01 task 3 - read it to see INDEX_HTML_TEMPLATE, NGINX_TEMPLATE, LANDING_HTML_TEMPLATE, main() structure)
    - phone-logger/generate_config.py (canonical Python style: type hints, pathlib.Path, sys.exit(main()))
    - .planning/phases/04-scaffold-ingress-validation/04-RESEARCH.md sections 5 (namespace regex + reserved list) and 7 (landing page)
  </read_first>
  <action>
    Edit markdown-renderer/generate_nginx.py to add these constants near the top (after existing module constants; before INDEX_HTML_TEMPLATE):

    import re   (add to stdlib imports)
    RESERVED_NAMES = frozenset({"_docsify", "api", "data", "share", "config", "media"})
    VALID_NAME_RE = re.compile(r"^[a-z0-9][a-z0-9-]{0,62}$")

    Add a new function validate_namespace(name: str) -> None that raises ValueError if:
    - not name (empty): raise ValueError("namespace name cannot be empty")
    - not VALID_NAME_RE.match(name): raise ValueError(f"namespace name {name!r} must match {VALID_NAME_RE.pattern}")
    - name in RESERVED_NAMES: raise ValueError(f"namespace name {name!r} is reserved (conflicts with {_docsify, api, ...} paths)")

    Also add validate_directories(directories: list) -> list[dict] that:
    - Returns the validated list (in input order)
    - Checks for duplicate names (raise ValueError if two entries share a name)
    - Calls validate_namespace(name) on each entry
    - Validates entry has both "name" and "path" string fields (raise ValueError otherwise)
    - Prints "Validated N namespace(s): name1, name2, ..." with flush=True

    Modify the existing NGINX_TEMPLATE so its per-namespace block becomes a {namespace_blocks} placeholder. The NGINX_TEMPLATE must still contain at the http level: absolute_redirect off; (REQUIRED for INGRESS-04). The server block must still contain: location /_docsify/ { alias /app/_docsify/; } + location = / { root /tmp/landing; try_files /index.html =404; } + location / { root /tmp/landing; try_files /index.html =404; }. The {namespace_blocks} placeholder sits where the per-namespace pair goes.

    Define a function render_namespace_blocks(namespaces: list[dict]) -> str that returns a string containing one block per namespace, formatted as EXACT 4 lines per namespace:
        location = /<name> {{ return 301 /<name>/; }}
        location /<name>/ {{
          alias /tmp/docroots/<name>/;
          try_files $uri $uri/ /index.html;
        }}

    (The {{ / }} are escaped braces because the result is later .format()-substituted into NGINX_TEMPLATE.)

    Define a function render_landing_html(namespaces: list[dict]) -> str that returns the landing page HTML with one card per namespace. Each card: <a class="card" href="/<name>/"><h2><name></h2><p><path></p></a>. Wrap in <!DOCTYPE html><html lang="en"><head><meta charset="UTF-8"><title>Markdown Renderer</title><style>...</style></head><body><main><h1>Markdown Namespaces</h1><div class="grid">{cards}</div></main></body></html>. Minimal inline CSS for .card (display block, padding 1em, border 1px solid #ccc, border-radius 8px, margin 0.5em, text-decoration none, color inherit) and .grid (display flex, flex-wrap wrap, gap 1em). Do NOT use any external CSS or JS.

    Modify the existing main() function:
    - Remove the "Phase 4 skeleton: using first namespace only" print + the [0] indexing.
    - Replace with: directories = options.get("directories", []); if not directories: print warning + write minimal /tmp/nginx.conf (just location /_docsify/ + location = / { root /tmp/landing; try_files /index.html =404; } + location / { return 404; } since no namespaces) + return 0
    - Call validated = validate_directories(directories) to run all checks; on ValueError: print error to stderr (flush=True) + return 1
    - Wipe + recreate DOCROOTS_DIR / LANDING_DIR
    - Loop: for ns in validated: (DOCROOTS_DIR / ns["name"]).mkdir(parents=True); (DOCROOTS_DIR / ns["name"] / "index.html").write_text(INDEX_HTML_TEMPLATE.format(name=ns["name"], name_display=ns["name"]))
    - LANDING_DIR.mkdir(parents=True); (LANDING_DIR / "index.html").write_text(render_landing_html(validated))
    - Render final nginx.conf: nginx_conf_text = NGINX_TEMPLATE.format(namespace_blocks=render_namespace_blocks(validated)); NGINX_CONF_PATH.write_text(nginx_conf_text)
    - Run subprocess.run(["nginx", "-t", "-c", str(NGINX_CONF_PATH)], check=False, capture_output=True). If returncode != 0: print(f"nginx -t failed:\n{result.stderr.decode()}", flush=True) + return 1
    - Print final summary line listing all validated namespaces + the nginx.conf path (flush=True)
    - return 0

    Keep the existing FileNotFoundError handling for OPTIONS_PATH from Plan 01 unchanged.

  </action>
  <verify>
    python3 -c "import re; from pathlib import Path; sys_path = 'markdown-renderer'; import sys; sys.path.insert(0, sys_path); import generate_nginx; assert hasattr(generate_nginx, 'validate_namespace'); assert hasattr(generate_nginx, 'validate_directories'); assert hasattr(generate_nginx, 'render_namespace_blocks'); assert hasattr(generate_nginx, 'render_landing_html'); assert generate_nginx.RESERVED_NAMES == frozenset({'_docsify','api','data','share','config','media'}); assert generate_nginx.VALID_NAME_RE.pattern == r'^[a-z0-9][a-z0-9-]{0,62}\$'; print('symbols OK')"

    python3 -c "
    import sys; sys.path.insert(0, 'markdown-renderer')
    from generate_nginx import validate_namespace, validate_directories
    # Valid name passes
    validate_namespace('docs'); print('docs OK')
    # Empty rejected
    try: validate_namespace(''); assert False, 'should raise'
    except ValueError as e: print('empty rejected')
    # Uppercase rejected
    try: validate_namespace('Docs'); assert False
    except ValueError as e: print('uppercase rejected')
    # Reserved rejected
    try: validate_namespace('_docsify'); assert False
    except ValueError as e: print('reserved rejected')
    # Duplicate rejected
    try: validate_directories([{'name':'a','path':'/p'},{'name':'a','path':'/q'}])
    except ValueError as e: print('duplicate rejected')
    # Valid list passes
    validate_directories([{'name':'a','path':'/p'},{'name':'b','path':'/q'}]); print('valid list OK')
    "

    # Two-namespace dry-run:
    rm -rf /tmp/docroots /tmp/landing /tmp/nginx.conf
    python3 -c "
    import sys, json, pathlib
    sys.path.insert(0, 'markdown-renderer')
    import generate_nginx
    generate_nginx.OPTIONS_PATH.write_text(json.dumps({'directories':[{'name':'docs','path':'/tmp/share/docs'},{'name':'runbooks','path':'/tmp/share/runbooks'}]}))
    pathlib.Path('/tmp/share/docs').mkdir(parents=True, exist_ok=True)
    pathlib.Path('/tmp/share/runbooks').mkdir(parents=True, exist_ok=True)
    pathlib.Path('/tmp/share/docs/README.md').write_text('# docs')
    pathlib.Path('/tmp/share/runbooks/README.md').write_text('# runbooks')
    rc = generate_nginx.main()
    print(f'main returned {rc}')
    assert rc == 0, f'main returned nonzero: {rc}'
    "

    # Verify outputs
    test -f /tmp/nginx.conf && grep -q 'absolute_redirect off' /tmp/nginx.conf && echo "INGRESS-04: absolute_redirect off present"
    grep -E 'location = /docs \{ return 301 /docs/; \}' /tmp/nginx.conf && echo "INGRESS-04: docs trailing-slash redirect present"
    grep -E 'location = /runbooks \{ return 301 /runbooks/; \}' /tmp/nginx.conf && echo "INGRESS-04: runbooks trailing-slash redirect present"
    grep -E 'location /docs/ \{' /tmp/nginx.conf && grep -E 'alias /tmp/docroots/docs/;' /tmp/nginx.conf && echo "MULTI-02: docs location block present"
    grep -E 'location /runbooks/ \{' /tmp/nginx.conf && grep -E 'alias /tmp/docroots/runbooks/;' /tmp/nginx.conf && echo "MULTI-02: runbooks location block present"
    test -f /tmp/docroots/docs/index.html && test -f /tmp/docroots/runbooks/index.html && echo "per-ns index.html exists"
    grep -q 'window.location.pathname' /tmp/docroots/docs/index.html && echo "INGRESS-02: docs basePath present"
    grep -q 'window.location.pathname' /tmp/docroots/runbooks/index.html && echo "INGRESS-02: runbooks basePath present"
    grep -q '../_docsify/docsify.min.js' /tmp/docroots/docs/index.html && echo "INGRESS-03: docs relative asset"
    grep -q '../_docsify/docsify.min.js' /tmp/docroots/runbooks/index.html && echo "INGRESS-03: runbooks relative asset"
    grep -q 'hook.doneEach' /tmp/docroots/docs/index.html && echo "INGRESS-05: docs Mermaid doneEach hook"
    grep -q 'hook.doneEach' /tmp/docroots/runbooks/index.html && echo "INGRESS-05: runbooks Mermaid doneEach hook"
    test -f /tmp/landing/index.html && echo "MULTI-03: landing page exists"
    grep -q 'href="/docs/"' /tmp/landing/index.html && echo "MULTI-03: landing page links to docs"
    grep -q 'href="/runbooks/"' /tmp/landing/index.html && echo "MULTI-03: landing page links to runbooks"

    # Invalid name test (reserved):
    python3 -c "
    import sys, json
    sys.path.insert(0, 'markdown-renderer')
    import generate_nginx
    generate_nginx.OPTIONS_PATH.write_text(json.dumps({'directories':[{'name':'_docsify','path':'/x'}]}))
    rc = generate_nginx.main()
    assert rc != 0, f'main should reject reserved name, got rc={rc}'
    print(f'invalid reserved name correctly rejected with rc={rc}')
    "

    pre-commit run check-executables-have-shebangs --files markdown-renderer/generate_nginx.py  (still passes - chmod +x retained)

  </verify>
  <done>
    markdown-renderer/generate_nginx.py now: imports re, defines RESERVED_NAMES frozenset {_docsify, api, data, share, config, media} and VALID_NAME_RE = ^[a-z0-9][a-z0-9-]{0,62}$, defines validate_namespace(name) raising ValueError on empty/non-matching/reserved, defines validate_directories(directories) checking duplicates and required fields, defines render_namespace_blocks returning per-ns location = /<ns> { return 301 /<ns>/; } + location /<ns>/ { alias /tmp/docroots/<ns>/; try_files $uri $uri/ /index.html; } pairs, defines render_landing_html returning a styled HTML page with one <a class="card" href="/<name>/"> per namespace, and main() iterates all validated directories (writes per-ns index.html + landing + nginx.conf), runs nginx -t -c /tmp/nginx.conf, returns 1 on validation or nginx -t failure, returns 0 on success. Two-namespace fixture (docs + runbooks) produces /tmp/nginx.conf passing nginx -t with both namespace blocks + trailing-slash redirects + absolute_redirect off + per-ns index.html files containing window.location.pathname basePath + relative ../_docsify/ script tags + doneEach Mermaid hook + /tmp/landing/index.html listing both namespaces.
  </done>
</task>

</tasks>

<verification>
Run all task 1 verify commands. Phase-level checks for Plan 02:

1. The generator handles 1..N namespaces via a single code path (no per-namespace special case in main()).
2. `python3 -c "import sys; sys.path.insert(0, 'markdown-renderer'); import generate_nginx; generate_nginx.OPTIONS_PATH.write_text(...)..."`
   exits 0 with the two-namespace fixture and produces the three output files (/tmp/nginx.conf, per-ns index.html,
   landing).
3. nginx -t -c /tmp/nginx.conf inside main() exits 0 for valid fixtures.
4. main() exits 1 (does NOT crash) for invalid names (reserved / uppercase / duplicate / empty).
5. /tmp/nginx.conf contains: `absolute_redirect off`, one `location = /<ns> { return 301 /<ns>/; }` per namespace, one
   `location /<ns>/ { alias /tmp/docroots/<ns>/; try_files ... }` per namespace, one
   `location /_docsify/ { alias /app/_docsify/; }` for vendored assets, one landing-page location.
6. /tmp/landing/index.html contains one `<a class="card" href="/<name>/">` per namespace.
7. Per-namespace index.html contains: `window.location.pathname` (basePath), `../_docsify/docsify.min.js` (relative
   script), `hook.doneEach` (Mermaid hook).
8. Phase 4 success criteria 3 (Mermaid diagrams render) is satisfied structurally - actual SVG output requires empirical
   HA Ingress test in user's HA instance (Plan 03 manual verification checklist).
9. Phase 4 success criteria 4 (no absolute slash-prefixed paths) verified by grep on generated index.html.
   </verification>

<success_criteria> INGRESS-04 (absolute_redirect off + per-namespace trailing-slash redirect), INGRESS-05 (doneEach
Mermaid hook reused per namespace), MULTI-02 (per-namespace SPA under /<name>/), MULTI-03 (landing page at /), MULTI-04
(generate_nginx.py reads /data/options.json + writes /tmp/nginx.conf + per-ns index.html + landing; run.sh invokes it
before exec nginx), MULTI-05 (namespace name validation rejects empty / non-matching regex / reserved / duplicate). All
6 requirements delivered by this plan alone. </success_criteria>

<output>
After completion, create .planning/phases/04-scaffold-ingress-validation/04-02-SUMMARY.md describing the multi-namespace generator architecture, name validation rules, and confirmation that nginx -t passes on the two-namespace fixture.
</output>
