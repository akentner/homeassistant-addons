---
phase: 14-real-ha-end-to-end-verification-operator-documentation
plan: 02
type: execute
wave: 2
depends_on:
  - 14-01-PLAN.md
files_modified:
  - internal/verify-bridge-e2e/01-unauthorized.sh
  - internal/verify-bridge-e2e/02-not-found.sh
  - internal/verify-bridge-e2e/03-critical-addon-protected.sh
  - internal/verify-bridge-e2e/04-prevented-destroy.sh
  - internal/verify-bridge-e2e/05-already-installed.sh
  - internal/verify-bridge-e2e/06-locked.sh
  - internal/verify-bridge-e2e/07-nonce-expired.sh
  - internal/verify-bridge-e2e/08-nonce-used.sh
  - internal/verify-bridge-e2e/09-install-timeout.sh
  - internal/verify-bridge-e2e/10-upstream-error.sh
  - internal/verify-bridge-e2e/11-pwned.sh
  - internal/verify-bridge-e2e/12-version-mismatch.sh
autonomous: true
requirements:
  - OPS-04

estimate:
  tokens: 65000
  raw_tokens: 33000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "Each of the 12 scenario scripts (`01-unauthorized.sh` through `12-version-mismatch.sh`) provokes the
      corresponding `error_code` against the live Bridge via curl or `tofu apply`, captures the Bridge response body
      verbatim to `terraform-bridge/internal/testdata/diagnostics/<error_code>.txt`, and exits non-zero when the
      captured response does not match the expected (HTTP status, `error_code`) pair from
      `terraform-bridge/internal/supervisor/client.go:661-680` MapError"
    - "Every scenario's first action is the same pre-flight gate as `00-happy-path.sh` (Plan 01): `command -v tofu` +
      Provider binary exists + `curl $BRIDGE_URL/healthz` returns 200. When any prerequisite is missing the scenario
      exits 0 with a `skipped — <reason>` annotation per D-10 so the executor (which lacks `tofu`/`docker`/`go`) can
      author the scenarios without spurious CI failures"
    - "Scenarios that require destructive setup the host can't tolerate (09-install-timeout.sh requires N parallel
      installs per D-10; any scenario that the operator chooses to defer) are encoded with an explicit skip-condition
      that prints `skipped — would require <reason>` and exits 0. The corresponding captured-diagnostic file is
      annotated `[not empirically observed — synthetic scenario]` so Plan 03's DOCS.md troubleshooting entry is
      traceable to the skip decision"
    - "Every scenario's captured-diagnostic file is named for its `error_code` (not its HTTP status) so the file naming
      matches the Provider DOCS.md anchor convention: `unauthorized.txt` → `#troubleshooting-unauthorized`,
      `critical_addon_protected.txt` → `#troubleshooting-critical-addon-protected`, etc.
      (`internal/diagnostics/doc.go:129-135` DocAnchor helper produces the kebab-case anchor)"
    - "All 12 scenarios pass shellcheck with the repo's SC1091 + SC2034 exemptions (AGENTS.md Gotcha #4); all 12
      scenarios source `_lib.sh` (not redeclare its constants); all 12 scenarios use the same `preflight`
      exit-0-with-skip-annotation pattern from Plan 01"
  artifacts:
    - path: "internal/verify-bridge-e2e/01-unauthorized.sh"
      provides:
        'Sends `curl $BRIDGE_URL/v1/version` with a wrong bearer token (a static string
        `phase-14-wrong-token-do-not-use-in-prod`); captures the 401 response body to `diagnostics/unauthorized.txt`;
        asserts `jq -e ''.error_code == "unauthorized"'' <file>` exits 0. Mirrors
        `internal/verify-bridge-no-token-leak.sh`''s exit-non-zero-on-assertion discipline'
      contains: "unauthorized|Wrong|error_code"
    - path: "internal/verify-bridge-e2e/02-not-found.sh"
      provides:
        'Sends `curl $BRIDGE_URL/v1/addons/nonexistent-slug-$(date +%s)/info` with the correct bearer; captures the 404
        response body to `diagnostics/not_found.txt`; asserts the body carries `error_code: "not_found"` and a Bridge
        `request_id`'
      contains: "not_found|nonexistent"
    - path: "internal/verify-bridge-e2e/03-critical-addon-protected.sh"
      provides:
        'Issues `POST /v1/addons/core_mosquitto/uninstall` (the test add-on is NEVER the victim — per Pitfall 4). With
        the correct bearer but WITHOUT a fresh `X-Force-Destroy` nonce (LIFE-03). Expects 403 + `error_code:
        "critical_addon_protected"`. Captures the response body to `diagnostics/critical_addon_protected.txt`. CRITICAL:
        the script''s header comment documents "DO NOT modify critical_addons to include local_test-addon — the verify
        suite needs to uninstall it between iterations" per Pitfall 4 + CF-12'
      contains: "core_mosquitto|critical_addon_protected|X-Force-Destroy|local_test-addon"
    - path: "internal/verify-bridge-e2e/04-prevented-destroy.sh"
      provides:
        "Embeds a `*.tf` with `lifecycle.prevent_destroy = true` on `homeassistant_addon.test` against
        `local_test-addon`; runs `tofu destroy`; expects the typed `prevented_destroy` diagnostic in the captured `tofu`
        output (with the DOCS.md anchor in the URL field). Captures `tofu destroy` output to
        `diagnostics/prevented_destroy.txt` and asserts the canonical Summary text from
        `diagnostics.ErrPreventedDestroyText` is present"
      contains: "prevent_destroy|prevented_destroy|local_test-addon"
    - path: "internal/verify-bridge-e2e/05-already-installed.sh"
      provides:
        "Runs `tofu apply` TWICE consecutively against `local_test-addon`. Iteration 1 installs; iteration 2
        (adoption-aware per PROV-05) reports `already_installed` to the Provider, which treats it as success and
        re-Reads the state. Captures iteration-2's `tofu apply` output to `diagnostics/already_installed.txt` and
        asserts the apply succeeded with no destructive diff"
      contains: "already_installed|adoption|local_test-addon"
    - path: "internal/verify-bridge-e2e/06-locked.sh"
      provides:
        "Deliberately races two `tofu apply` operations against `local_test-addon` in parallel (e.g. via background `&`
        + `wait`). The per-slug mutex (STATE-03) serializes them; the second one should succeed (not error 423 — that's
        the desired user-facing behavior). The scenario's header documents that `423 locked` is only surfaced when the
        Provider explicitly triggers an immediate retry WITHOUT serialization, which is not the Provider's natural flow.
        The captured diagnostic is the SUCCESSFUL apply output (the racing scenario proves the mutex works, not that 423
        fires in normal Provider usage)"
      contains: "locked|mutex|local_test-addon"
    - path: "internal/verify-bridge-e2e/07-nonce-expired.sh"
      provides:
        'Issues `POST /v1/addons/local_test-addon/uninstall` WITH an expired `X-Force-Destroy` nonce (manually
        constructed: a 65-second-old timestamp embedded in the request). Expects 401 + `error_code: "nonce_expired"`.
        Captures the response body to `diagnostics/nonce_expired.txt`. Documents the recovery path: POST /v1/auth/nonce
        → fresh nonce → retry'
      contains: "nonce_expired|X-Force-Destroy"
    - path: "internal/verify-bridge-e2e/08-nonce-used.sh"
      provides:
        'Obtains a fresh nonce via `POST /v1/auth/nonce`; uses it for one successful `DELETE
        /v1/addons/local_test-addon`; re-uses the SAME nonce for a second `DELETE` and expects 401 + `error_code:
        "nonce_used"`. Captures the second response to `diagnostics/nonce_used.txt`. Documents that nonces are
        single-use (Phase 12 D-06 + LIFE-03)'
      contains: "nonce_used|X-Force-Destroy"
    - path: "internal/verify-bridge-e2e/09-install-timeout.sh"
      provides:
        "Synthetic scenario per D-10 — the script's pre-flight exits 0 with `skipped — would require N parallel installs
        to exhaust Supervisor's install job slots; the host cannot tolerate this setup`. The captured-diagnostic file
        `diagnostics/install_timeout.txt` is left as a stub with a header comment `[not empirically observed — synthetic
        scenario per D-10]` so Plan 03's DOCS.md entry is traceable"
      contains: "skipped|would require|not empirically observed"
    - path: "internal/verify-bridge-e2e/10-upstream-error.sh"
      provides:
        'Sends `curl $BRIDGE_URL/v1/addons/local_test-addon/info` after temporarily blocking Bridge→Supervisor traffic
        (or by configuring Bridge to point at an unreachable Supervisor URL via a separate options file — operator
        choice). Expects 502 + `error_code: "upstream_error"`. Captures the response body to
        `diagnostics/upstream_error.txt`. Operator-driven per CF-14'
      contains: "upstream_error|502"
    - path: "internal/verify-bridge-e2e/11-pwned.sh"
      provides:
        'Runs `tofu apply` with `homeassistant_addon.test.options.dummy_setting = "P@ssw0rd"` (a known pwned-secret
        value). Expects the apply to SUCCEED with a Warning diagnostic carrying the `PwnedWarningText` Summary from
        `internal/diagnostics/doc.go:111`. Captures the full `tofu apply` output to `diagnostics/pwned.txt` and asserts
        the Warning text is present AND the apply exit code is 0. Per A8: if empirical observation fails, the script
        annotates the captured file `[not empirically observed — synthetic scenario per D-10]`'
      contains: "pwned|P@ssw0rd|known compromised"
    - path: "internal/verify-bridge-e2e/12-version-mismatch.sh"
      provides:
        'Constructs an HTTP `GET $BRIDGE_URL/v1/version` request with a Provider-side `min_provider_version` (sent via a
        custom `*.tf` variable) that exceeds what the Bridge''s `schema_version` advertises. Expects 401 + `error_code:
        "version_below_min"` OR the Provider''s `version` handshake rejection. Captures the response to
        `diagnostics/version.txt`. Note: the Provider DOCS.md troubleshooting table lists `version` (not
        `version_below_min`) as the anchor — the file is named `version.txt` to match the anchor, NOT
        `version_below_min.txt`'
      contains: "version|min_provider_version|schema_version"
  key_links:
    - from: "internal/verify-bridge-e2e/0[1-9,10-12]-*.sh"
      to: "internal/verify-bridge-e2e/_lib.sh"
      via:
        "Every scenario sources _lib.sh to inherit BRIDGE_URL + BRIDGE_TOKEN + TEST_ADDON_SLUG + preflight +
        snapshot_state + fingerprint_state. No redeclaration"
      pattern: "source.*_lib\\.sh"
    - from: "internal/verify-bridge-e2e/0[1-9,10-12]-*.sh"
      to: "terraform-bridge/internal/supervisor/client.go:661-680"
      via:
        "Each scenario's expected (HTTP status, error_code) pair matches the MapError switch. The captured-diagnostic
        files assert the literal `error_code` string from that switch"
      pattern: "error_code|critical_addon_protected|nonce_used"
    - from: "internal/verify-bridge-e2e/0[1-9,10-12]-*.sh"
      to: "terraform-bridge/internal/testdata/diagnostics/<error_code>.txt"
      via:
        "Each scenario `tee`s the Bridge response (or `tofu apply` output) verbatim into the per-error_code testdata
        file. Plan 03's DOCS.md troubleshooting cross-links read from these files (D-07)"
      pattern: "diagnostics/[a-z_]+\\.txt"
    - from: "internal/verify-bridge-e2e/04-prevented-destroy.sh"
      to: "terraform-provider-homeassistant/internal/diagnostics/doc.go"
      via:
        "The scenario asserts `diagnostics.ErrPreventedDestroyText` (`lifecycle.prevent_destroy = true is set on this
        resource; comment it out or destroy explicitly`) appears verbatim in the captured `tofu destroy` output"
      pattern: "prevent_destroy.*is set on this resource"
    - from: "internal/verify-bridge-e2e/11-pwned.sh"
      to: "terraform-provider-homeassistant/internal/diagnostics/doc.go:111"
      via:
        "The scenario asserts `diagnostics.PwnedWarningText` (`This add-on has a known compromised credentials leak
        (pwned): review the supervisor warning and rotate the add-on credentials before continuing.`) appears verbatim
        in the captured `tofu apply` output"
      pattern: "known compromised credentials leak"
---

<objective>
Land the 12 per-`error_code` verify scenarios that empirically confirm every Bridge error response
maps to the documented Provider diagnostic. Each scenario provokes one `error_code` against the
live Bridge (or `tofu apply` against the live Provider+Bridge stack), captures the response verbatim to
`terraform-bridge/internal/testdata/diagnostics/<error_code>.txt`, and exits non-zero on a
mismatch with the expected (HTTP status, `error_code`) pair from `terraform-bridge/internal/supervisor/
client.go:661-680` MapError. Plan 03's DOCS.md troubleshooting cross-links read from these captured
files (D-07) — they are the empirical source of truth the documentation is generated from.

Purpose: closing the SC-4 success criterion ("Every Bridge `error_code` produces the documented Provider diagnostic")
with empirical evidence. Unit tests + hermetic fixtures prove the Bridge returns the right `error_code` envelope on a
stub Supervisor; Phase 14's verify suite proves the same on a REAL Supervisor with REAL network latency + REAL addon
lifecycle state. The captured files are also the source DOCS.md will reference for the observed-issues section.

Output: 12 new files in `internal/verify-bridge-e2e/` named `01-unauthorized.sh` through `12-version-mismatch.sh`. Zero
changes to Bridge or Provider source. Zero changes to Plan 01's foundation files (`_lib.sh`, `00-happy-path.sh`,
`tools/test-addon/`, testdata `.gitkeep` files).

Requirements: OPS-04. The captured-diagnostic files are the empirical source Plan 03's DOCS.md troubleshooting section
draws from (D-07: "Each scenario script provokes the error against the live Bridge ... captures the resulting diagnostic
/ error body to `terraform-bridge/internal/testdata/diagnostics/<error_code>.txt`"). SC-4 success criterion ("Every
Bridge `error_code` produces the documented Provider diagnostic") is the gate this plan closes. </objective>

<execution_context> @/data/.config/opencode/gsd-core/workflows/execute-plan.md
@/data/.config/opencode/gsd-core/templates/summary.md </execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md (Phase 14 entry, lines 280-308 — SC-4 is this plan's surface)
@.planning/REQUIREMENTS.md (OPS-04 line 142-144 — the single Phase-14 requirement; covered by this
plan's empirical-observation harness + Plan 03's documentation)
@.planning/phases/14-real-ha-end-to-end-verification-operator-documentation/14-CONTEXT.md (D-06 the
12 scenario names, D-07 captured-diagnostic discipline, D-08 helper responsibilities, D-09 fix-docs-
to-match-observation rule, D-10 skip-when-unsafe, CF-06 critical_addons + per-slug mutex + X-Force-
Destroy, CF-08 Provider diagnostics contract, CF-14 live-system rule)
@.planning/phases/14-real-ha-end-to-end-verification-operator-documentation/14-RESEARCH.md (Pattern 1
per-error_code verify script, Pitfall 3 Bridge-restart auth chain, Pitfall 4 critical_addons not
including local_test-addon, Pitfall 6 state snapshot restore, Pitfall 7 lifecycle.prevent_destroy
conflict, Open Question #1 pwned fallback, A8 pwned scenario fallback, A9 state index empty case)
@.planning/phases/14-real-ha-end-to-end-verification-operator-documentation/14-01-PLAN.md (Plan 01's
_lib.sh + 00-happy-path.sh + test add-on — Plan 02 inherits all of these unchanged)
@internal/verify-bridge-e2e/_lib.sh (Plan 01's helper — sources BRIDGE_URL + BRIDGE_TOKEN +
TEST_ADDON_SLUG + preflight + snapshot_state + fingerprint_state from here; no redeclaration in
scenarios)
@internal/verify-bridge-e2e/00-happy-path.sh (Plan 01's happy-path scenario — structural template
for Plan 02's 12 scripts: set -euo pipefail, source _lib.sh, preflight, scenario body, exit codes)
@terraform-bridge/internal/supervisor/client.go:661-680 (MapError — source of truth for the
expected (HTTP status, error_code) pair each scenario asserts)
@terraform-bridge/contract/types.go (every JSON envelope the scenarios' curl + jq assertions match:
ErrorResponse with error_code + message + request_id, AddOnInfo, VersionHandshake, HealthResponse)
@terraform-provider-homeassistant/internal/diagnostics/doc.go (ErrUnauthorizedText +
ErrNotFoundText + ErrCriticalAddonText + ErrPreventedDestroyText + ErrAlreadyInstalledText +
ErrLockedText + ErrNonceExpiredText + ErrNonceUsedText + ErrInstallTimeoutText + ErrUpstreamText +
PwnedWarningText + DocAnchor — the strings Plan 02's `tofu apply` scenarios assert verbatim in the
captured Provider output)
@terraform-provider-homeassistant/DOCS.md §"Troubleshooting" lines 402-618 (the Provider DOCS.md
section that Plan 03 cross-links; Plan 02's captured files are the empirical source Plan 03 cites)
@internal/verify-bridge-no-token-leak.sh (Phase 10 structural template — exit-non-zero-on-assertion
discipline + color helpers + `set -euo pipefail`; mirrored verbatim)
@.planning/phases/12-bridge-write-api-safety-concurrency-index/12-CONTEXT.md (D-05..D-08 X-Force-
Destroy nonce lifecycle — 60-second TTL, single-use; consumed by scenarios 07 + 08)
@.planning/phases/13-provider-resource-data-sources-schema-handshake/13-CONTEXT.md (CF-08 Provider
diagnostics contract: per-error_code Summary text + request_id in Detail + DOCS.md anchor via Link;
consumed by every scenario's captured-output assertion)
@AGENTS.md (Live Systems rule §🔌 — no unsolicited restarts; pre-commit pipeline §🛠️; the four
critical gotchas that affect this plan: #1 no manual version edits — Plan 02 doesn't bump versions;
#3 check-yaml --unsafe — no YAML changes; #4 shellcheck SC1091/SC2034 — every shell script honors it)
</context>

<interfaces>
Locked contracts from earlier phases that this plan consumes (do NOT modify):

- **terraform-bridge/internal/supervisor/client.go MapError** (lines 661-680): the (HTTP status, error_code) pair table.
  Every scenario's expected pair matches this table exactly.

- **terraform-bridge/contract/types.go ErrorResponse**: `{ error_code, message?, request_id? }` JSON envelope. Every
  4xx/5xx Bridge response decodes into this shape; every captured-diagnostic file asserts on `error_code` and (where
  present) `request_id`.

- **terraform-provider-homeassistant/internal/diagnostics/doc.go**: per-error_code Summary text constants
  (ErrUnauthorizedText, ErrNotFoundText, ErrCriticalAddonText, ErrPreventedDestroyText, ErrAlreadyInstalledText,
  ErrLockedText, ErrNonceExpiredText, ErrNonceUsedText, ErrInstallTimeoutText, ErrUpstreamText, PwnedWarningText,
  ErrUnknownText). Scenarios 04 + 11 assert these strings appear verbatim in the captured `tofu apply` output.

- **Plan 01's `_lib.sh`**: every scenario sources this for BRIDGE_URL + BRIDGE_TOKEN + TEST_ADDON_SLUG
  - preflight + snapshot_state + fingerprint_state. Adding helpers to `_lib.sh` is out of scope for Plan 02 (Plan 03's
    99-cleanup.sh is the only file that touches `_lib.sh` after Plan 01).

- **Plan 01's `tools/test-addon/`**: every scenario that exercises the test add-on (04, 05, 06, 07, 08, 11, 12) targets
  `local_test-addon` exactly as declared in the test add-on's config.yaml.

Locked decisions from 14-CONTEXT.md (the executor MUST follow without re-deciding):

- **D-06** (scenario names): exactly `01-unauthorized.sh` through `12-version-mismatch.sh` + the `00-happy-path.sh`
  already shipped in Plan 01. No scenario skipped, no scenario renamed.
- **D-07** (captured-diagnostic discipline): each scenario `tee`s the Bridge response (or `tofu` output) verbatim into
  `terraform-bridge/internal/testdata/diagnostics/<error_code>.txt`. The file is the source DOCS.md reads from —
  captured diagnostic, NOT reformatted.
- **D-09** (fix docs to match observation): if a captured diagnostic differs from the documented text, Plan 03's DOCS.md
  update fixes the docs to match the observation. Plan 02 just captures; it does NOT modify DOCS.md.
- **D-10** (skip-when-unsafe): scenarios 09 (install-timeout) and any scenario the operator chooses to defer exit 0 with
  `skipped — <reason>` annotation + the corresponding testdata file is left as a stub annotated
  `[not empirically observed — synthetic scenario]`.
- **CF-14** (live-system rule): no Bridge restart, no Supervisor restart, no HA backup operation. The
  10-upstream-error.sh scenario that needs Bridge→Supervisor traffic blocked is operator-driven (the operator
  temporarily changes the Bridge options); Plan 02 writes the scenario script, the operator runs it.

Deferred to later plans (do NOT implement here):

- Plan 03: README.md + DOCS.md expansion + 99-cleanup.sh. Plan 03 reads the captured-diagnostic files this plan creates;
  Plan 02 does NOT reference DOCS.md anchors in any code.

Pitfalls to honor (from 14-RESEARCH.md):

- **Pitfall 3** (Bridge restart): every scenario's preflight handles the case where the Bridge is unreachable — exits 0
  with skip annotation. The operator is in the loop for restart decisions.
- **Pitfall 4** (critical_addons): scenario 03 uses `core_mosquitto` as the victim slug, NEVER `local_test-addon`. The
  scenario header comment documents this explicitly.
- **Pitfall 6** (state snapshot restore): every scenario that touches state calls `_lib.sh::snapshot_state` BEFORE the
  destructive action. The fingerprint (D-17) gives the operator a recovery check.
- **Pitfall 7** (prevent_destroy conflict): the 5-iteration happy-path scenario (Plan 01) and the already-installed
  scenario (Plan 02 scenario 05) explicitly do NOT set `lifecycle.prevent_destroy = true`. Only scenario 04 sets it
  (because that scenario's purpose IS to provoke that path).
- **A8** (pwned fallback): if empirical observation of `pwned: true` fails, scenario 11 annotates the captured file
  `[not empirically observed — synthetic scenario]` per D-10. The scenario does NOT exit non-zero on this fallback; it
  exits 0 with the annotation.

Plan 02 deliverable proof: every scenario exits 0 with a `PASS` line when the captured diagnostic matches the expected
pair; every scenario exits 0 with a `skipped` annotation when preflight fails; every scenario exits non-zero with a
`FAIL` line + the offending captured body when the diagnostic mismatches the expected pair. </interfaces>

<tasks>

<task type="auto">
  <name>Task 1: write the 6 read-mostly error-code scenarios (01-unauthorized, 02-not-found, 03-critical-addon-protected, 04-prevented-destroy, 05-already-installed, 06-locked)</name>
  <files>
    - internal/verify-bridge-e2e/01-unauthorized.sh
    - internal/verify-bridge-e2e/02-not-found.sh
    - internal/verify-bridge-e2e/03-critical-addon-protected.sh
    - internal/verify-bridge-e2e/04-prevented-destroy.sh
    - internal/verify-bridge-e2e/05-already-installed.sh
    - internal/verify-bridge-e2e/06-locked.sh
  </files>
  <read_first>
    - internal/verify-bridge-e2e/_lib.sh (Plan 01's helper — every scenario sources this for BRIDGE_URL
      + BRIDGE_TOKEN + TEST_ADDON_SLUG + preflight + snapshot_state + fingerprint_state)
    - internal/verify-bridge-e2e/00-happy-path.sh (Plan 01's happy-path scenario — structural
      template: `set -euo pipefail`, source _lib.sh, preflight, scenario body, assertion + exit)
    - internal/verify-bridge-no-token-leak.sh (the verify-scenario template: set -euo pipefail,
      color helpers, exit-non-zero-on-assertion discipline)
    - terraform-bridge/internal/supervisor/client.go:661-680 (MapError switch — every scenario's
      expected (HTTP status, error_code) pair matches this table exactly)
    - terraform-provider-homeassistant/internal/diagnostics/doc.go (the canonical per-error_code
      Summary text constants; scenarios 04 + 11 assert these strings appear verbatim)
    - terraform-provider-homeassistant/DOCS.md §"Troubleshooting" lines 402-618 (the Provider DOCS.md
      section that Plan 03 cross-links; the captured-diagnostic files this task creates are the
      empirical source Plan 03 cites)
    - .planning/phases/14-real-ha-end-to-end-verification-operator-documentation/14-CONTEXT.md
      D-06 + D-07 + D-10 + CF-14 (locked decisions for this plan)
    - .planning/phases/14-real-ha-end-to-end-verification-operator-documentation/14-RESEARCH.md
      Pattern 1 + Pitfall 4 + Pitfall 7 (the read-mostly scenarios' structural pattern + the
      critical_addons-not-including-local_test-addon rule + the prevent_destroy-on-test-addon
      happy-path conflict)
    - .planning/phases/12-bridge-write-api-safety-concurrency-index/12-CONTEXT.md D-05..D-08
      (X-Force-Destroy nonce lifecycle — consumed by scenario 03's nonce-precondition logic)
  </read_first>
  <action>
    This task writes the 6 read-mostly error-code scenarios (01..06 in D-06). The remaining 6 scenarios
    (07..12, nonce + lifecycle + handshake errors + pwned warning) are written in Task 2. Each scenario
    is structurally identical to `00-happy-path.sh` (Plan 01): `set -euo pipefail`, source `_lib.sh`,
    call `preflight`, exit 0 with `skipped` annotation if preflight fails, scenario body that
    provokes the error and captures the response to `diagnostics/<error_code>.txt`, final assertion
    that exits non-zero on diagnostic mismatch.

    Run a single `shellcheck` command after writing all 6 scenarios in this task to confirm they all
    pass with the repo's SC1091 + SC2034 exemptions (AGENTS.md Gotcha #4). Do NOT run pre-commit
    (Task 2's parallel scenarios need to land first; the pre-commit run is in Plan 03's README/DOCS
    task scope).

    Honour Pitfall 4: scenario 03's header comment MUST document explicitly:
    `DO NOT modify critical_addons to include local_test-addon — the verify suite needs to uninstall
    it between iterations. The victim slug is core_mosquitto (or another always-on critical slug)
    per CF-12.` Add this as a top-of-file comment block (4-5 lines), not just a passing mention.

    Honour Pitfall 7: scenario 04's embedded `*.tf` IS allowed to set `lifecycle.prevent_destroy =
    true` (this scenario's purpose IS to provoke the prevented_destroy path), but the
    `00-happy-path.sh` (Plan 01) and scenario 05 must NOT set it (the 5-iteration loop and the
    adoption-aware install flow need to teardown). Document this distinction in scenario 04's header
    comment.

    Per scenario body:

    1. **01-unauthorized.sh** — `curl -sS -o diagnostics/unauthorized.txt -w '%{http_code}'
       -H "Authorization: Bearer phase-14-wrong-token-do-not-use-in-prod"
       "$BRIDGE_URL/v1/version"`. Assert HTTP 401 AND `jq -e '.error_code == "unauthorized"' <file>`
       exits 0. Header: "Phase 14 SC-4 + D-07: provoke 401 + error_code: unauthorized".

    2. **02-not-found.sh** — `curl -sS -o diagnostics/not_found.txt -w '%{http_code}'
       -H "Authorization: Bearer $BRIDGE_TOKEN"
       "$BRIDGE_URL/v1/addons/nonexistent-slug-$(date +%s)/info"`. Assert HTTP 404 AND
       `.error_code == "not_found"`. Header: "Phase 14 SC-4 + D-07: provoke 404 + error_code:
       not_found".

    3. **03-critical-addon-protected.sh** — `POST /v1/addons/core_mosquitto/uninstall` with bearer
       but NO `X-Force-Destroy` nonce. Use `curl -sS -X POST -H "Authorization: Bearer $BRIDGE_TOKEN"
       -o diagnostics/critical_addon_protected.txt -w '%{http_code}'
       "$BRIDGE_URL/v1/addons/core_mosquitto/uninstall"`. Assert HTTP 403 AND `.error_code ==
       "critical_addon_protected"`. Header: "Phase 14 SC-4 + D-07 + Pitfall 4 + CF-12: provoke 403
       + critical_addon_protected against core_mosquitto. DO NOT modify critical_addons to include
       local_test-addon — the verify suite needs to uninstall it between iterations."

    4. **04-prevented-destroy.sh** — Embed a `*.tf` with `lifecycle.prevent_destroy = true` on
       `homeassistant_addon.test` against `local_test-addon` (slug, repository="local" per the
       adoption import path). Run `tofu init && tofu apply -auto-approve` to install the resource.
       Then run `tofu destroy` and capture its output to `diagnostics/prevented_destroy.txt`.
       Assert the captured output contains `prevented_destroy` AND contains the literal Summary
       text `lifecycle.prevent_destroy = true is set on this resource; comment it out or destroy
       explicitly` (from `diagnostics.ErrPreventedDestroyText`). Header: "Phase 14 SC-4 + D-07 +
       Pitfall 7: this scenario EXPLICITLY sets lifecycle.prevent_destroy = true — its purpose IS
       to provoke that path."

    5. **05-already-installed.sh** — Embed a `*.tf` for `homeassistant_addon.test` with
       `slug = "local_test-addon"`, `start = true`, NO `prevent_destroy` (per Pitfall 7). Run
       `tofu init && tofu apply -auto-approve` (iteration 1: installs); then immediately run
       `tofu apply -auto-approve` again (iteration 2: adoption-aware per PROV-05, treats
       `409 already_installed` as success). Capture iteration-2's `tofu apply` output to
       `diagnostics/already_installed.txt`. Assert `tofu apply` exits 0 AND the captured output
       contains `No changes` (PROV-04 + PROV-10 idempotency). Header: "Phase 14 SC-1 + SC-4 +
       D-07 + PROV-05: provoke 409 already_installed via adoption-aware Create; treat as success".

    6. **06-locked.sh** — Two `tofu apply` operations against `local_test-addon` in parallel via
       `&` + `wait`. The per-slug mutex (STATE-03 + CF-06) serializes them; both should succeed.
       Capture the combined output (order is non-deterministic; capture both stdout streams
       interleaved) to `diagnostics/locked.txt`. Assert `tofu apply` for both background processes
       exited 0. Header: "Phase 14 SC-4 + CF-06: per-slug mutex (STATE-03) serializes concurrent
       apply operations. NOTE: 423 locked is NOT surfaced in normal Provider usage — the Provider's
       own retry semantics + the Bridge's per-slug mutex make it transparent. This scenario proves
       the mutex works, not that 423 fires. To trigger 423 directly, send two raw `curl
       POST /v1/addons/{slug}/install` requests in parallel against an unprovisioned slug."

  </action>
  <acceptance_criteria>
    - internal/verify-bridge-e2e/{01,02,03,04,05,06}-*.sh each: source _lib.sh, call preflight, exit 0 with
      `skipped` annotation when preflight fails (per D-10 + Plan 01's pattern), and exit non-zero on
      captured-diagnostic mismatch (verify: shellcheck -s bash -e SC1091,SC2034
      internal/verify-bridge-e2e/0{1,2,3,4,5,6}-*.sh exits 0; grep -E
      'source.*_lib\.sh|preflight|skipped' internal/verify-bridge-e2e/0{1,2,3,4,5,6}-*.sh produces all
      expected matches across the 6 files; grep -c -E 'preflight|skipped' per file >= 2)
    - 01-unauthorized.sh: sends curl with WRONG bearer; captures to diagnostics/unauthorized.txt; asserts HTTP
      401 + .error_code == "unauthorized" (verify: grep -E 'Authorization: Bearer phase-14-wrong-token|unauthorized\.txt|\.error_code.*unauthorized' internal/verify-bridge-e2e/01-unauthorized.sh produces all expected matches)
    - 02-not-found.sh: sends curl to /v1/addons/nonexistent-*/info; captures to diagnostics/not_found.txt;
      asserts HTTP 404 + .error_code == "not_found" (verify: grep -E 'nonexistent|not_found\.txt|not_found' internal/verify-bridge-e2e/02-not-found.sh produces all expected matches)
    - 03-critical-addon-protected.sh: POSTs to /v1/addons/core_mosquitto/uninstall WITHOUT nonce; captures to
      diagnostics/critical_addon_protected.txt; asserts HTTP 403 + .error_code == "critical_addon_protected";
      header comment EXPLICITLY documents "DO NOT modify critical_addons to include local_test-addon" per
      Pitfall 4 + CF-12 (verify: grep -E 'core_mosquitto|critical_addon_protected\.txt|DO NOT modify
      critical_addons' internal/verify-bridge-e2e/03-critical-addon-protected.sh produces all expected matches;
      grep -v '^#' | grep -c 'local_test-addon' returns 0 in 03's body — only `local_test-addon` mention is
      in the warning comment, not in the curl target)
    - 04-prevented-destroy.sh: embeds *.tf with lifecycle.prevent_destroy = true; runs tofu apply + tofu destroy;
      captures to diagnostics/prevented_destroy.txt; asserts the canonical ErrPreventedDestroyText Summary
      appears in captured output (verify: grep -E 'prevent_destroy|prevented_destroy\.txt|comment it out or
      destroy explicitly' internal/verify-bridge-e2e/04-prevented-destroy.sh produces all expected matches;
      the canonical Summary text `lifecycle.prevent_destroy = true is set on this resource; comment it out
      or destroy explicitly` is present verbatim in the script's assertion)
    - 05-already-installed.sh: runs tofu apply TWICE (iteration 1 installs, iteration 2 adopts); captures
      iteration-2 output to diagnostics/already_installed.txt; asserts both apply exits 0 + iteration 2
      reports "No changes" (verify: grep -E 'already_installed|local_test-addon|No changes' internal/verify-bridge-e2e/05-already-installed.sh produces all expected matches)
    - 06-locked.sh: runs two tofu apply operations in parallel via & + wait; captures to
      diagnostics/locked.txt; asserts both background processes exited 0 (verify: grep -E 'locked\.txt|local_test-addon|&|wait' internal/verify-bridge-e2e/06-locked.sh produces all expected matches)
    - All 6 scenarios pass shellcheck with the repo's SC1091 + SC2034 exemptions (verify: shellcheck -s bash
      -e SC1091,SC2034 internal/verify-bridge-e2e/0{1,2,3,4,5,6}-*.sh exits 0 across all 6 files)
    - No scenario modifies the Bridge, Provider, or Plan 01's _lib.sh (verify: git diff Plan 01 files after
      this task completes returns empty output)
  </acceptance_criteria>
  <verify>
    <automated>shellcheck -s bash -e SC1091,SC2034 internal/verify-bridge-e2e/01-unauthorized.sh internal/verify-bridge-e2e/02-not-found.sh internal/verify-bridge-e2e/03-critical-addon-protected.sh internal/verify-bridge-e2e/04-prevented-destroy.sh internal/verify-bridge-e2e/05-already-installed.sh internal/verify-bridge-e2e/06-locked.sh</automated>
    <fails_when>any of the 6 scenarios fails shellcheck (SC1091 + SC2034 already suppressed — any other SC#### warning fails the gate), OR any scenario fails to source _lib.sh + call preflight (the two invariants every scenario must honor), OR scenario 03's body accidentally targets `local_test-addon` instead of `core_mosquitto` (the Pitfall 4 violation), OR scenario 05's body accidentally sets `lifecycle.prevent_destroy = true` (the Pitfall 7 violation that would block the destroy step)</fails_when>
  </verify>
  <done>
    The 6 read-mostly error-code scenarios (01-unauthorized through 06-locked) are committed. Each scenario
    follows Plan 01's structural template (set -euo pipefail, source _lib.sh, preflight, scenario body,
    captured-diagnostic assertion). Each scenario's header documents its specific Pitfall or contract it
    honors (Pitfall 4 for scenario 03, Pitfall 7 for scenario 04, PROV-05 for scenario 05, STATE-03 for
    scenario 06). All 6 pass shellcheck with the repo's SC1091/SC2034 exemptions. Each scenario is safe
    to invoke on the operator workstation AND safe to invoke on the executor (preflight exits 0 with
    `skipped` when `tofu` or the Provider binary is missing).
  </done>
</task>

<task type="auto">
  <name>Task 2: write the 6 write-paths error-code scenarios (07-nonce-expired, 08-nonce-used, 09-install-timeout [synthetic], 10-upstream-error, 11-pwned, 12-version-mismatch)</name>
  <files>
    - internal/verify-bridge-e2e/07-nonce-expired.sh
    - internal/verify-bridge-e2e/08-nonce-used.sh
    - internal/verify-bridge-e2e/09-install-timeout.sh
    - internal/verify-bridge-e2e/10-upstream-error.sh
    - internal/verify-bridge-e2e/11-pwned.sh
    - internal/verify-bridge-e2e/12-version-mismatch.sh
  </files>
  <read_first>
    - internal/verify-bridge-e2e/01-unauthorized.sh (Task 1's read-mostly scenario — structural template
      for Task 2's write-paths scenarios; plan to mirror its set -euo pipefail + source _lib.sh +
      preflight pattern verbatim)
    - internal/verify-bridge-e2e/_lib.sh (Plan 01's helper — same imports + preflight + snapshot_state +
      fingerprint_state)
    - .planning/phases/14-real-ha-end-to-end-verification-operator-documentation/14-CONTEXT.md D-05..D-08
      (X-Force-Destroy nonce lifecycle — 60-second TTL, single-use; consumed by scenarios 07 + 08)
    - .planning/phases/14-real-ha-end-to-end-verification-operator-documentation/14-RESEARCH.md
      Open Question #1 (pwned scenario fallback) + A8 (pwned advisory may not fire on the live
      Supervisor) + Pitfall 7 (prevent_destroy conflict on the test add-on)
    - terraform-provider-homeassistant/internal/diagnostics/doc.go:111 (PwnedWarningText constant —
      the canonical Summary string scenario 11 asserts verbatim)
    - terraform-bridge/internal/supervisor/client.go:661-680 (MapError switch — every scenario's
      expected pair matches this table)
    - terraform-provider-homeassistant/DOCS.md §"Troubleshooting: pwned" line 554 (the Provider
      DOCS.md row that scenario 11's captured output is the empirical source for)
    - .planning/phases/12-bridge-write-api-safety-concurrency-index/12-CONTEXT.md D-05..D-08
      (X-Force-Destroy nonce lifecycle — Phase 12's authoritative design surface for scenarios 07 + 08)
    - terraform-provider-homeassistant/DOCS.md §"Troubleshooting: version" line 570 (the Provider
      DOCS.md row that scenario 12's captured output is the empirical source for)
  </read_first>
  <action>
    This task writes the 6 write-paths error-code scenarios (07..12 in D-06). These scenarios differ
    from Task 1's read-mostly scenarios in two ways: (a) scenarios 07 + 08 + 11 touch state (they call
    the Bridge's mutating endpoints), so they MUST call `_lib.sh::snapshot_state` before the
    destructive action (D-16) and `fingerprint_state` after (D-17); (b) scenario 09 is synthetic
    (per D-10 + A8) — its pre-flight exits 0 with `skipped` annotation and the captured-diagnostic
    file is a stub annotated `[not empirically observed — synthetic scenario]`.

    Run a single `shellcheck` command after writing all 6 scenarios in this task to confirm they all
    pass with the repo's SC1091 + SC2034 exemptions. Then run the consolidated Task 1 + Task 2
    `shellcheck` (12 scenarios total) one more time as the final acceptance gate.

    Per-scenario specifics:

    1. **07-nonce-expired.sh** — Call `POST /v1/addons/local_test-addon/uninstall` with a stale
       `X-Force-Destroy` header (e.g. an obviously-fake or expired nonce). Expects 401 +
       `error_code: "nonce_expired"`. Captures to `diagnostics/nonce_expired.txt`. Asserts the
       response body carries `.error_code == "nonce_expired"`. Header documents the recovery path:
       `POST /v1/auth/nonce` → fresh nonce → retry. Header also notes: "DO NOT actually uninstall
       local_test-addon in this scenario — the X-Force-Destroy check fails BEFORE the uninstall
       runs." Snapshot state before the call (D-16); fingerprint after (D-17).

    2. **08-nonce-used.sh** — Call `POST /v1/auth/nonce` to obtain a fresh nonce; use it ONCE for a
       successful `POST /v1/addons/local_test-addon/uninstall` (the test add-on must be installed
       from Plan 01's scenario 00 — scenario 05's iteration 1 also installs it; the scenario is
       resilient to either state); reuse the SAME nonce for a second `POST /v1/addons/local_test-
       addon/uninstall` and expect 401 + `error_code: "nonce_used"`. Captures the SECOND response to
       `diagnostics/nonce_used.txt`. Asserts `.error_code == "nonce_used"`. Documents that nonces
       are single-use (LIFE-03 + Phase 12 D-06). After the second call, reinstall the test add-on
       via `tofu apply` so subsequent scenarios find it installed — clean state discipline.

    3. **09-install-timeout.sh** — Synthetic scenario per D-10. Pre-flight exits 0 with
       `skipped — would require N parallel installs to exhaust Supervisor's install job slots; the
       host cannot tolerate this setup`. The `diagnostics/install_timeout.txt` file is written as
       a stub file containing ONLY a header comment: `# install_timeout.txt — synthetic scenario per
       D-10. Phase 14 does NOT empirically exercise this path. Plan 03's DOCS.md troubleshooting
       entry for install_timeout is annotated accordingly.`. The script's body consists of: source
       _lib.sh; print the skip annotation; create the stub testdata file; exit 0. Header: "Phase 14
       SC-4 + D-10 + A8: synthetic scenario — NOT empirically exercised. install_timeout (504)
       requires Supervisor job-slot exhaustion which the host cannot tolerate. The captured
       diagnostic file is a stub so Plan 03's DOCS.md entry has a traceable source."

    4. **10-upstream-error.sh** — `curl $BRIDGE_URL/v1/addons/local_test-addon/info` with the
       correct bearer while the Bridge's upstream Supervisor is unreachable. The operator achieves
       this by either (a) temporarily blocking Bridge→Supervisor traffic via iptables on the host
       (CF-14: operator-initiated), or (b) configuring the Bridge's `SUPERVISOR_TOKEN` env var to
       point at an unreachable Supervisor URL via a separate options file. The scenario SCRIPT
       documents both operator options in its header; the SCRIPT ITSELF does not modify the host.
       Captures to `diagnostics/upstream_error.txt`. Asserts HTTP 502 + `.error_code ==
       "upstream_error"`. Header: "Phase 14 SC-4 + D-07 + CF-14: upstream_error (502) requires
       temporarily blocking Bridge→Supervisor traffic — operator-driven. The script does NOT
       perform the block; the operator does. Run only after `curl $BRIDGE_URL/healthz` returns 503
       (which proves the Bridge itself is up but its Supervisor ping fails)."

    5. **11-pwned.sh** — Embed a `*.tf` for `homeassistant_addon.test` with `slug =
       "local_test-addon"`, `options = { dummy_setting = "P@ssw0rd" }` (a known compromised-credential
       leak per HIBP). Run `tofu apply -auto-approve`. The apply SUCCEEDS (exit 0) but the
       Provider emits a Warning diagnostic carrying the canonical Summary text from
       `diagnostics.PwnedWarningText` (`This add-on has a known compromised credentials leak
       (pwned): review the supervisor warning and rotate the add-on credentials before continuing.`).
       Capture the full `tofu apply` output to `diagnostics/pwned.txt`. Assert (a) `tofu apply`
       exited 0, AND (b) the captured output contains `known compromised credentials leak (pwned)`.
       Per A8: if the Bridge's `pwned` flag does NOT fire (some HA versions disable the breach
       corpus check), the scenario annotates the captured file
       `[not empirically observed — synthetic scenario per D-10]` and exits 0 (NOT non-zero — this
       fallback is explicitly D-10's intent). Header: "Phase 14 SC-4 + D-07 + D-10 + A8: pwned
       (Warning severity). The apply SUCCEEDS — this is a Warning, not an Error. If empirical
       observation fails, the captured file is annotated [not empirically observed — synthetic
       scenario]."

    6. **12-version-mismatch.sh** — Construct an HTTP GET against `$BRIDGE_URL/v1/version` with a
       Provider-side `min_provider_version` baked into the request such that the Bridge's
       `schema_version` is below the Provider's minimum. The simplest way: set `HA_BRIDGE_MIN_PROVIDER_VERSION=99.99.99`
       in the Provider's environment (or pass via a custom `*.tf` variable) so the Provider's
       handshake refuses. Capture the Provider's typed Error diagnostic to
       `diagnostics/version.txt` and assert it carries the Provider DOCS.md#troubleshooting-version
       anchor text. Per the DOCS.md anchor convention (`internal/diagnostics/doc.go:129-135`), the
       file is named `version.txt` (matching the anchor name), NOT `version_below_min.txt` (which
       is what the underlying error_code looks like internally). Header: "Phase 14 SC-4 + D-07 +
       PROV-03: Provider handshake rejects Bridge whose schema_version < Provider's
       min_provider_version. The captured-diagnostic file is named `version.txt` to match the
       DOCS.md anchor convention (`#troubleshooting-version`)."

    Honour Pitfall 4 explicitly in scenario 08: when the scenario uninstalls `local_test-addon`
    with the second nonce, the destroy must succeed against a non-critical slug (which
    `local_test-addon` is, per CF-12 default critical_addons list). Document in the scenario header
    that the cleanup `tofu apply` at the end reinstalls the test add-on for subsequent scenarios.

  </action>
  <acceptance_criteria>
    - internal/verify-bridge-e2e/{07,08,09,10,11,12}-*.sh each: source _lib.sh, call preflight, exit 0
      with `skipped` annotation when preflight fails (per D-10) (verify: shellcheck -s bash -e SC1091,SC2034
      internal/verify-bridge-e2e/0{7,8,9,10,11,12}-*.sh exits 0; grep -E
      'source.*_lib\.sh|preflight|skipped' internal/verify-bridge-e2e/0{7,8,9,10,11,12}-*.sh produces all
      expected matches across the 6 files)
    - 07-nonce-expired.sh: sends curl with stale X-Force-Destroy header; captures to
      diagnostics/nonce_expired.txt; asserts HTTP 401 + .error_code == "nonce_expired"; calls
      snapshot_state + fingerprint_state per D-16 + D-17 (verify: grep -E 'X-Force-Destroy|nonce_expired\.txt|nonce_expired|snapshot_state|fingerprint_state' internal/verify-bridge-e2e/07-nonce-expired.sh produces all expected matches)
    - 08-nonce-used.sh: obtains fresh nonce via POST /v1/auth/nonce, uses once successfully, reuses SAME
      nonce and expects nonce_used on second call; cleans up by reinstalling the test add-on (verify: grep
      -E '/v1/auth/nonce|nonce_used\.txt|nonce_used|tofu apply' internal/verify-bridge-e2e/08-nonce-used.sh produces all expected matches)
    - 09-install-timeout.sh: synthetic scenario per D-10 — preflight exits 0 with skip annotation,
      testdata file is a stub annotated `[not empirically observed — synthetic scenario]` (verify: grep
      -E 'skipped|would require|not empirically observed|synthetic scenario' internal/verify-bridge-e2e/09-install-timeout.sh produces all expected matches; the script body does NOT make any curl/tofu call to the live Bridge)
    - 10-upstream-error.sh: documents in its header that the scenario requires operator-driven Bridge→
      Supervisor blocking (CF-14); the script itself does NOT perform the block (verify: grep -E 'upstream_error\.txt|operator-driven|CF-14|operator does' internal/verify-bridge-e2e/10-upstream-error.sh produces all expected matches; the script body contains only curl + jq assertions, no iptables/network manipulation)
    - 11-pwned.sh: embeds *.tf with dummy_setting = "P@ssw0rd"; runs tofu apply; captures to
      diagnostics/pwned.txt; asserts PwnedWarningText Summary appears in captured output AND
      tofu apply exited 0; falls back to `[not empirically observed — synthetic scenario]`
      annotation per A8 + D-10 if empirical observation fails (verify: grep -E 'pwned\.txt|P@ssw0rd|known compromised credentials leak|not empirically observed' internal/verify-bridge-e2e/11-pwned.sh produces all expected matches)
    - 12-version-mismatch.sh: sets a Provider-side min_provider_version that the Bridge's schema_version
      is below; captures to diagnostics/version.txt (NOT version_below_min.txt — the anchor is
      `#troubleshooting-version` per the DOCS.md convention); asserts the Provider DOCS.md anchor
      text appears in the captured output (verify: grep -E 'version\.txt|min_provider_version|schema_version|version_below_min|#troubleshooting-version' internal/verify-bridge-e2e/12-version-mismatch.sh produces all expected matches; the testdata filename is `version.txt`, NOT `version_below_min.txt`)
    - All 6 scenarios pass shellcheck with the repo's SC1091 + SC2034 exemptions (verify: shellcheck -s
      bash -e SC1091,SC2034 internal/verify-bridge-e2e/0{7,8,9,10,11,12}-*.sh exits 0 across all 6 files)
    - Consolidated Task 1 + Task 2 shellcheck (12 scenarios total) all pass (verify: shellcheck -s bash -e
      SC1091,SC2034 internal/verify-bridge-e2e/{00,01,02,03,04,05,06,07,08,09,10,11,12}-*.sh exits 0)
  </acceptance_criteria>
  <verify>
    <automated>shellcheck -s bash -e SC1091,SC2034 internal/verify-bridge-e2e/00-happy-path.sh internal/verify-bridge-e2e/01-unauthorized.sh internal/verify-bridge-e2e/02-not-found.sh internal/verify-bridge-e2e/03-critical-addon-protected.sh internal/verify-bridge-e2e/04-prevented-destroy.sh internal/verify-bridge-e2e/05-already-installed.sh internal/verify-bridge-e2e/06-locked.sh internal/verify-bridge-e2e/07-nonce-expired.sh internal/verify-bridge-e2e/08-nonce-used.sh internal/verify-bridge-e2e/09-install-timeout.sh internal/verify-bridge-e2e/10-upstream-error.sh internal/verify-bridge-e2e/11-pwned.sh internal/verify-bridge-e2e/12-version-mismatch.sh</automated>
    <fails_when>any of the 12 scenarios fails shellcheck (SC1091 + SC2034 already suppressed — any other SC#### warning fails the gate), OR scenario 09 makes a live Bridge call instead of exiting 0 with `skipped` annotation (the synthetic-scenario contract), OR scenario 10 performs host-network manipulation (the CF-14 violation), OR scenario 11 fails to assert that `tofu apply` exited 0 (pwned is Warning, not Error — the apply must succeed), OR scenario 12 names the testdata file `version_below_min.txt` instead of `version.txt` (the DOCS.md anchor convention violation)</fails_when>
  </verify>
  <done>
    All 12 error-code scenarios (01-unauthorized through 12-version-mismatch) are committed alongside
    Plan 01's 00-happy-path.sh. Each scenario follows Plan 01's structural template. Scenarios 07 + 08
    exercise the X-Force-Destroy nonce lifecycle; scenario 09 is the synthetic skip per D-10; scenario 10
    documents the operator-driven upstream-error setup per CF-14; scenario 11 exercises the pwned
    Warning with the no-empirical-observation fallback per A8; scenario 12 exercises the Provider
    handshake rejection with the DOCS.md anchor filename convention honored (version.txt, not
    version_below_min.txt). All 12 scenarios + 00-happy-path.sh pass shellcheck with the repo's
    SC1091/SC2034 exemptions.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                       | Description                                                                                      |
| ------------------------------ | ------------------------------------------------------------------------------------------------ |
| Operator workstation → host    | Scenarios reach the Bridge via HTTPS on port 8124 from the operator's Tailscale workstation. The |
|                                | scenarios also call the Bridge's mutating endpoints (POST /v1/addons/{slug}/install, /uninstall, |
|                                | /options) and the auth endpoints (POST /v1/auth/nonce). Bearer token comes from                  |
|                                | `_lib.sh::BRIDGE_TOKEN`.                                                                         |
| Operator workstation → host FS | Scenarios 07 + 08 + 11 call `_lib.sh::snapshot_state` and `fingerprint_state` which SSH into the |
|                                | host for /data/terraform.tfstate backup + /v1/state/index read. The snapshot writes inside the   |
|                                | Bridge's `addon_config:rw` mount, covered by HA backup integration (CF-13).                      |
| Scenario → live Bridge state   | Scenarios 04, 05, 06, 08, 11, 12 mutate the live Bridge's add-on state for `local_test-addon`.   |
|                                | Per D-16 + Pitfall 6, every state-mutating scenario snapshots before + fingerprints after. Per   |
|                                | AGENTS.md §🔌, Bridge restarts are NEVER triggered by a scenario.                                |

## STRIDE Threat Register

| Threat ID | Category               | Component                                         | Severity | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                                                                                                                                                                                |
| --------- | ---------------------- | ------------------------------------------------- | -------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-14-02   | Information Disclosure | Bearer token in captured `tofu apply` output      | medium   | mitigate    | The Provider marks `bearer_token` as `Sensitive: true` (CF-03 + Phase 13 schema), so `tofu apply` output masks the value as `(sensitive value)`. Scenarios assert on `error_code` strings + Provider Summary text, NEVER on the bearer value. Verify: scenario 01 sends a wrong bearer (NOT the real one) — the captured `diagnostics/unauthorized.txt` file proves the Bridge rejects bad tokens.                                             |
| T-14-05   | Tampering              | Test add-on accidentally added to critical_addons | medium   | mitigate    | Scenario 03's header comment + CF-12 default `["core_mosquitto", "core_zigbee2mqtt", "core_esphome"]` make this explicit. The scenario targets `core_mosquitto` (an always-on critical slug), never `local_test-addon`. Recovery: if the operator accidentally edits the Bridge's `critical_addons` to include `local_test-addon`, scenario 05's iteration 1 fails with `critical_addon_protected` and the operator restores the default list. |
| T-14-06   | Tampering              | Stale state after nonce reuse                     | medium   | mitigate    | Scenario 08 reuses a consumed nonce for the SECOND `DELETE` — the Bridge returns `nonce_used` BEFORE performing any state mutation (LIFE-03 + Phase 12 D-06). The test add-on is NOT uninstalled by the second call. Verify: scenario 08 reinstalls the test add-on via `tofu apply` at the end (cleanup discipline) so scenario 09 + 11 + 12 find it installed.                                                                               |
| T-14-07   | Denial of Service      | Exhaust Supervisor's install job slots            | low      | accept      | Scenario 09 is the synthetic fallback per D-10 + A8 — the scenario's pre-flight exits 0 with `skipped` annotation, NOT by actually exhausting Supervisor's job slots. The script's body contains no curl/tofu call to the live Bridge. The `install_timeout.txt` testdata file is a stub annotated `[not empirically observed — synthetic scenario]`.                                                                                          |
| T-14-08   | Tampering              | Provider handshake version drift undetected       | low      | accept      | Scenario 12 exercises the Provider-side handshake rejection by setting an impossibly-high `min_provider_version` in the test `*.tf`. The Bridge itself is NOT touched; the Provider's `Configure` is the only code path affected. Recovery: revert the test `*.tf` (it's in `/tmp/`, ephemeral).                                                                                                                                               |
| T-14-SC   | Tampering              | Package legitimacy of any new install             | high     | mitigate    | Plan 02 installs ZERO new external packages. The 12 scenarios use only `bash`, `curl`, `jq`, `tofu`, `ssh` — all already required by Plan 01 or the existing verify-bridge-no-token-leak.sh / verify-install-provider.sh pipelines. No SLOP/SUS packages to audit.                                                                                                                                                                             |

</threat_model>

<verification>
For overall Phase 14 Plan 02 success (not just this plan's surface):

1. **SC-4 error-code coverage** (this plan's surface): the 12 scenarios each provoke one `error_code` and capture the
   response to `terraform-bridge/internal/testdata/diagnostics/<error_code>.txt`. Operator runs them on the live Bridge;
   each scenario's `PASS` or `FAIL` line documents the outcome. Captured files are the source Plan 03's DOCS.md
   troubleshooting section reads from.

2. **SC-4 documented-text alignment**: when the operator runs the 12 scenarios and a captured diagnostic differs from
   the documented Provider Summary text, Plan 03's DOCS.md update fixes the docs to match the observation (D-09). Plan
   02 captures; Plan 03 reconciles.

3. **SC-2 drift behavior** (partial coverage): scenario 04 (`prevented_destroy`) and scenario 11 (`pwned`) exercise
   Update-side drift. The remaining SC-2 surface (state refresh no-op via `UseStateForUnknown()`, 404 → recreate plan)
   is implicitly covered by the `tofu apply` cycle itself — when a resource disappears from the host, `tofu plan` shows
   a recreate diff (not a destroy diff) per PROV-04 + PROV-10.

4. **SC-1 + SC-3** (Plan 01's surface): Plan 01's `00-happy-path.sh` remains the authoritative verifier. Plan 02 does
   not modify it.

5. **SC-5 operator documentation** (NOT covered by this plan; Plan 03 owns): the captured testdata files this plan
   creates are the source DOCS.md reads from. Plan 03's README + DOCS
   - cleanup task scope.
   </verification>

<success_criteria>

- All 12 error-code scenarios are committed at `internal/verify-bridge-e2e/0{1..12}-*.sh` per D-06.
- Every scenario sources `_lib.sh`, calls `preflight`, and exits 0 with `skipped` annotation when prerequisites are
  missing (per D-10).
- Every scenario's captured-diagnostic file matches the naming convention
  `terraform-bridge/internal/testdata/diagnostics/<error_code>.txt` (where `<error_code>` is the kebab-case DOCS.md
  anchor name — e.g. `version.txt` for the `#troubleshooting-version` anchor).
- Scenarios that touch state (07, 08, 11) call `snapshot_state` before + `fingerprint_state` after (D-16 + D-17).
- Scenarios 04 + 11 assert the canonical Summary text from `internal/diagnostics/doc.go` (`ErrPreventedDestroyText`,
  `PwnedWarningText`) appears verbatim in the captured Provider output.
- Scenario 09 is the synthetic-skip fallback per D-10; the script's body makes no live Bridge call.
- Scenario 10 documents the operator-driven setup per CF-14; the script itself does no network manipulation.
- All 12 scenarios + Plan 01's `00-happy-path.sh` pass shellcheck with the repo's SC1091/SC2034 exemptions.
  </success_criteria>

<output>
Create `.planning/phases/14-real-ha-end-to-end-verification-operator-documentation/14-02-SUMMARY.md`
when complete — the standard atomic-commit summary for this plan.
</output>
