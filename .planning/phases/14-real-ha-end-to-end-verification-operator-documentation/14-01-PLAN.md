---
phase: 14-real-ha-end-to-end-verification-operator-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/test-addon/config.yaml
  - tools/test-addon/build.yaml
  - tools/test-addon/Dockerfile
  - tools/test-addon/run.sh
  - tools/test-addon/README.md
  - internal/verify-bridge-e2e/_lib.sh
  - internal/verify-bridge-e2e/00-happy-path.sh
  - terraform-bridge/internal/testdata/diagnostics/.gitkeep
  - terraform-bridge/internal/testdata/apply-output/.gitkeep
  - terraform-bridge/internal/testdata/state-fingerprints/.gitkeep
autonomous: true
requirements:
  - OPS-04

estimate:
  tokens: 55000
  raw_tokens: 28000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "`tools/test-addon/` is a complete HA add-on (config.yaml + build.yaml + Dockerfile + run.sh + README.md) that
      follows the 4-file pattern every other add-on in the repo follows, with slug `local_test-addon` per D-05;
      its schema exposes the two string options `log_level` (default `info`) and `dummy_setting` (default
      `default`) so Update + pwned-warning scenarios (Plan 02) have something to vary"
    - "`internal/verify-bridge-e2e/_lib.sh` is a single sourced helper that exposes `BRIDGE_URL`, `BRIDGE_TOKEN`,
      `TEST_ADDON_SLUG`, `snapshot_state`, `fingerprint_state`, `cleanup_scenario_baks`, and color helpers; it
      reads the bearer token from `/usr/share/hassio/addons/data/terraform-bridge/initial-token` on the host
      (honoring the `BRIDGE_TOKEN` env override per D-08) and exits 2 with a clear remediation message when
      the file is unreadable"
    - "`internal/verify-bridge-e2e/00-happy-path.sh` runs the install → start → options → stop → uninstall cycle
      against `local_test-addon` FIVE consecutive times via `tofu apply` (D-11) and asserts the SC-3 idempotency
      property: every iteration after the first reports `No changes` in captured `tofu plan` output"
    - "Every scenario script's first action is a pre-flight assertion: `command -v tofu` + the Provider binary at
      `terraform-provider-homeassistant/terraform-provider-homeassistant` + `curl $BRIDGE_URL/healthz` returns
      200. When any prerequisite is missing the script exits 0 with a `skipped — <reason>` annotation per D-10
      so the script is safe for the executor (which lacks `tofu`/`docker`/`go` per the Environment Availability
      audit) AND for the operator on a workstation that does not yet have the Bridge rebuilt"
    - "`terraform-bridge/internal/testdata/diagnostics/`, `apply-output/`, and `state-fingerprints/` directories
      exist as git-tracked empty directories (via `.gitkeep` files) so Plan 02's 12 error-code scenarios and
      Plan 03's DOCS.md expansion have a known location to write captured artifacts without committing
      actual diagnostic text this plan"
    - "All YAML files pass yamllint; all shell scripts pass shellcheck (with the repo-standard SC1091/SC2034
      exemptions per AGENTS.md Gotcha #4); markdownlint-cli2 + prettier stay green on `tools/test-addon/README.md`;
      the 3-file versioning scheme passes `internal/validate-versions.sh` for the new test add-on (publishing
      as `tools/test-addon/` requires config.yaml X.Y.Z-N + build.yaml X.Y.Z + README badge vX.Y.Z to agree
      per AGENTS.md Critical Gotchas #1 — the pre-commit hook auto-discovers any new add-on directory)"
  artifacts:
    - path: "tools/test-addon/config.yaml"
      provides: "HA add-on manifest: name, slug local_test-addon, version 0.1.0-0, arch amd64, options {log_level,
        dummy_setting} with sensible defaults, schema declaring both as `str?` per the YAML-list DSL convention
        (mirrors terraform-bridge/config.yaml:33-34). `init: false`, `startup: application`, `boot: manual`,
        `host_network: false`, `hassio_api: false` (the test add-on has no Supervisor client needs; the
        Provider reaches it through Bridge). NO `map:` block — the test add-on has no /data requirements
        (the Provider's state file lives in the Bridge add-on's /data, not here)"
      contains: "slug.*local_test-addon|version.*0\\.1\\.0-0"
    - path: "tools/test-addon/build.yaml"
      provides: "build_from.amd64 = ghcr.io/home-assistant/amd64-base:3.24 (matches Bridge Dockerfile base);
        labels block with org.opencontainers.image.* metadata; args.VERSION = 0.1.0 (matches config.yaml's
        version base per the 3-file scheme enforced by internal/validate-versions.sh). No aarch64 entry —
        per PROJECT.md Out of Scope, only amd64 is built in this repo"
      contains: "VERSION.*0\\.1\\.0"
    - path: "tools/test-addon/Dockerfile"
      provides: "Single-stage `FROM ${BUILD_FROM}`; COPY run.sh /run.sh + chmod a+x; CMD [\"/run.sh\"]. ARGs
        declared before FROM (passes internal/validate-dockerfile-args.sh). No apk installs — the no-op
        run.sh is the entire payload"
      contains: "BUILD_FROM|COPY.*run.sh"
    - path: "tools/test-addon/run.sh"
      provides: "No-op sleep infinity wrapped in bashio setup (mirrors D-03's intent — the container process
        stays alive so Supervisor sees the add-on as `started: true`, but nothing observable happens).
        bashio::log.info at startup so the Bridge's `/v1/addons/local_test-addon/info` response is non-empty"
      contains: "sleep infinity|bashio::log.info"
    - path: "tools/test-addon/README.md"
      provides: "One-paragraph explanation of the add-on's purpose (live test target for the Phase 14 verify
        suite; the Provider can install/configure/start/stop/uninstall it without touching any real add-on).
        Includes `version-v0.1.0` badge URL per the 3-file scheme"
      contains: "version-v0\\.1\\.0"
    - path: "internal/verify-bridge-e2e/_lib.sh"
      provides: "Single sourced helper. Exports BRIDGE_URL (default `https://ha-nextgen.akentner.ts.net:8124`
        per D-01), BRIDGE_TOKEN (read from `/usr/share/hassio/addons/data/terraform-bridge/initial-token`
        on the host via SSH when BRIDGE_TOKEN is unset), TEST_ADDON_SLUG=`local_test-addon`, REPO_ROOT,
        TESTDATA_DIR. Provides `snapshot_state <scenario>` (D-16: cp /data/terraform.tfstate to
        /data/terraform.tfstate.bak.<scenario>), `fingerprint_state <scenario> <when>` (D-17: GET
        /v1/state/index → terraform-bridge/internal/testdata/state-fingerprints/<scenario>-<when>.json),
        `cleanup_scenario_baks` (D-18: find /data -maxdepth 1 -name 'terraform.tfstate.bak.*' -mtime +7
        -delete), color helpers (red/green/yellow)"
      contains: "snapshot_state|fingerprint_state|BRIDGE_TOKEN|local_test-addon"
    - path: "internal/verify-bridge-e2e/00-happy-path.sh"
      provides: "Five-iteration happy-path scenario. Pre-flight: `command -v tofu` + Provider binary exists +
        `curl /healthz` returns 200 (exits 0 with `skipped` annotation if any check fails per D-10). For
        each iteration 1..5: snapshot state, run `tofu apply -auto-approve` against the embedded `*.tf`
        file, capture plan+apply output to terraform-bridge/internal/testdata/apply-output/<iter>.txt,
        fingerprint state via GET /v1/state/index after the run. Asserts iterations 2-5 report
        `No changes` (SC-3). The embedded `*.tf` does NOT set lifecycle.prevent_destroy = true per Pitfall 7"
      contains: "No changes|skipped|local_test-addon"
    - path: "terraform-bridge/internal/testdata/diagnostics/.gitkeep"
      provides: "Marks the per-error_code diagnostic capture directory as git-tracked so Plan 02's 12
        scenarios can `tee` captured responses without committing them at plan-author time. No actual
        diagnostic text in this plan"
      contains: ""
    - path: "terraform-bridge/internal/testdata/apply-output/.gitkeep"
      provides: "Marks the apply-output capture directory as git-tracked. Plan 01's `00-happy-path.sh`
        writes 1..5.txt here at operator-invocation time"
      contains: ""
    - path: "terraform-bridge/internal/testdata/state-fingerprints/.gitkeep"
      provides: "Marks the state-fingerprint capture directory as git-tracked. Plan 01's happy-path + Plan
        02's 12 scenarios write `*-before.json` and `*-after.json` pairs here per D-17"
      contains: ""
  key_links:
    - from: "internal/verify-bridge-e2e/_lib.sh"
      to: "tools/test-addon/config.yaml"
      via: "TEST_ADDON_SLUG constant (`local_test-addon`) matches the slug declared in the test add-on's
        config.yaml. Mismatched slug → every scenario's POST /v1/addons/{slug}/* call returns 404 not_found
        and the scenario exits non-zero"
      pattern: "local_test-addon"
    - from: "internal/verify-bridge-e2e/00-happy-path.sh"
      to: "internal/verify-bridge-e2e/_lib.sh"
      via: "Sources _lib.sh to inherit BRIDGE_URL + BRIDGE_TOKEN + TEST_ADDON_SLUG + snapshot/fingerprint
        helpers. No redeclaration of these constants in the scenario"
      pattern: "source.*_lib\\.sh"
    - from: "internal/verify-bridge-e2e/00-happy-path.sh"
      to: "terraform-provider-homeassistant/terraform-provider-homeassistant"
      via: "Pre-flight asserts the Provider binary exists at the path `make install-provider` installs it
        to. Missing binary → script exits 0 with `skipped — make install-provider not yet run` annotation
        per D-10 + Pitfall 1. The script does NOT rebuild the Provider — that is operator-driven per Phase 15
        TOFU-04"
      pattern: "command -v tofu|terraform-provider-homeassistant$"
    - from: "internal/verify-bridge-e2e/_lib.sh"
      to: "/usr/share/hassio/addons/data/terraform-bridge/initial-token"
      via: "BRIDGE_TOKEN retrieval defaults to SSH+cat from the host filesystem path Supervisor uses for
        the Bridge add-on's /data mount (per Phase 10 D-08 + CF-01). Operators with the Bearer already in
        env override via `BRIDGE_TOKEN=...` env var. Failure to read the file is FATAL (exit 2) so the
        scenario cannot proceed with a missing/invalid token"
      pattern: "initial-token|ssh.*cat"
    - from: "tools/test-addon/config.yaml + build.yaml + README.md"
      to: "internal/validate-versions.sh"
      via: "The pre-commit hook auto-discovers every directory with config.yaml + build.yaml. The new
        `tools/test-addon/` is therefore subject to the 3-file versioning rule: config.yaml 0.1.0-0,
        build.yaml VERSION 0.1.0, README.md badge version-v0.1.0 — all three must agree or the
        pre-commit hook fails per AGENTS.md Critical Gotchas #1"
      pattern: "0\\.1\\.0"

---

<objective>
Land Phase 14's tracer slice: a minimal HA add-on (`tools/test-addon/`) that the Provider can install +
configure + start + stop + uninstall against a live Bridge without touching any real add-on, plus the
verify-scenario helper (`internal/verify-bridge-e2e/_lib.sh`) and the SC-1/SC-3 idempotency scenario
(`00-happy-path.sh`). Plans 02 and 03 expand from this skeleton — Plan 02 adds the 12 per-`error_code`
scenarios on top of `_lib.sh`, Plan 03 adds README.md + DOCS.md + cleanup on top of the testdata
directory structure this plan creates.

Purpose: catching missing-infrastructure dead-ends on the smallest possible vertical slice. The tracer
proves (a) the test add-on's local-build pipeline actually works against `ha-nextgen`, (b) the bearer
token retrieval path is reachable from the operator workstation, (c) the `tofu apply` round-trip
produces a "No changes" plan on iteration 2 — proving Provider + Bridge + test add-on integrate end-to-end
on the operator's workstation. If any of those primitives are wrong, fixing it in Plan 02 is one commit;
fixing it after all 12 error scenarios are written is twelve commits.

Output: 5 new files in `tools/test-addon/` (config.yaml + build.yaml + Dockerfile + run.sh + README.md),
2 new files in `internal/verify-bridge-e2e/` (_lib.sh + 00-happy-path.sh), 3 `.gitkeep` files in
`terraform-bridge/internal/testdata/` subdirectories. Zero changes to Bridge or Provider source —
this plan creates verification infrastructure, not code.

Requirements: OPS-04 (operator documentation). The two files this plan writes (_lib.sh + 00-happy-path.sh)
are the verify suite's foundation — Plan 03's DOCS.md references this suite by name and explains how to
invoke it. Plan 02's scenarios extend from this skeleton. OPS-04's surface requirement ("install steps,
token issuance, example *.tf, troubleshooting section") is satisfied downstream by Plan 03; this plan
provides the empirical-observation harness Plan 03 will reference.
</objective>

<execution_context>
@/data/.config/opencode/gsd-core/workflows/execute-plan.md
@/data/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md (Phase 14 entry, lines 280-308 — SC-1, SC-2, SC-3 are this plan's surface)
@.planning/REQUIREMENTS.md (OPS-04 line 142-144 — the single Phase-14 requirement)
@.planning/phases/14-real-ha-end-to-end-verification-operator-documentation/14-CONTEXT.md (D-01 host identity, D-02 no-unsolicited-restart, D-03 test add-on design, D-04 hermetic publishing, D-05 slug, D-06 scenario names, D-07 captured-diagnostic discipline, D-08 helper responsibilities, D-10 skip-when-unsafe, D-11 5-iteration idempotency, D-16 state snapshot, D-17 fingerprint, CF-01..CF-14 carried forward)
@.planning/phases/14-real-ha-end-to-end-verification-operator-documentation/14-RESEARCH.md (Standard Stack §Core §Supporting, Recommended Project Structure §"Plan 01 owns" tree, Pattern 1 + Pattern 2, Architecture Patterns §Tracer-First Decomposition, Pitfall 1 + Pitfall 2 + Pitfall 4 + Pitfall 7, Environment Availability table A6 + A9, Open Questions #2)
@.planning/phases/13-provider-resource-data-sources-schema-handshake/13-CONTEXT.md (CF-07 Provider schema, CF-08 diagnostics contract, CF-09 prevent_destroy default, CF-13 HA backup integration)
@.planning/phases/12-bridge-write-api-safety-concurrency-index/12-CONTEXT.md (CF-06 critical_addons + per-slug mutex + X-Force-Destroy nonce — exercised but not modified)
@.planning/phases/10-auth-layer-structured-logging-healthcheck/10-CONTEXT.md (CF-04 /healthz contract, CF-05 bind-gate, token format base64url 43 chars / SHA-256 at-rest)
@internal/verify-bridge-no-token-leak.sh (Phase 10 structural template — `set -euo pipefail`, color helpers, exit-non-zero-on-assertion discipline; mirrored verbatim in `_lib.sh`)
@tools/test-bridge-fixture/main.go (Phase 15's CI fixture — sibling-directory convention; Phase 14's `tools/test-addon/` follows the same "sibling-of-addons directory, own README explaining purpose" pattern)
@terraform-bridge/contract/types.go (Phase 14's verify scenarios consume the JSON envelopes: AddOnInfo for /v1/addons/{slug}/info, ErrorResponse for 4xx/5xx bodies, HealthResponse for /healthz — read for the curl + jq patterns _lib.sh uses)
@terraform-bridge/internal/supervisor/client.go:661-680 (the MapError switch — source of truth for every `error_code` Plan 02's scenarios must provoke)
@terraform-provider-homeassistant/internal/diagnostics/doc.go (canonical per-error_code Summary text constants; the strings _lib.sh's grep assertions match against)
@terraform-provider-homeassistant/DOCS.md:402-410 (the Provider troubleshooting section that Plan 02 captures and Plan 03 cross-links; rows exist for every error_code in BRIDGE-09)
@internal/validate-versions.sh (3-file versioning scheme — Plan 01's tools/test-addon/ must satisfy or pre-commit hook fires)
@internal/validate-addon-config.py:99-106 (auto-discovery picks up any directory with config.yaml + build.yaml — Plan 01's tools/test-addon/ WILL be validated; config.yaml must declare all required fields)
@internal/validate-dockerfile-args.sh (the test add-on's Dockerfile must declare ARGs before FROM or this script fails)
@info_hub/config.yaml + build.yaml + Dockerfile + run.sh (simplest existing add-on — Plan 01's tools/test-addon/ mirrors this 4-file structure with even simpler contents)
@AGENTS.md (Live Systems rule §🔌 — no unsolicited restarts; pre-commit pipeline §🛠️; 3-file versioning §🔑)
</context>

<interfaces>
Locked contracts from earlier phases that this plan consumes (do NOT modify):

- **terraform-bridge/contract/types.go**: AddOnInfo + ErrorResponse + HealthResponse + VersionHandshake — every
  JSON shape the verify scenarios' curl + jq assertions must match. Plan 01's `_lib.sh` does not decode
  JSON itself; it delegates to `jq -e` for path-level assertions (e.g. `.error_code == "unauthorized"`).

- **terraform-bridge/internal/supervisor/client.go MapError** (lines 661-680): the (HTTP status, error_code)
  pairs are the canonical target for Plan 02's scenarios. Plan 01's pre-flight uses `/healthz` (which is
  NOT a MapError-mapped path; it's the always-200 / always-503 endpoint per Phase 10 D-08).

- **terraform-provider-homeassistant/internal/diagnostics/doc.go**: ErrUnauthorizedText, ErrNotFoundText,
  PwnedWarningText, etc. — the strings that appear in Provider Diagnostic Summary fields. Plan 01's
  happy-path scenario does NOT assert on these (it expects success); Plan 02 asserts them per scenario.

- **terraform-bridge/internal/httpapi/handlers**: every `/v1/*` endpoint Plan 01 + Plan 02 exercise exists.
  No code changes.

- **3-file versioning scheme** (AGENTS.md §🔑): tools/test-addon/ is auto-discovered by
  internal/validate-versions.sh. The three files MUST carry matching versions:
  - config.yaml: `version: "0.1.0-0"` (subpatch, always starts at -0)
  - build.yaml: `VERSION: "0.1.0"` (semver, no subpatch)
  - README.md: badge URL containing `version-v0.1.0`

Locked decisions from 14-CONTEXT.md that the executor MUST follow without re-deciding:

- **D-03** (test add-on design): base image `ghcr.io/home-assistant/amd64-base:3.24`; no-op `run.sh`;
  schema exposes string options for Update/pwned variation.
- **D-04** (hermetic publishing): no GitHub release, no custom-repo URL; `tools/test-addon/` is consumed
  by HA's local-build pipeline via `ha addons reload` after each rebuild.
- **D-05** (slug): `local_test-addon` — HA's convention for locally-built add-ons.
- **D-06** (scenario names): Plan 01 ships the `00-happy-path.sh` slot; Plan 02 ships 01..12.
- **D-08** (helper responsibilities): `_lib.sh` handles bearer token retrieval, slug, state snapshot
  discipline, jq wrappers; color helpers; pre-flight assertion (per D-10).
- **D-10** (skip-when-unsafe): pre-flight `command -v tofu` + Provider binary + `/healthz` 200; when any
  prerequisite is missing the scenario exits 0 with `skipped — <reason>` so the executor (which lacks
  these binaries per the Environment Availability audit) and the developer (pre-rebuild) both see a clean
  pass instead of an unrelated CI failure.
- **D-11** (5-iteration idempotency): the happy-path scenario runs `tofu apply` FIVE consecutive times
  against the same `*.tf` and asserts "No changes" on iterations 2..5.
- **D-16** (state snapshot): `_lib.sh::snapshot_state` copies `/data/terraform.tfstate` to
  `/data/terraform.tfstate.bak.<scenario>` before any destructive scenario.
- **D-17** (fingerprint): `_lib.sh::fingerprint_state` calls `GET /v1/state/index` and writes the
  response to `terraform-bridge/internal/testdata/state-fingerprints/<scenario>-<when>.json`.

Deferred to later plans (do NOT implement here):

- Plan 02: the 12 per-`error_code` scenarios (01-unauthorized.sh through 12-version-mismatch.sh) — each
  provokes one error_code, captures the response to its testdata file, asserts the captured text.
- Plan 03: README.md (D-12 1-pager) + DOCS.md expansion (D-13..D-15) + 99-cleanup.sh (D-18). These live
  in Plan 03 because Plan 02's captured diagnostics feed Plan 03's troubleshooting cross-links.

Plan 01 deliverable proof: the executor workstation, the operator workstation, and any CI runner can all
run `bash internal/verify-bridge-e2e/00-happy-path.sh` and observe one of two outcomes: (a) the pre-flight
checks all pass and the script enters the 5-iteration loop; (b) the pre-flight checks fail and the
script exits 0 with a `skipped` annotation naming the missing prerequisite. Neither outcome requires a
live Bridge at plan-author time.
</interfaces>

<tasks>

<task type="tracer">
  <name>Task 1: scaffold tools/test-addon/ + internal/verify-bridge-e2e foundation + 00-happy-path scenario + testdata directory skeleton</name>
  <files>
    - tools/test-addon/config.yaml
    - tools/test-addon/build.yaml
    - tools/test-addon/Dockerfile
    - tools/test-addon/run.sh
    - tools/test-addon/README.md
    - internal/verify-bridge-e2e/_lib.sh
    - internal/verify-bridge-e2e/00-happy-path.sh
    - terraform-bridge/internal/testdata/diagnostics/.gitkeep
    - terraform-bridge/internal/testdata/apply-output/.gitkeep
    - terraform-bridge/internal/testdata/state-fingerprints/.gitkeep
  </files>
  <read_first>
    - internal/verify-bridge-no-token-leak.sh (the structural template — `set -euo pipefail`, color
      helpers, exit-non-zero-on-assertion discipline; every shell script this task writes follows the
      same pattern)
    - tools/test-bridge-fixture/main.go (sibling-directory convention; Plan 14's tools/test-addon/
      follows the same "sibling-of-addons directory, own README explaining purpose" pattern)
    - terraform-bridge/internal/supervisor/client.go:661-680 (MapError switch — the source of truth
      for the error_code enumeration Plan 02 will exercise; Plan 01's pre-flight uses /healthz which
      is NOT a MapError path)
    - terraform-provider-homeassistant/DOCS.md §"Troubleshooting" beginning at line 402 (the canonical
      troubleshooting section that Plan 03 cross-links; Plan 01's 00-happy-path.sh does NOT assert on
      these Diagnostic Summaries because it expects success — Plan 02 is where those assertions live)
    - .planning/phases/14-real-ha-end-to-end-verification-operator-documentation/14-CONTEXT.md D-03..D-08,
      D-10, D-11, D-16, D-17 (locked decisions for this plan; honored verbatim)
    - .planning/phases/14-real-ha-end-to-end-verification-operator-documentation/14-RESEARCH.md Pattern 1
      §"Example 1: _lib.sh skeleton" (lines 765-843) + Pattern 2 §"Five-iteration idempotency loop"
      (lines 527-567) — the structural templates this task's _lib.sh + 00-happy-path.sh follow
    - info_hub/{config.yaml, build.yaml, Dockerfile, run.sh} (the simplest existing 4-file add-on in the
      repo; Plan 01's tools/test-addon/ is even simpler — single-stage Dockerfile, no Python, no app/
      directory)
    - AGENTS.md §🔑 Critical Versioning Scheme (the 3-file rule — config.yaml X.Y.Z-N, build.yaml X.Y.Z,
      README badge vX.Y.Z; pre-commit hook auto-discovers any new add-on directory)
    - internal/validate-versions.sh (the pre-commit hook implementation; Plan 01's tools/test-addon/
      MUST satisfy or commit fails)
    - internal/validate-addon-config.py:99-106 (auto-discovery filter `(d / 'config.yaml').exists()
      and (d / 'build.yaml').exists()` — tools/test-addon/ WILL be validated; config.yaml must declare
      all required fields like `name`, `slug`, `version`)
    - internal/validate-dockerfile-args.sh (the test add-on's Dockerfile must declare ARGs before the
      FROM instruction or this script fails)
  </read_first>
  <action>
    This task is the tracer. Build the smallest possible vertical slice that proves the live-verify
    harness integrates end-to-end: a real (trivial) HA add-on + the helper that every Phase-14 scenario
    sources + the one scenario that exercises the canonical install/start/options/stop/uninstall cycle
    five times. Plan 02 adds error-code scenarios on top of this skeleton; Plan 03 adds the operator
    documentation.

    Build bottom-up — finish the test add-on first, then the helper, then the scenario — so each layer
    is committed individually and the failure mode of any single step is identifiable.

    1. **tools/test-addon/config.yaml** (NEW) — Minimal HA add-on manifest. Follow the structure of
       `info_hub/config.yaml` exactly but with even simpler options. Required fields per
       `internal/validate-addon-config.py`: `name`, `version`, `slug`, `description`, `arch`. Options
       block carries two string fields (`log_level` default `info`, `dummy_setting` default `default`)
       so Plan 02's Update/pwned scenarios have something to vary per D-03. Schema block declares both
       as `str` (single-line form — this is a single-value option, not a list). `init: false`,
       `startup: application`, `boot: manual`, `host_network: false`, `hassio_api: false` (no Supervisor
       client needed; the Provider reaches this add-on through the Bridge). NO `map:` block — the test
       add-on has no /data requirements (the Provider's state file lives in the Bridge add-on's /data,
       not here; per PROV-06 + STATE-01). Version: `0.1.0-0` per the 3-file scheme (AGENTS.md §🔑).
       Slug: `local_test-addon` per D-05.

    2. **tools/test-addon/build.yaml** (NEW) — `build_from.amd64` =
       `ghcr.io/home-assistant/amd64-base:3.24` (matches `terraform-bridge/Dockerfile:5`). Labels block
       with `org.opencontainers.image.title`, `.description`, `.source`, `.licenses`. `args.VERSION`
       = `0.1.0` (matches config.yaml's `0.1.0-0` per the 3-file scheme: build.yaml carries the semver
       base, config.yaml adds the `-0` subpatch). No `aarch64:` entry — per PROJECT.md Out of Scope,
       only amd64 is built in this repo.

    3. **tools/test-addon/Dockerfile** (NEW) — Multi-line `ARG BUILD_FROM` first (so it satisfies
       `internal/validate-dockerfile-args.sh`'s ARG-before-FROM check); then `FROM ${BUILD_FROM}`;
       `COPY run.sh /run.sh`; `RUN chmod a+x /run.sh`; `CMD ["/run.sh"]`. No `RUN apk add` — the no-op
       `run.sh` is the entire payload. Build context = the `tools/test-addon/` directory.

    4. **tools/test-addon/run.sh** (NEW) — No-op sleep infinity wrapped in bashio setup. Per
       D-03: the container process stays alive so Supervisor sees the add-on as `started: true`, but
       nothing observable happens. One `bashio::log.info` line at startup so the Bridge's
       `GET /v1/addons/local_test-addon/info` response carries a non-empty log line for post-mortem
       debugging. Add a comment header explaining the add-on's purpose (1 sentence) so anyone
       `cat`-ing the file understands why it exists.

    5. **tools/test-addon/README.md** (NEW) — One paragraph (5-10 lines) explaining: this add-on
       exists so the Provider can install/configure/start/stop/uninstall it during Phase 14's verify
       suite without touching any real add-on on the host. It does nothing. The schema fields
       `log_level` and `dummy_setting` exist solely so Update scenarios have something to vary. The
       slug `local_test-addon` follows HA's `local_` prefix convention. Two short sections:
       (1) "What it does" — 2-3 lines; (2) "Building" — 1 sentence on `docker build` + `ha addons reload`.
       Include `version-v0.1.0` badge URL per the 3-file scheme (the
       `internal/validate-versions.sh` regex matches `version-vX.Y.Z`). Markdown format must pass
       markdownlint-cli2 (max 120 chars per `.markdownlint.json` MD013).

    6. **internal/verify-bridge-e2e/_lib.sh** (NEW) — Single sourced helper. Mirror the structural
       template from `internal/verify-bridge-no-token-leak.sh`: `set -euo pipefail` is NOT used here
       (this is a library file, sourced by scenarios that DO use it). Constants:
       - `BRIDGE_HOST="${BRIDGE_HOST:-ha-nextgen.akentner.ts.net}"` per D-01.
       - `BRIDGE_PORT="${BRIDGE_PORT:-8124}"`.
       - `BRIDGE_URL="https://${BRIDGE_HOST}:${BRIDGE_PORT}"`.
       - `TEST_ADDON_SLUG="local_test-addon"` per D-05.
       - `REPO_ROOT="$(cd "${SCRIPT_DIR}/../.." && pwd)"`.
       - `TESTDATA_DIR="${REPO_ROOT}/terraform-bridge/internal/testdata"`.
       Token retrieval: `BRIDGE_TOKEN` defaults to SSH-reading
       `/usr/share/hassio/addons/data/terraform-bridge/initial-token` from
       `${BRIDGE_HOST%.akentner.ts.net}` (strips the `.akentner.ts.net` suffix so the SSH target is
       the bare hostname). Operators override via `BRIDGE_TOKEN=...` env var. Failure exits 2 with a
       clear remediation message. Functions:
       - `snapshot_state <scenario>` — D-16: SSH `cp /data/terraform.tfstate
         /data/terraform.tfstate.bak.<scenario>` (no-op when the file doesn't exist yet).
       - `fingerprint_state <scenario> <when>` — D-17: curl GET /v1/state/index → write to
         `${TESTDATA_DIR}/state-fingerprints/<scenario>-<when>.json` (mkdir -p first).
       - `cleanup_scenario_baks` — D-18: SSH `find /data -maxdepth 1 -name 'terraform.tfstate.bak.*'
         -mtime +7 -delete`. NOT called by individual scenarios — Plan 03's 99-cleanup.sh owns this.
       - `red`, `green`, `yellow` color helpers (verbatim from verify-bridge-no-token-leak.sh).
       - `preflight` — D-10 gate: `command -v tofu` + Provider binary exists at
         `terraform-provider-homeassistant/terraform-provider-homeassistant` + `curl
         $BRIDGE_URL/healthz` returns 200. Returns 0 on all-pass; returns 1 on any-fail (caller
         decides whether to exit 0 with skip annotation OR exit 1 with FAIL).

    7. **internal/verify-bridge-e2e/00-happy-path.sh** (NEW) — 5-iteration happy-path scenario. Top
       of file: `# verify-bridge-e2e/00-happy-path.sh — Phase 14 SC-1 + SC-3:` followed by a
       3-line comment explaining what it does. `set -euo pipefail` per the
       verify-bridge-no-token-leak.sh pattern. Source `_lib.sh`. Call `preflight`; if it returns 1,
       print a `skipped — <reason>` annotation and exit 0 (per D-10 + Pitfall 3). Embed the test
       `*.tf` as a heredoc into `/tmp/00-happy-path.tf` with variables `bridge_url` and
       `bridge_token`. The `.tf` declares a `homeassistant_addon.test` resource with
       `slug = "local_test-addon"`, `start = true`, minimal `options = { log_level = "info" }`, and
       EXPLICITLY does NOT set `lifecycle.prevent_destroy = true` (per Pitfall 7 — that lifecycle
       guard would block the destroy step of the 5-iteration loop). Loop `ITER in 1 2 3 4 5`:
       `cd` to a per-iteration work dir under `/tmp/00-happy-path.iter${ITER}/`, copy the `.tf` in,
       `tofu init -upgrade` (idempotent), `tofu plan -out=tfplan` and capture plan output to
       `${TESTDATA_DIR}/apply-output/${ITER}.txt` (via `tee`), `tofu apply -auto-approve tfplan`
       with stdout/stderr captured to the same file. After iteration: if `ITER > 1` AND the captured
       output contains a `+` or `-` line AND does NOT contain `No changes`, exit 1 with the failing
       diff. End: print green `00-happy-path: PASS — 5 iterations, iterations 2-5 reported 'No
       changes'`. Call `cleanup_scenario_baks` at the end only when no destructive scenario ran
       (this is Plan 01; Plan 02's scenarios own their own cleanup).

    8. **terraform-bridge/internal/testdata/{diagnostics,apply-output,state-fingerprints}/.gitkeep**
       (NEW ×3) — Empty files (zero bytes, no trailing newline needed) marking these three
       subdirectories as git-tracked. Without `.gitkeep`, `git` does not commit empty directories, and
       Plan 02's 12 scenarios' first action would be `mkdir -p` against an untracked path — which works
       locally but breaks when scenarios are checked out fresh in a CI environment. Comment on each
       (or in a sibling README inside the testdata directory) explaining the directory's purpose: 12
       diagnostic captures + 5 apply-output captures + N state-fingerprint pairs land here across
       Plans 02 + 03.

    Per D-15, the markdownlint + prettier pipeline MUST stay green on `tools/test-addon/README.md`.
    Wrap long lines at 120 chars; use ATX headers (`#` / `##`) per `.markdownlint.json`. The `*.sh`
    files MUST pass shellcheck with the repo's exemptions (SC1091 + SC2034 ignored per AGENTS.md
    Gotcha #4 + `.pre-commit-config.yaml`). The `*.yaml` files MUST pass yamllint with the relaxed
    profile (120-char lines, 2-space indent, quoted strings for versions).

    Honour the live-system rule (AGENTS.md §🔌 + D-02): this task writes files only — it does NOT
    invoke `docker build`, `ssh`, `tofu apply`, or any host-mutating command. The operator runs the
    scenario against the live host AFTER the test add-on's first `ha addons rebuild local_test-addon`.
    The executor's verification step (Task 2) executes only static checks (shellcheck, yamllint,
    markdownlint, validate-versions, validate-addon-config, validate-dockerfile-args).
  </action>
  <acceptance_criteria>
    - tools/test-addon/config.yaml declares: name "Test Add-on", slug "local_test-addon", version "0.1.0-0", arch amd64,
      options block with log_level (default "info") and dummy_setting (default "default"), schema block with both
      options declared as `str`, init false, startup application, boot manual, host_network false, hassio_api false;
      NO map: block (verify: grep -E '^slug:|^version:|^name:|log_level|dummy_setting|^  - amd64|^  str$|hassio_api'
      tools/test-addon/config.yaml produces all expected matches, and ! grep -E '^map:' tools/test-addon/config.yaml)
    - tools/test-addon/build.yaml declares: build_from.amd64 = "ghcr.io/home-assistant/amd64-base:3.24", labels
      block with org.opencontainers.image.* fields, args.VERSION = "0.1.0" (no subpatch; matches config.yaml's
      base per the 3-file scheme) (verify: grep -E 'BUILD_FROM.*3\.24|VERSION.*0\.1\.0$|org\.opencontainers\.image\.'
      tools/test-addon/build.yaml produces all expected matches)
    - tools/test-addon/Dockerfile declares ARG BUILD_FROM BEFORE the FROM instruction (passes
      validate-dockerfile-args.sh), uses ${BUILD_FROM} in the FROM line, COPY run.sh /run.sh, chmod a+x, CMD
      ["/run.sh"] (verify: shellcheck -s bash -e SC1091,SC2034 tools/test-addon/run.sh exits 0;
      validate-dockerfile-args.sh tools/test-addon/Dockerfile exits 0)
    - tools/test-addon/run.sh: bashio setup at top, one bashio::log.info line, sleep infinity at the end, comment
      header explaining purpose (verify: grep -E 'bashio::log.info|sleep infinity' tools/test-addon/run.sh matches
      both; head -1 tools/test-addon/run.sh is a comment line)
    - tools/test-addon/README.md: includes the `version-v0.1.0` badge URL, header structure H1 + H2, max line
      length 120 chars (verify: grep -E 'version-v0\.1\.0' tools/test-addon/README.md matches; awk 'length > 120'
      tools/test-addon/README.md exits 0 with no output; markdownlint-cli2 tools/test-addon/README.md exits 0)
    - internal/verify-bridge-e2e/_lib.sh exports BRIDGE_HOST (default ha-nextgen.akentner.ts.net), BRIDGE_PORT
      (default 8124), BRIDGE_URL, BRIDGE_TOKEN (read from /usr/share/hassio/addons/data/terraform-bridge/initial-token
      on the host), TEST_ADDON_SLUG (local_test-addon), REPO_ROOT, TESTDATA_DIR, and provides snapshot_state,
      fingerprint_state, cleanup_scenario_baks, preflight, red, green, yellow functions (verify: shellcheck -s bash
      -e SC1091,SC2034 internal/verify-bridge-e2e/_lib.sh exits 0; grep -c -E
      'BRIDGE_HOST|BRIDGE_PORT|BRIDGE_URL|BRIDGE_TOKEN|TEST_ADDON_SLUG|REPO_ROOT|TESTDATA_DIR|snapshot_state|fingerprint_state|cleanup_scenario_baks|preflight'
      internal/verify-bridge-e2e/_lib.sh >= 10)
    - internal/verify-bridge-e2e/00-happy-path.sh sources _lib.sh, calls preflight (exits 0 with `skipped`
      annotation if any preflight check fails per D-10), embeds a `*.tf` file with the homeassistant_addon.test
      resource (slug=local_test-addon, start=true, options={log_level:info}), EXPLICITLY does NOT set
      lifecycle.prevent_destroy = true (per Pitfall 7), loops 5 iterations of `tofu init && tofu plan && tofu apply`,
      writes per-iteration output to ${TESTDATA_DIR}/apply-output/${ITER}.txt, asserts "No changes" on iterations
      2..5 (verify: shellcheck -s bash -e SC1091,SC2034 internal/verify-bridge-e2e/00-happy-path.sh exits 0; grep -E
      'source.*_lib\.sh|skipped|No changes|local_test-addon' internal/verify-bridge-e2e/00-happy-path.sh produces
      all expected matches, and ! grep 'prevent_destroy' internal/verify-bridge-e2e/00-happy-path.sh has zero
      matches in active code — `grep -v '^#' | grep -c prevent_destroy` returns 0)
    - terraform-bridge/internal/testdata/{diagnostics,apply-output,state-fingerprints}/.gitkeep: each file exists
      (zero bytes), and each parent directory is git-tracked (verify: ls -la
      terraform-bridge/internal/testdata/diagnostics/.gitkeep terraform-bridge/internal/testdata/apply-output/.gitkeep
      terraform-bridge/internal/testdata/state-fingerprints/.gitkeep shows all three files exist)
    - internal/validate-versions.sh exits 0 (no version drift) — specifically: tools/test-addon/config.yaml's
      `0.1.0-0` agrees with build.yaml's `0.1.0` and README.md's `v0.1.0` badge (verify: bash
      internal/validate-versions.sh exits 0; bash internal/validate-versions.sh | grep -c tools/test-addon matches 3)
    - internal/validate-addon-config.py exits 0 for tools/test-addon/ (config.yaml passes schema validation per
      the auto-discovered filter at line 105) (verify: python3 internal/validate-addon-config.py
      tools/test-addon/ exits 0)
    - internal/validate-dockerfile-args.sh exits 0 for tools/test-addon/Dockerfile (verify: bash
      internal/validate-dockerfile-args.sh tools/test-addon/Dockerfile exits 0)
  </acceptance_criteria>
  <verify>
    <automated>bash internal/validate-versions.sh && python3 internal/validate-addon-config.py tools/test-addon/ && bash internal/validate-dockerfile-args.sh tools/test-addon/Dockerfile && shellcheck -s bash -e SC1091,SC2034 internal/verify-bridge-e2e/_lib.sh internal/verify-bridge-e2e/00-happy-path.sh tools/test-addon/run.sh && yamllint -d relaxed tools/test-addon/config.yaml tools/test-addon/build.yaml && markdownlint-cli2 tools/test-addon/README.md</automated>
    <fails_when>validate-versions.sh exits non-zero (3-file scheme drift), validate-addon-config.py reports a missing/required-field error for tools/test-addon/config.yaml, validate-dockerfile-args.sh reports an undeclared ARG in tools/test-addon/Dockerfile, shellcheck reports any warning (SC1091 + SC2034 suppressed), yamllint reports any error against the relaxed profile, markdownlint-cli2 reports MD013 (line length) or any other violation against tools/test-addon/README.md</fails_when>
  </verify>
  <done>
    The new `tools/test-addon/` directory is a valid 4-file HA add-on that the host Supervisor can install via the
    local-build path; its 3-file versioning scheme is consistent; all YAML/markdown/shell lint passes. The new
    `internal/verify-bridge-e2e/` directory contains `_lib.sh` + `00-happy-path.sh` as the verify suite's
    foundation. The new `terraform-bridge/internal/testdata/` subdirectories are git-tracked with `.gitkeep`
    placeholders. The executor (and any developer on any workstation) can invoke
    `bash internal/verify-bridge-e2e/00-happy-path.sh` and observe one of two outcomes: pre-flight passes and
    the script enters the 5-iteration loop against the live Bridge, OR pre-flight fails and the script exits 0
    with a `skipped — <reason>` annotation. Both outcomes are valid for this plan — neither requires a live Bridge
    at plan-execution time.
  </done>
</task>

<task type="auto">
  <name>Task 2: run the pre-commit pipeline on all touched files and verify the validate-versions cross-artifact sync (Bridge + Provider unchanged) is unaffected</name>
  <files>
    - internal/verify-bridge-e2e/_lib.sh
    - internal/verify-bridge-e2e/00-happy-path.sh
    - tools/test-addon/config.yaml
    - tools/test-addon/build.yaml
    - tools/test-addon/Dockerfile
    - tools/test-addon/run.sh
    - tools/test-addon/README.md
    - terraform-bridge/internal/testdata/diagnostics/.gitkeep
    - terraform-bridge/internal/testdata/apply-output/.gitkeep
    - terraform-bridge/internal/testdata/state-fingerprints/.gitkeep
  </files>
  <read_first>
    - .pre-commit-config.yaml (the pre-commit pipeline definition; every hook this task runs is wired here)
    - AGENTS.md §"Code Quality & Validation" (the `make lint` / `pre-commit run` workflow + the list of
      linters that fire)
    - internal/validate-versions.sh (the cross-artifact Bridge/Provider sync check at lines 109-124;
      Task 2 confirms Plan 01's tools/test-addon/ does not perturb this check)
  </read_first>
  <action>
    Run the pre-commit pipeline scoped to the files Task 1 created. The pipeline enforces: yamllint on the new
    YAML files, shellcheck on the new shell scripts, markdownlint-cli2 + prettier on the new README.md, and
    `internal/validate-versions.sh` (which auto-discovers `tools/test-addon/` and validates its 3-file
    versioning). DO NOT run `pre-commit run --all-files` — that would touch unrelated files on the working
    tree (e.g. the mod-staged `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md` would fail unrelated
    markdown lint). Use `pre-commit run --files <explicit list>` to scope to Task 1's touched files only.

    The list of files to pass `--files`:
    - `tools/test-addon/config.yaml`
    - `tools/test-addon/build.yaml`
    - `tools/test-addon/Dockerfile`
    - `tools/test-addon/run.sh`
    - `tools/test-addon/README.md`
    - `internal/verify-bridge-e2e/_lib.sh`
    - `internal/verify-bridge-e2e/00-happy-path.sh`

    If any pre-commit hook fails, FIX the underlying issue (NOT by adding `# noqa` comments) and re-run.
    Common failures and their fixes:
    - `validate-versions.sh`: the README.md badge version disagrees with config.yaml / build.yaml — fix the
      badge URL.
    - `validate-addon-config.py`: a required field missing in tools/test-addon/config.yaml (likely
      `description` — `name` and `version` and `slug` are caught by `validate-versions.sh`).
    - `markdownlint-cli2` MD013: a line in tools/test-addon/README.md exceeds 120 chars — wrap or
      reformat.
    - `shellcheck` (SC2086 etc.): an unquoted variable expansion in the scenario script — quote it.
    - `yamllint`: a line in config.yaml exceeds 120 chars or has inconsistent indentation — fix.

    Separately verify that the cross-artifact Bridge + Provider version sync (TOFU-05,
    `internal/validate-versions.sh` lines 109-124) is unaffected by this plan: the Bridge version in
    `terraform-bridge/build.yaml` and the Provider version in `terraform-provider-homeassistant/build.yaml`
    still match (Plan 14 does NOT bump versions — CF-11). Verify by running
    `bash internal/validate-versions.sh` and confirming the cross-artifact check at the bottom of the
    output reports no drift.

    Do NOT modify any file other than the ones Task 1 created. If a pre-commit hook reports a failure on
    an unrelated file (e.g. `info_hub/README.md` has a markdownlint failure per the existing working
    tree state), note it in the SUMMARY but do NOT attempt to fix it — that file is out of scope.

    The AGENTS.md "ask first, act later" rule applies to version bumps + deployment changes. Plan 14 does
    NOT bump versions (CF-11) and does NOT change any deployment (no Bridge restart, no Supervisor
    change, no HA backup operation). Task 2's read-only checks are within the executor's authority.
  </action>
  <acceptance_criteria>
    - `pre-commit run --files tools/test-addon/config.yaml tools/test-addon/build.yaml tools/test-addon/Dockerfile
      tools/test-addon/run.sh tools/test-addon/README.md internal/verify-bridge-e2e/_lib.sh
      internal/verify-bridge-e2e/00-happy-path.sh` exits 0 (all hooks pass on Task 1's touched files)
      (verify: pre-commit run --files tools/test-addon/config.yaml tools/test-addon/build.yaml
      tools/test-addon/Dockerfile tools/test-addon/run.sh tools/test-addon/README.md
      internal/verify-bridge-e2e/_lib.sh internal/verify-bridge-e2e/00-happy-path.sh exits 0)
    - `bash internal/validate-versions.sh` exits 0 AND its cross-artifact Bridge/Provider sync report
      shows both versions still match (Plan 14 does NOT bump versions per CF-11)
      (verify: bash internal/validate-versions.sh exits 0; bash internal/validate-versions.sh | grep
      'TOFU-05' returns empty output — no drift reported)
    - No file outside Task 1's `files_modified` list is modified by this task — Plan 14's surface is
      additive only (the existing Bridge config.yaml, DOCS.md, internal/{supervisor,auth,mutex,nonce,state}
      are not touched; the Provider is not touched)
      (verify: git status --short | grep -v -E '^\?\? tools/test-addon/|^\?\?
      internal/verify-bridge-e2e/|^\?\? terraform-bridge/internal/testdata/' returns empty output after
      Task 1 + Task 2 complete)
  </acceptance_criteria>
  <verify>
    <automated>pre-commit run --files tools/test-addon/config.yaml tools/test-addon/build.yaml tools/test-addon/Dockerfile tools/test-addon/run.sh tools/test-addon/README.md internal/verify-bridge-e2e/_lib.sh internal/verify-bridge-e2e/00-happy-path.sh && bash internal/validate-versions.sh</automated>
    <fails_when>any pre-commit hook exits non-zero on a Task 1 file (yamllint violation, shellcheck warning, markdownlint MD013 line-length, prettier reformat, validate-versions.sh drift, validate-addon-config.py missing required field) OR validate-versions.sh's TOFU-05 cross-artifact check reports Bridge/Provider drift</fails_when>
  </verify>
  <done>
    Every pre-commit hook that touches Plan 14's files passes. The Bridge + Provider cross-artifact version sync
    is unchanged (Plan 14 does not bump versions — CF-11). The repo's static quality gates are clean on the
    new verification infrastructure.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary                       | Description                                                                                       |
| ------------------------------ | ------------------------------------------------------------------------------------------------- |
| Operator workstation → host    | The verify scenario script runs on the operator's workstation (where `tofu` is installed). It     |
|                                | reaches the Bridge via HTTPS on port 8124 from the Tailscale hostname `ha-nextgen.akentner.ts.net` |
|                                | (or LAN IP `192.168.178.3` when the operator overrides BRIDGE_HOST). All Bearer-authenticated     |
|                                | traffic carries the plaintext bearer token read from the host's `/data/initial-token`.            |
| Operator workstation → host FS | `_lib.sh::snapshot_state` and `cleanup_scenario_baks` reach the host filesystem via SSH to        |
|                                | `cp /data/terraform.tfstate` (D-16) and `find /data -name '*.tfstate.bak.*' -delete` (D-18).     |
|                                | These write inside the Bridge add-on's `addon_config:rw` mounted `/data` directory.              |
| Operator workstation → Bridge  | Every curl in the scenario carries `Authorization: Bearer $BRIDGE_TOKEN`. The Bearer is NEVER     |
|                                | written to any file by the scenario; it lives only in the operator's shell env or the SSH        |
|                                | stdout of the `cat /data/initial-token` invocation.                                              |

## STRIDE Threat Register

| Threat ID    | Category             | Component                          | Severity | Disposition | Mitigation Plan                                                                                                                                  |
| ------------ | -------------------- | ---------------------------------- | -------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-14-01      | Information Disclosure | Bearer token in `/tmp/*.tf`       | medium   | mitigate    | The embedded `*.tf` writes to `/tmp/00-happy-path.tf` (created by the scenario script). The file contains `variable "bridge_token" { type = string, sensitive = true }` — the value is passed via `-var bridge_token=$BRIDGE_TOKEN` at `tofu apply` time, NEVER written to disk by the script. The `/tmp/*.tf` file is in the operator's ephemeral `/tmp` (not HA backup coverage) and is removed at script exit (trap EXIT). Verify command for the executor: `bash internal/verify-bridge-e2e/00-happy-path.sh` on a developer workstation WITHOUT live Bridge → preflight fails → script exits 0 with `skipped` annotation → `/tmp/*.tf` is never created. |
| T-14-02      | Information Disclosure | Bearer token via Bridge logs      | low      | accept      | Phase 10's AUTH-05 invariant (two-layer masking: slog Handler scrubber + chi RequestLogger middleware stripping Authorization from r.Header.Clone()) is already in place. Plan 14 does NOT modify Bridge code. Acceptance: phase 10's verify-bridge-no-token-leak.sh still passes (verify: bash internal/verify-bridge-no-token-leak.sh exits 0). |
| T-14-03      | Tampering             | State snapshot overwrite           | medium   | mitigate    | D-16's per-scenario snapshot writes to `/data/terraform.tfstate.bak.<scenario>` BEFORE any destructive scenario; recovery via `mv`. D-17's fingerprint via `GET /v1/state/index` before + after gives the operator a "did the file change?" check. Acceptance: the snapshot is taken unconditionally at scenario start regardless of preflight outcome (the snapshot command runs locally via SSH and does not require the Bridge to be reachable). |
| T-14-04      | Denial of Service    | Pre-flight blocks Bridge access    | low      | accept      | The pre-flight assertion (`command -v tofu` + Provider binary + `/healthz` 200) is a no-op when prerequisites are missing — script exits 0 with `skipped` annotation. No automated Bridge restart is attempted. Per AGENTS.md §🔌 live-system rule + D-02, Bridge restarts are operator-initiated. |
| T-14-SC      | Tampering             | Package legitimacy of any new npm/pip/cargo install | high | mitigate | Plan 14 installs ZERO new external packages (per the Phase 14 RESEARCH §"Package Legitimacy Audit"). The test add-on consumes `ghcr.io/home-assistant/amd64-base:3.24` (the official HA base — already used by terraform-bridge + phone-logger). No SLOP/SUS packages to audit. |

</threat_model>

<verification>
For overall Phase 14 Plan 01 success (not just this plan's surface):

1. **SC-1 foundation** (covered by this plan; verified by the operator at runtime): `make install-provider`
   installs the built Provider binary; `tofu init/plan/apply` against the live Bridge installs a test add-on
   end-to-end. Plan 01's `00-happy-path.sh` IS the runtime verifier for SC-1 — it embeds the same `*.tf`
   file the operator would write by hand, exercises install + start + options + stop + uninstall, and
   captures `tofu apply` output for post-mortem review.

2. **SC-3 idempotency foundation** (covered by this plan; verified at runtime): the 5-iteration loop in
   `00-happy-path.sh` asserts "No changes" on iterations 2..5. The operator runs the scenario once after
   `make install-provider` to confirm SC-3.

3. **SC-2 drift behavior** (covered by this plan's foundation; the full empirical exercise happens at
   operator-runtime): the `00-happy-path.sh` iteration 1 captures the canonical `tofu apply` output for the
   options-change → Update path. Plan 02's scenarios 04 (prevented_destroy) and 11 (pwned) cover the
   remaining SC-2 surface.

4. **SC-4 error-code coverage** (NOT covered by this plan; Plan 02 owns): the 12 scenarios in Plan 02
   capture every error_code in `terraform-bridge/internal/supervisor/client.go:661-680` MapError +
   Provider-side handshake failures.

5. **SC-5 operator documentation** (NOT covered by this plan; Plan 03 owns): README.md + DOCS.md
   expansion + cleanup script.
</verification>

<success_criteria>
- `tools/test-addon/` is a valid 4-file HA add-on (config.yaml + build.yaml + Dockerfile + run.sh + README.md)
  that passes yamllint + shellcheck + markdownlint + the 3-file versioning hook + validate-addon-config +
  validate-dockerfile-args. The slug is `local_test-addon`; versions are `0.1.0-0` / `0.1.0` / `v0.1.0`.
- `internal/verify-bridge-e2e/_lib.sh` exports the constants and functions described in the artifacts
  section and passes shellcheck with SC1091/SC2034 suppressed.
- `internal/verify-bridge-e2e/00-happy-path.sh` passes shellcheck, embeds the canonical happy-path `*.tf`
  (without `lifecycle.prevent_destroy = true` per Pitfall 7), and exercises the 5-iteration idempotency
  loop with `No changes` assertions.
- `terraform-bridge/internal/testdata/{diagnostics,apply-output,state-fingerprints}/.gitkeep` files
  exist and mark the three subdirectories as git-tracked for Plan 02 + Plan 03 use.
- The pre-commit pipeline (scoped to Task 1's files) passes with zero warnings or errors.
- The Bridge + Provider cross-artifact version sync (TOFU-05) is unchanged from before this plan
  (Plan 14 does not bump versions).
</success_criteria>

<output>
Create two SUMMARY.md files when complete:
- `.planning/phases/14-real-ha-end-to-end-verification-operator-documentation/14-01-SUMMARY.md` —
  the standard atomic-commit summary for this plan
- (No verification script summary needed — `00-happy-path.sh` IS the verification)
</output>
