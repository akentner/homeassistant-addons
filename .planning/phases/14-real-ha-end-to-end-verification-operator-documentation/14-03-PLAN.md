---
phase: 14-real-ha-end-to-end-verification-operator-documentation
plan: 03
type: execute
wave: 3
depends_on:
  - 14-02-PLAN.md
files_modified:
  - terraform-bridge/README.md
  - terraform-bridge/DOCS.md
  - internal/verify-bridge-e2e/99-cleanup.sh
autonomous: true
requirements:
  - OPS-04

estimate:
  tokens: 70000
  raw_tokens: 35000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "`terraform-bridge/README.md` is the new 1-page HA add-on store listing per D-12: it covers the
      install triad (HA add-on store → repository → install), the token retrieval triad (find the
      Bearer file at `/data/initial-token`, copy it, delete the file after Provider is configured),
      the Tailscale bind-gate note (plain HTTP on port 8124; network-layer ACL is the security
      boundary during Phase 1), and a link to `DOCS.md` for the full operator reference. README.md
      has NO error-code remediation content — that lives exclusively in DOCS.md per D-14"
    - "`terraform-bridge/DOCS.md` is the expanded full operator reference per D-13 with the 9
      required sections in order: (1) Options (`bind_address`, `bind_allowed_subnets`,
      `critical_addons`, `install_job_timeout_seconds`, `try_lock_timeout_seconds` — pulled from
      the current `terraform-bridge/config.yaml` schema), (2) Token issuance (extends the existing
      text to note `/data/initial-token` is the canonical source), (3) Token rotation (extends with
      the empirical rotation round-trip from Phase 14's verify run), (4) Token recovery (kept
      verbatim from the existing skeleton), (5) Endpoints reference (every `/v1/*` route with method
      + auth + request shape + response shape — new section), (6) Troubleshooting (1-paragraph
      pointer to Provider DOCS.md#troubleshooting table per D-14, NO duplication), (7) Observed
      issues (3+ entries from real Phase-14 verify run), (8) State management (`/v1/state/index`,
      STATE-02 coverage, HA backup integration), (9) HA backup integration (explicit note that
      `addon_config:rw` mount contents including `terraform.tfstate`, `bridge-token`,
      `bridge-nonce-audit.json` are auto-included in `ha backups new --app terraform-bridge` per
      CF-13 + Phase 9 §pending-spike §10 result)"
    - "`internal/verify-bridge-e2e/99-cleanup.sh` is the separate-manual-invocation cleanup scenario
      per D-18: it uninstalls the test add-on (`tofu destroy -auto-approve` against
      `local_test-addon`), LEAVES `bridge-nonce-audit.json` in place (append-only forensics per
      Phase 12 D-06), removes `*.tfstate.bak.<scenario>` files older than 7 days (`find /data -maxdepth
      1 -name 'terraform.tfstate.bak.*' -mtime +7 -delete`), and prints a green PASS line. The
      cleanup script's header explicitly documents SEPARATE MANUAL INVOCATION per the agent's
      Discretion and marks the script as destructive so the operator does not invoke it as part
      of the verify suite"
    - "Both README.md and DOCS.md pass markdownlint-cli2 + prettier (D-15 + AGENTS.md §🛠️). Max line
      length 120 chars per `.markdownlint.json` MD013. ATX headers per `.markdownlint.json` MD003.
      The expanded DOCS.md uses the `markdownlint-cli2 --fix` autofix for any wrapping issues
      discovered during pre-commit"
    - "Every locked decision from 14-CONTEXT.md (D-12, D-13 §1..§9, D-14, D-15, D-18) is honored
      verbatim. Provider DOCS.md remains the canonical source for per-error_code Summary text per
      D-14; Bridge DOCS.md's Troubleshooting section is a 1-paragraph pointer + the cross-link
      anchor table"
  artifacts:
    - path: "terraform-bridge/README.md"
      provides: "1-page HA add-on store listing. Sections: (1) About (3-4 sentences: what the
        add-on does, plain HTTP, Tailscale-filtered), (2) Features (4-5 bullets: bearer auth,
        version handshake, per-slug mutex, async job polling, typed diagnostics), (3) Install (3
        numbered steps: Settings → Add-ons → Add-on Store → akentner/homeassistant-addons →
        Install), (4) First-time setup (3 numbered steps: find the Bearer at
        `/data/initial-token`, copy it into the Provider's `bearer_token`, delete the file), (5)
        Configuration pointer to DOCS.md, (6) Repository link. Total: ≤80 lines. Badge URL includes
        `version-v0.2.0` (the current Bridge version per terraform-bridge/build.yaml) per the
        3-file scheme enforced by internal/validate-versions.sh"
      contains: "version-v0\\.2\\.0|/data/initial-token|DOCS\\.md"
    - path: "terraform-bridge/DOCS.md"
      provides: "Full operator reference per D-13. Sections in order: # Configuration header +
        intro paragraph; ## Options (table with the 5 current options + their defaults + per-option
        prose); ## Token issuance (extends the existing Token issuance section with the note that
        `/data/initial-token` is the canonical operator-side source); ## Token rotation (extends
        the existing Token rotation section with the empirical rotation round-trip prose from the
        Phase 14 verify run); ## Token recovery (kept verbatim); ## Endpoints reference (NEW —
        table with every `/v1/*` route: method, auth requirement, request shape, response shape —
        pulled from `terraform-bridge/contract/types.go`); ## Troubleshooting (NEW — 1-paragraph pointer to
        Provider DOCS.md#troubleshooting table per D-14 + cross-link anchor table that lists every
        error_code with its kebab-case anchor); ## Observed issues (NEW — 3+ entries from real
        Phase-14 verify run; placeholder format during plan-execution is
        `[observed-issue-N]` and the executor fills them in from the verify-run transcript);
        ## State management (NEW — `/v1/state/index`, STATE-02 coverage explanation); ## HA backup
        integration (NEW — explicit note that `addon_config:rw` mount contents are auto-included
        in `ha backups new --app terraform-bridge` per CF-13). Total: ≤600 lines"
      contains: "## Options|## Token issuance|## Token rotation|## Token recovery|## Endpoints reference|## Troubleshooting|## Observed issues|## State management|## HA backup integration|troubleshooting-unauthorized|troubleshooting-critical-addon-protected|troubleshooting-pwned|terraform.tfstate"
    - path: "internal/verify-bridge-e2e/99-cleanup.sh"
      provides: "Separate manual cleanup scenario per D-18 + the agent's Discretion. Sources
        _lib.sh. Header documents SEPARATE MANUAL INVOCATION per the agent's Discretion and
        marks the script as destructive. It uninstalls the test add-on, removes old state
        backups, and prints PASS. Run after all verify scenarios complete. Do NOT include this
        script in the verify suite. Body: (a) snapshot_state 99-cleanup; (b) tofu destroy
        -auto-approve against local_test-addon (or ha addons uninstall local_test-addon if
        Provider is not available); (c) print confirmation that bridge-nonce-audit.json is left
        in place; (d) call _lib.sh::cleanup_scenario_baks to remove *.tfstate.bak.* files older
        than 7 days; (e) print green PASS line. Exits 0 on success. Pre-flight skips with
        annotation when prerequisites are missing (per D-10)"
      contains: "cleanup_scenario_baks|separate manual invocation|bridge-nonce-audit.json|local_test-addon"
  key_links:
    - from: "terraform-bridge/DOCS.md § Troubleshooting"
      to: "terraform-provider-homeassistant/DOCS.md § Troubleshooting"
      via: "Bridge DOCS.md's Troubleshooting section is a 1-paragraph pointer per D-14. The
        per-error_code anchor table in Bridge DOCS.md cross-links to
        Provider DOCS.md#troubleshooting-<kebab-error_code>. NO duplication of the Summary text —
        the Provider DOCS.md is the canonical source per D-14"
      pattern: "DOCS\\.md#troubleshooting-[a-z-]+"
    - from: "terraform-bridge/DOCS.md § Endpoints reference"
      to: "terraform-bridge/contract/types.go"
      via: "Every endpoint's response shape comes from the contract types directly: AddOnInfo,
        VersionHandshake, ErrorResponse, HealthResponse, BridgeInfo, NonceResponse,
        StateIndexResponse, RotateResponse, TokenResponse. The DOCS.md section quotes the struct
        field names verbatim — no paraphrasing"
      pattern: "AddOnInfo|VersionHandshake|ErrorResponse|HealthResponse|BridgeInfo"
    - from: "terraform-bridge/DOCS.md § HA backup integration"
      to: "Phase 9 §10 spike transcript"
      via: "The HA backup integration section explicitly cites Phase 9 §10 spike result:
        `addon_config:rw` mount contents (including `terraform.tfstate`, `bridge-token`,
        `bridge-nonce-audit.json`) are auto-included in `ha backups new --app terraform-bridge`
        per CF-13"
      pattern: "addon_config:rw|terraform.tfstate|bridge-nonce-audit.json"
    - from: "internal/verify-bridge-e2e/99-cleanup.sh"
      to: "internal/verify-bridge-e2e/_lib.sh"
      via: "Cleanup scenario sources _lib.sh for snapshot_state, cleanup_scenario_baks,
        BRIDGE_URL, BRIDGE_TOKEN, TEST_ADDON_SLUG. The script's body makes one `tofu destroy`
        call (or `ha addons uninstall` fallback when Provider is unavailable) and one
        `_lib.sh::cleanup_scenario_baks` call"
      pattern: "source.*_lib\\.sh|cleanup_scenario_baks"
    - from: "terraform-bridge/README.md"
      to: "terraform-bridge/build.yaml"
      via: "README.md badge URL contains `version-vX.Y.Z` per the 3-file scheme enforced by
        internal/validate-versions.sh. The badge URL must match the current Bridge version
        (0.2.0 per build.yaml); changing one without the other fails pre-commit"
      pattern: "version-v0\\.2\\.0"

---

<objective>
Land Phase 14's documentation surface: the Bridge's HA add-on store listing (`README.md`) per D-12 +
the full operator reference (`DOCS.md`) per D-13 + the separate-manual cleanup scenario
(`99-cleanup.sh`) per D-18. The DOCS.md expansion is the SC-5 success-criterion gate — every error
code has a documented remediation (via cross-link to the Provider DOCS.md per D-14), three+
observed issues appear from the empirical verify run, and an end-to-end operator walkthrough
(install → token → tofu init/plan/apply) is present.

Purpose: closing the SC-5 success criterion ("Bridge README.md and DOCS.md are complete: install
steps via HA add-on store, token issuance + rotation procedure, OpenTofu provider install command,
an example `*.tf` file covering every resource attribute, every error code with documented
remediation, and a troubleshooting section with at least three real observed issues"). The DOCS.md
is the durable operator-facing artifact — when an operator hits a `tofu apply` failure, the IDE/CLI
renders the typed Diagnostic's Link field as a clickable anchor pointing into Provider
DOCS.md#troubleshooting-<code>; Bridge DOCS.md's Troubleshooting section is the operator-side
companion that explains the architecture + lists the cross-link anchors.

Output: 1 new file (`internal/verify-bridge-e2e/99-cleanup.sh`) + 2 modified files
(`terraform-bridge/README.md` rewritten from 43 lines to ≤80 lines per D-12, `terraform-bridge/DOCS.md`
expanded from 131 lines to ≤600 lines per D-13 with 9 required sections). Zero changes to Bridge or
Provider source.

Requirements: OPS-04. The full requirement surface ("install steps via HA add-on store, token
issuance + rotation procedure, OpenTofu provider install command, an example `*.tf` file covering
every resource attribute, every error code with documented remediation, and a troubleshooting
section with at least three real observed issues") is satisfied by this plan's README + DOCS +
cleanup-script deliverables.
</objective>

<execution_context>
@/data/.config/opencode/gsd-core/workflows/execute-plan.md
@/data/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md (Phase 14 entry, lines 280-308 — SC-5 is this plan's surface; lines 304-306
quote the exact SC-5 requirements this plan closes)
@.planning/REQUIREMENTS.md (OPS-04 line 142-144 — the single Phase-14 requirement; the source text
DOCS.md's section structure must cover)
@.planning/phases/14-real-ha-end-to-end-verification-operator-documentation/14-CONTEXT.md (D-12
README 1-pager structure, D-13 DOCS.md 9 sections in order, D-14 Provider DOCS.md is canonical for
per-error_code text — no duplication, D-15 markdownlint + prettier pre-commit pass, D-18 cleanup
script + 7-day .bak retention, CF-08 Provider diagnostics contract, CF-13 HA backup integration,
CF-14 live-system rule)
@.planning/phases/14-real-ha-end-to-end-verification-operator-documentation/14-RESEARCH.md (Pattern 3
§"Cross-link DOCS.md anchor", State of the Art table — "Documentation written from captured
empirical diagnostics" replaces "Documentation written from unit-test expectations", Open Questions
#1 pwned + #2 5-iteration output wording + #3 prevent_destroy adoption interaction)
@.planning/phases/14-real-ha-end-to-end-verification-operator-documentation/14-01-PLAN.md (Plan 01's
test add-on + _lib.sh + 00-happy-path.sh — Plan 03's DOCS.md references the verify suite by name)
@.planning/phases/14-real-ha-end-to-end-verification-operator-documentation/14-02-PLAN.md (Plan 02's
12 error-code scenarios — Plan 03's DOCS.md Observed issues section reads from the captured
transcripts this plan produces)
@internal/verify-bridge-e2e/_lib.sh (Plan 01's helper — 99-cleanup.sh sources it for snapshot_state
+ cleanup_scenario_baks + BRIDGE_URL + BRIDGE_TOKEN)
@terraform-bridge/README.md (current 43-line placeholder — Plan 03 REPLACES this with the D-12
1-pager)
@terraform-bridge/DOCS.md (current 131-line skeleton — Plan 03 EXPANDS this to the D-13 full
reference)
@terraform-bridge/contract/types.go (every endpoint response shape — DOCS.md § Endpoints reference
section quotes the struct field names verbatim)
@terraform-bridge/config.yaml (every Options field — DOCS.md § Options section is generated from
this file's schema)
@terraform-provider-homeassistant/DOCS.md §"Troubleshooting" lines 402-618 (the Provider DOCS.md
section that Bridge DOCS.md cross-links to per D-14; the canonical source for per-error_code Summary
text)
@terraform-provider-homeassistant/internal/diagnostics/doc.go (DocAnchor helper at lines 129-135 —
the anchor convention Bridge DOCS.md's Troubleshooting section follows)
@.pre-commit-config.yaml (the pre-commit pipeline — markdownlint-cli2 + prettier + yamllint +
shellcheck + validate-versions hook all fire on the touched files)
@.markdownlint.json (MD013 max line length 120 + MD003 ATX headers — the markdown formatting rules
the README + DOCS must satisfy)
@AGENTS.md (Live Systems rule §🔌 — no unsolicited restarts; pre-commit pipeline §🛠️; Critical
Gotchas #1 — no manual version edits)
@spike-transcripts/pitfalls10-20260831T153403Z.log (the Phase 9 §10 empirical HA backup + addon_config
spike transcript — DOCS.md § HA backup integration section references this finding per CF-13)
</context>

<interfaces>
Locked contracts from earlier phases that this plan consumes (do NOT modify):

- **terraform-bridge/contract/types.go**: AddOnInfo, VersionHandshake, ErrorResponse, HealthResponse,
  BridgeInfo, NonceResponse, StateIndexResponse, RotateResponse, TokenResponse. DOCS.md § Endpoints
  reference quotes the struct field names verbatim.

- **terraform-bridge/config.yaml**: every Options field documented in DOCS.md § Options. The 5
  fields per CF-12 are `bind_address`, `bind_allowed_subnets`, `critical_addons`,
  `install_job_timeout_seconds`, `try_lock_timeout_seconds`. Defaults from config.yaml lines 23-30.

- **terraform-provider-homeassistant/internal/diagnostics/doc.go DocAnchor**: returns
  `DOCS.md#troubleshooting-<kebab-error_code>`. Bridge DOCS.md's Troubleshooting cross-link table
  uses this exact format for every error_code.

- **Plan 01's `_lib.sh`** + **Plan 02's 12 scenarios**: 99-cleanup.sh sources `_lib.sh` and does not
  redefine its constants. Plan 02's captured-diagnostic files (`terraform-bridge/internal/testdata/
  diagnostics/*.txt`) are the empirical source for the Observed issues section's content.

- **3-file versioning scheme** (AGENTS.md §🔑): README.md badge URL must contain
  `version-v0.2.0` matching `terraform-bridge/build.yaml`'s `VERSION: "0.2.0"`. Plan 03 does NOT
  bump versions.

Locked decisions from 14-CONTEXT.md that the executor MUST follow without re-deciding:

- **D-12** (README structure): install + token + Tailscale-bind-gate triad; NO error-code remediation.
- **D-13** (DOCS structure): 9 sections in order — Options, Token issuance, Token rotation, Token
  recovery, Endpoints reference, Troubleshooting (pointer only), Observed issues (3+ entries), State
  management, HA backup integration.
- **D-14** (cross-link, no duplication): Bridge DOCS.md's Troubleshooting section is a 1-paragraph
  pointer to Provider DOCS.md#troubleshooting. NO duplication of per-error_code Summary text.
- **D-15** (pre-commit pipeline): markdownlint + prettier MUST pass. MD013 line-length ≤120. ATX
  headers. Run `pre-commit run --files terraform-bridge/README.md terraform-bridge/DOCS.md
  internal/verify-bridge-e2e/99-cleanup.sh` before commit.
- **D-18** (cleanup script): separate manual invocation; uninstalls test add-on; LEAVES nonce journal
  in place; removes *.tfstate.bak.* older than 7 days.

Deferred to later plans (do NOT implement here):

- README.md + DOCS.md revisions for FUTURE Bridge API changes (e.g. when BRIDGE-04..09 land in a
  future Phase 2+ milestone, DOCS.md's Endpoints reference section expands). Plan 03 captures the
  current state as of Phase 14.
- The actual observed-issue text content (the executor fills in the placeholder entries during
  plan execution by reading the verify-run transcript — the executor is in the loop at runtime, NOT
  at plan-author time).
- Cross-link anchor table extensions for FUTURE error_codes (e.g. when Phase 2+ adds new typed
  errors, the table grows).

Plan 03 deliverable proof: the operator can install the Bridge via the HA add-on store, retrieve
the Bearer token, configure the Provider, run `tofu apply`, and on any error follow the
clickable diagnostic link from `tofu` output into the Provider DOCS.md#troubleshooting table, with
the Bridge DOCS.md as the architecture-and-endpoints reference alongside.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: write the new 1-page terraform-bridge/README.md per D-12 + the internal/verify-bridge-e2e/99-cleanup.sh cleanup script per D-18</name>
  <files>
    - terraform-bridge/README.md
    - internal/verify-bridge-e2e/99-cleanup.sh
  </files>
  <read_first>
    - phone-logger/README.md (the 1-page pattern Plan 03 mirrors — concise add-on store listing with
      badge + install steps + link to DOCS.md)
    - phone-logger/DOCS.md (the full operator reference pattern Plan 03 mirrors — phone-logger's
      DOCS.md is the canonical repo example of a comprehensive add-on reference)
    - terraform-bridge/build.yaml (current VERSION: "0.2.0" — the badge URL in the new README.md
      must reference this version per the 3-file scheme)
    - terraform-provider-homeassistant/DOCS.md §"Installation" lines 16-128 (the canonical install
      walkthrough — Plan 03's DOCS.md (Task 2) cross-references this section for the
      `make install-provider` walkthrough that lives in the Provider DOCS.md)
    - .planning/phases/14-real-ha-end-to-end-verification-operator-documentation/14-CONTEXT.md D-12 +
      D-14 + D-18 (locked decisions for this task)
    - .planning/phases/14-real-ha-end-to-end-verification-operator-documentation/14-RESEARCH.md
      State of the Art (Phase 14 is the FIRST time the Bridge has a complete operator reference)
    - internal/verify-bridge-e2e/_lib.sh (Plan 01's helper — 99-cleanup.sh sources this for
      snapshot_state + cleanup_scenario_baks + BRIDGE_URL + BRIDGE_TOKEN + TEST_ADDON_SLUG)
    - internal/verify-bridge-e2e/00-happy-path.sh (the structural template every scenario follows;
      99-cleanup.sh mirrors its set -euo pipefail + source _lib.sh + preflight pattern)
    - .markdownlint.json (MD013 max line length 120 + MD003 ATX headers — the rules the README.md
      must satisfy)
    - AGENTS.md §🔑 Critical Versioning Scheme (3-file rule: README.md badge URL must match
      config.yaml + build.yaml versions or pre-commit hook fails)
  </read_first>
  <action>
    This task writes the small, low-risk files first — the new 1-page README + the cleanup script —
    so the larger DOCS.md expansion in Task 2 can build on the README's structure. Both files
    follow their established patterns: README.md mirrors `phone-logger/README.md`'s concise
    add-on-store listing style; 99-cleanup.sh mirrors the other verify-scenario scripts' `set
    -euo pipefail` + source _lib.sh + preflight pattern.

    Run a single `pre-commit run --files` command after writing both files in this task to confirm
    markdownlint + prettier + shellcheck + validate-versions all pass on the touched files. The
    DOCS.md file (Task 2) is NOT in scope for this verification step — it has its own verify step.

    Honour D-15: the README.md must pass markdownlint + prettier. Wrap long lines at 120 chars;
    use ATX headers (`#` / `##`) per `.markdownlint.json`. The cleanup script must pass shellcheck
    with the repo's SC1091 + SC2034 exemptions.

    Per file:

    1. **terraform-bridge/README.md** (REPLACE the 43-line placeholder) — New 1-page HA add-on
       store listing per D-12. Total ≤80 lines. Structure:
       - Title `# Home Assistant Add-on: Terraform Bridge` (matched to existing line 1).
       - Badge block: `[![Release][release-shield]][release]` with `version-v0.2.0` URL (matching
         current build.yaml VERSION — preserves the 3-file scheme).
       - 1-paragraph "About" — what the add-on does in 3-4 sentences (Bearer-authenticated JSON
         proxy of Supervisor HTTP API, plain HTTP on port 8124, Tailscale-filtered).
       - 5-bullet "Features" list — bearer auth, schema-version handshake, per-slug write mutex,
         async job polling with typed diagnostics, `homeassistant_addon` resource CRUD via the
         co-located Provider.
       - "Install" section — 3 numbered steps: (1) In HA: Settings → Add-ons → Add-on Store, (2)
         Add repository `https://github.com/akentner/homeassistant-addons`, (3) Find "Terraform
         Bridge" in the store → Install → Start.
       - "First-time setup" section — 3 numbered steps for Bearer token retrieval: (1) Wait for
         the Bridge to log `bridge.token.issued` (or read directly: `sudo cat
         /usr/share/hassio/addons/data/terraform-bridge/initial-token`), (2) Copy the token into
         the Provider's `bearer_token` argument, (3) Delete the file to minimize on-disk exposure
         (`sudo rm /usr/share/hassio/addons/data/terraform-bridge/initial-token`).
       - "Configuration" pointer — 1 paragraph: full reference in [DOCS.md][docs].
       - "Direct Access" — 1-2 sentences: not exposed through HA Ingress; configure port 8124
         reachable from the OpenTofu client (Tailscale, LAN, or a reverse proxy). Plain HTTP.
       - Link reference definitions at the bottom (`[release-shield]:`, `[release]:`,
         `[docs]:`).
       Update the badge URL from `v0.2.0` to whichever current version matches the build.yaml —
       use `grep VERSION terraform-bridge/build.yaml` to confirm.

    2. **internal/verify-bridge-e2e/99-cleanup.sh** (NEW) — Separate manual cleanup scenario per
       D-18 + the agent's Discretion. Total ≤60 lines. Structure:
       - Header comment block: `# verify-bridge-e2e/99-cleanup.sh — Phase 14 SC-4 + D-18
         cleanup. SEPARATE MANUAL INVOCATION per the agent's Discretion. This script is destructive.
         It uninstalls the test add-on, removes old state backups, and prints PASS. Run after
         all verify scenarios complete. Do NOT include this script in the verify suite.`
       - `set -euo pipefail` per the verify-bridge-no-token-leak.sh pattern.
       - Source `_lib.sh`.
       - Call `preflight`; if it returns 1, print `skipped — <reason>` annotation and exit 0
         (per D-10).
       - Call `snapshot_state "99-cleanup"` (D-16 — backup the state before the destructive
         uninstall).
       - Run `tofu destroy -auto-approve` in a working dir with the test add-on's embedded
         `*.tf`. Capture stdout/stderr to a local var for logging. If `tofu` exit is non-zero,
         fall back to `ha addons uninstall local_test-addon` (the operator chooses; the script
         documents the fallback).
       - Print `Leaving /data/bridge-nonce-audit.json in place (append-only forensics per Phase
         12 D-06).`
       - Call `_lib.sh::cleanup_scenario_baks` (D-18 — find /data -maxdepth 1 -name
         'terraform.tfstate.bak.*' -mtime +7 -delete).
       - Print green `99-cleanup: PASS — test add-on uninstalled, old state backups pruned (≥7
         days), nonce journal preserved.`
       - Exit 0.
  </action>
  <acceptance_criteria>
    - terraform-bridge/README.md is between 40 and 90 lines total; sections present: About, Features, Install,
      First-time setup, Configuration (pointer to DOCS.md), Direct Access; badge URL contains `version-v0.2.0`
      matching terraform-bridge/build.yaml (verify: wc -l terraform-bridge/README.md reports ≤90; grep -c -E
      '^## ' terraform-bridge/README.md >= 5; grep 'version-v0\.2\.0' terraform-bridge/README.md matches)
    - terraform-bridge/README.md passes markdownlint-cli2 + prettier (verify: markdownlint-cli2
      terraform-bridge/README.md exits 0; awk 'length > 120' terraform-bridge/README.md exits 0 with no output
      or pre-commit's prettier wraps any over-length lines)
    - terraform-bridge/README.md does NOT duplicate any per-error_code Summary text from Provider DOCS.md
      per D-14 — no `troubleshooting-<code>` anchor URLs appear in README.md (verify: ! grep -E
      'troubleshooting-(unauthorized|not-found|critical-addon|prevented-destroy|already-installed|locked|nonce-expired|nonce-used|install-timeout|upstream-error|pwned|version)' terraform-bridge/README.md has zero matches — error-code remediation lives in DOCS.md)
    - internal/verify-bridge-e2e/99-cleanup.sh: sources _lib.sh, calls preflight, calls snapshot_state "99-cleanup",
      runs `tofu destroy -auto-approve` (or `ha addons uninstall` fallback), calls cleanup_scenario_baks, prints
      a green PASS line; header explicitly documents "SEPARATE MANUAL INVOCATION per the agent's Discretion"
      per D-18 + the agent's Discretion (verify: shellcheck -s bash -e SC1091,SC2034
      internal/verify-bridge-e2e/99-cleanup.sh exits 0; grep -E 'source.*_lib\.sh|SEPARATE MANUAL
      INVOCATION|snapshot_state|cleanup_scenario_baks|99-cleanup' internal/verify-bridge-e2e/99-cleanup.sh
      produces all expected matches)
    - The pre-commit pipeline scoped to README.md + 99-cleanup.sh passes (verify: pre-commit run --files
      terraform-bridge/README.md internal/verify-bridge-e2e/99-cleanup.sh exits 0)
    - internal/validate-versions.sh still passes after the README.md rewrite — the new badge URL
      `version-v0.2.0` matches terraform-bridge/build.yaml VERSION (verify: bash internal/validate-versions.sh
      exits 0)
  </acceptance_criteria>
  <verify>
    <automated>pre-commit run --files terraform-bridge/README.md internal/verify-bridge-e2e/99-cleanup.sh && bash internal/validate-versions.sh && shellcheck -s bash -e SC1091,SC2034 internal/verify-bridge-e2e/99-cleanup.sh && markdownlint-cli2 terraform-bridge/README.md && awk 'length > 120' terraform-bridge/README.md</automated>
    <fails_when>markdownlint-cli2 reports MD013 line-length or any other violation, pre-commit's prettier rewrites lines (signals README.md formatting doesn't match repo style), validate-versions.sh reports TOFU-05 drift or README.md badge version mismatch with build.yaml, shellcheck reports any warning against 99-cleanup.sh, OR awk 'length > 120' reports any line in README.md exceeding 120 chars</fails_when>
  </verify>
  <done>
    The new terraform-bridge/README.md is a complete 1-page HA add-on store listing per D-12 — install +
    first-time setup + Configuration pointer + Direct Access, NO error-code remediation content. The
    new internal/verify-bridge-e2e/99-cleanup.sh is the separate-manual cleanup scenario per D-18 +
    the agent's Discretion. Both files pass the pre-commit pipeline. The 3-file versioning scheme is
    unchanged (README.md badge URL still matches build.yaml — Plan 14 does not bump versions).
  </done>
</task>

<task type="auto">
  <name>Task 2: expand terraform-bridge/DOCS.md from 131 lines to the full operator reference per D-13 with all 9 sections in order</name>
  <files>
    - terraform-bridge/DOCS.md
  </files>
  <read_first>
    - terraform-bridge/DOCS.md (current 131-line skeleton — the file Plan 03 expands. Sections to
      KEEP verbatim: Token recovery §, Health check, Logs, Add-on network access. Sections to
      EXTEND: Options (add the 3 missing fields), Token issuance (add /data/initial-token canonical
      source note), Token rotation (add empirical round-trip prose). Sections to ADD: Endpoints
      reference, Troubleshooting (1-paragraph pointer + cross-link anchor table per D-14), Observed
      issues (3+ entries), State management, HA backup integration)
    - terraform-bridge/config.yaml (the 5 Options fields + their defaults: bind_address="auto",
      bind_allowed_subnets=[], critical_addons=["core_mosquitto", "core_zigbee2mqtt",
      "core_esphome"], install_job_timeout_seconds=300, try_lock_timeout_seconds=5 — DOCS.md §
      Options is generated from this file's schema)
    - terraform-bridge/contract/types.go (every endpoint response shape — DOCS.md § Endpoints
      reference section quotes the struct field names verbatim)
    - terraform-provider-homeassistant/DOCS.md §"Troubleshooting" lines 402-618 (the Provider
      DOCS.md section that Bridge DOCS.md cross-links to per D-14 — the canonical source for
      per-error_code Summary text)
    - terraform-provider-homeassistant/internal/diagnostics/doc.go (DocAnchor helper at lines
      129-135 — the anchor convention Bridge DOCS.md's Troubleshooting section follows:
      `DOCS.md#troubleshooting-<kebab-error_code>`)
    - phone-logger/DOCS.md (the canonical repo example of a comprehensive add-on reference —
      the structural pattern Plan 03's expanded DOCS.md mirrors: H1 + intro + Options table +
      Operations sections + Troubleshooting + Appendix)
    - .planning/phases/14-real-ha-end-to-end-verification-operator-documentation/14-CONTEXT.md D-13
      §1..§9 + D-14 + D-15 (locked decisions for this task; D-13 enumerates the 9 sections)
    - .planning/phases/14-real-ha-end-to-end-verification-operator-documentation/14-RESEARCH.md
      Pattern 3 §"Cross-link DOCS.md anchor" (the markdown template for the cross-link anchor
      table)
    - .planning/phases/14-real-ha-end-to-end-verification-operator-documentation/14-02-PLAN.md
      (the captured-diagnostic files this task's Observed issues section reads from)
    - .planning/phases/14-real-ha-end-to-end-verification-operator-documentation/14-01-PLAN.md (the
      verify suite Plan 03's DOCS.md references by name in § State management)
    - spike-transcripts/pitfalls10-20260831T153403Z.log (the Phase 9 §10 empirical HA backup +
      addon_config spike transcript — DOCS.md § HA backup integration section references this
      finding per CF-13)
    - .markdownlint.json (MD013 max line length 120 + MD003 ATX headers — the rules the expanded
      DOCS.md must satisfy)
    - AGENTS.md §🔌 Live Systems rule + §🛠️ pre-commit pipeline + §🔑 3-file versioning
  </read_first>
  <action>
    This task expands `terraform-bridge/DOCS.md` from the current 131-line skeleton to the full
    D-13 operator reference (≤600 lines). Honour D-14 strictly: the Troubleshooting section is a
    1-paragraph pointer to Provider DOCS.md#troubleshooting; the per-error_code Summary text is NOT
    duplicated here — the Provider DOCS.md is the single source of truth. Honour D-15 strictly:
    markdownlint + prettier MUST pass — wrap long lines at 120 chars; use ATX headers; use the
    same markdown style as phone-logger/DOCS.md (the repo's canonical comprehensive DOCS.md).

    Run `pre-commit run --files terraform-bridge/DOCS.md` after the rewrite. If prettier or
    markdownlint-cli2 reports violations, fix them by reformatting — do NOT add `# noqa` comments
    to the markdownlint config. The pre-commit pipeline is the gate.

    Per section (in order):

    1. **# Configuration** header + intro paragraph (3-5 sentences): what the Bridge does, the
       three-tier architecture (Operator → Provider → Bridge → Supervisor), where DOCS.md sits in
       the operator's mental model. Keep the existing intro paragraph verbatim — it correctly
       describes the Bridge's role.

    2. **## Options** section (table form + per-option prose):
       - Header: `# Options`
       - 5-row table with columns `Option | Type | Default | Description` for the 5 fields per
         CF-12: `bind_address`, `bind_allowed_subnets`, `critical_addons`,
         `install_job_timeout_seconds`, `try_lock_timeout_seconds`.
       - Per-option prose paragraph below the table. For `bind_address`: extend the existing
         prose with explicit mention of the `auto` default + the `0.0.0.0` always-refused rule.
         For `bind_allowed_subnets`: extend with the Phase-1 escape hatch note. For
         `critical_addons`: explain the Bridge's 403 refusal path (cross-link to Troubleshooting).
         For `install_job_timeout_seconds`: explain the Supervisor job polling budget. For
         `try_lock_timeout_seconds`: explain the per-slug mutex acquisition budget.

    3. **## Token issuance** section: keep the existing JSON log shape + the two retrieval
       commands (`sudo cat` + `ha addons cli`). ADD a new paragraph at the end: "The canonical
       operator-side path is `/usr/share/hassio/addons/data/terraform-bridge/initial-token` on
       the host. The verify suite (`internal/verify-bridge-e2e/_lib.sh`) reads from this path via
       SSH."

    4. **## Token rotation** section: keep the existing rotation procedure verbatim. ADD a new
       paragraph: "Phase 14's empirical round-trip (CONTEXT.md Specifics): the operator
       deliberately rotates the bearer mid-verification via `POST /v1/auth/rotate`; both old
       and new tokens authenticate successfully within the 24-hour grace window; manually
       deleting `/data/bridge-token.grace` immediately revokes the old token (the grace file is
       inert without background goroutines per Phase 10 D-13)."

    5. **## Token recovery** section: keep verbatim.

    6. **## Endpoints reference** section (NEW): table form for every `/v1/*` route. Columns:
       `Method | Path | Auth | Request | Response`. Rows (each row quotes the contract type from
       `terraform-bridge/contract/types.go`):
       - `GET /v1/version` | Bearer | (none) | `contract.VersionHandshake`
       - `GET /v1/info` | None | (none) | `contract.BridgeInfo`
       - `GET /v1/addons` | Bearer | (none) | `[]contract.AddOnInfo`
       - `GET /v1/addons/{slug}/info` | Bearer | (none) | `contract.AddOnInfo`
       - `POST /v1/addons/{slug}/install` | Bearer | (none) | `contract.AddOnInfo`
       - `POST /v1/addons/{slug}/uninstall` | Bearer | `X-Force-Destroy` header (LIFE-03) | `204` (no body) OR `contract.ErrorResponse`
       - `POST /v1/addons/{slug}/start` | Bearer | (none) | `contract.AddOnInfo`
       - `POST /v1/addons/{slug}/stop` | Bearer | (none) | `contract.AddOnInfo`
       - `POST /v1/addons/{slug}/options` | Bearer | JSON body + `X-Force-Destroy` | `contract.AddOnInfo` (with `pwned` flag)
       - `POST /v1/auth/nonce` | Bearer | (none) | `contract.NonceResponse`
       - `POST /v1/auth/rotate` | Bearer | (none) | `contract.RotateResponse`
       - `GET /v1/whoami` | Bearer | (none) | `contract.TokenResponse` (debug-only endpoint)
       - `GET /v1/state/index` | Bearer | (none) | `contract.StateIndexResponse`
       - `GET /healthz` | None | (none) | `contract.HealthResponse` (or 503 empty body per Phase 10 D-08)

    7. **## Troubleshooting** section (NEW per D-14): 1-paragraph pointer followed by a
       cross-link anchor table.
       - Paragraph: "Every error the Bridge returns carries a machine-readable `error_code`. The
         [`terraform-provider-homeassistant` DOCS.md § Troubleshooting](../terraform-provider-homeassistant/DOCS.md#troubleshooting)
         table is the single source of truth for what each code means, how the Provider surfaces
         it, and what the remediation is. This document does not duplicate the per-error_code
         Summary text — the Provider DOCS.md table is generated once from the
         `internal/diagnostics/doc.go` constants and consumed by both the Provider Diagnostic
         Link field and this cross-link table."
       - Cross-link anchor table: 12 rows mapping `error_code` → kebab-case DOCS.md anchor URL.
         Columns: `error_code | Bridge HTTP status | DOCS.md anchor`. Rows for: unauthorized
         (401), not_found (404), critical_addon_protected (403), prevented_destroy (403),
         already_installed (409), locked (423), nonce_expired (401), nonce_used (401),
         install_timeout (504), upstream_error (502), pwned (Warning), version (handshake
         rejection). The DOCS.md anchor URL for each row is
         `[Provider DOCS.md](../terraform-provider-homeassistant/DOCS.md#troubleshooting-<kebab>)`.

    8. **## Observed issues** section (NEW): 3+ entries from the Phase 14 verify run. Each entry
       has the same shape: Symptom → Root cause → Fix → Reference. The actual content is filled
       in during plan execution by reading the captured transcripts from
       `terraform-bridge/internal/testdata/apply-output/1..5.txt` and the operator's verify-run
       log. At plan-author time, leave placeholder entries of the shape:
       ```
       ### Observed issue N: <one-line title>

       **Symptom:** What the operator saw in the verify-run output.

       **Root cause:** Why it happened.

       **Fix:** What to do.

       **Reference:** Cross-link to the relevant section above.
       ```
       The plan executor fills in the three+ entries by reading the captured transcripts.

    9. **## State management** section (NEW): explanation of `/v1/state/index` + STATE-02
       coverage + the Provider's recommended `path = "/data/terraform.tfstate"` per STATE-01.
       Include a code block showing the OpenTofu backend configuration that points at the
       Bridge's `addon_config:rw` mount.

    10. **## HA backup integration** section (NEW per CF-13): 2-paragraph explanation that
        `addon_config:rw` mount contents (including `terraform.tfstate`, `bridge-token`,
        `bridge-nonce-audit.json`) are auto-included in `ha backups new --app terraform-bridge`
        per the Phase 9 §10 empirical result. Include a one-command example:
        `ha backups new --app terraform-bridge --name "bridge-backup-$(date +%Y%m%d)"`.

    Keep existing § Health check, § Logs, § Add-on network access sections from the current
    131-line skeleton VERBATIM (they are already correct + well-written; expanding them risks
    introducing regressions).

    Run `pre-commit run --files terraform-bridge/DOCS.md terraform-bridge/README.md
    internal/verify-bridge-e2e/99-cleanup.sh` as the final acceptance gate. This scopes the
    pre-commit run to Task 1 + Task 2's touched files, avoiding the unrelated
    `info_hub/README.md` markdownlint failure that the task header warns about.
  </action>
  <acceptance_criteria>
    - terraform-bridge/DOCS.md has 9 sections in order per D-13: Options, Token issuance, Token rotation,
      Token recovery, Endpoints reference, Troubleshooting, Observed issues, State management, HA backup
      integration (verify: grep -E '^## ' terraform-bridge/DOCS.md produces all 9 expected section headings
      in order; total line count is between 200 and 700)
    - DOCS.md § Troubleshooting is a 1-paragraph pointer to Provider DOCS.md#troubleshooting — NO
      per-error_code Summary text is duplicated in Bridge DOCS.md (verify: ! grep -E
      '(lifecycle.prevent_destroy.*is set on this resource|known compromised credentials leak|Add-on is already
      installed; this is treated as adoption)' terraform-bridge/DOCS.md has zero matches — those strings live
      ONLY in the Provider DOCS.md and the verify-scenario testdata files, NOT in Bridge DOCS.md)
    - DOCS.md § Troubleshooting contains a cross-link anchor table with 12 rows — one per error_code — each
      row pointing at `DOCS.md#troubleshooting-<kebab-error_code>` (verify: grep -c -E
      'troubleshooting-[a-z-]+' terraform-bridge/DOCS.md >= 12; the 12 distinct anchors are: unauthorized,
      not-found, critical-addon-protected, prevented-destroy, already-installed, locked, nonce-expired,
      nonce-used, install-timeout, upstream-error, pwned, version)
    - DOCS.md § Endpoints reference lists at least 13 routes (every `/v1/*` route + `/healthz`) — columns
      Method, Path, Auth, Request, Response; response types reference contract.AddOnInfo, contract.VersionHandshake,
      contract.BridgeInfo, contract.ErrorResponse, contract.NonceResponse, contract.RotateResponse,
      contract.StateIndexResponse (verify: grep -c -E 'GET|POST|PUT|DELETE' terraform-bridge/DOCS.md >= 20;
      grep -E 'contract\\.(AddOnInfo|VersionHandshake|ErrorResponse|HealthResponse|BridgeInfo|NonceResponse|RotateResponse|StateIndexResponse)' terraform-bridge/DOCS.md produces at least 6 matches across the § Endpoints reference rows)
    - DOCS.md § Observed issues has 3+ placeholder entries (executor fills them in at runtime from the
      captured transcripts) — each entry has the four sections: Symptom, Root cause, Fix, Reference
      (verify: grep -c -E '^### Observed issue [0-9]+:' terraform-bridge/DOCS.md >= 3; grep -c -E
      '\*\*Symptom:\*\*|\*\*Root cause:\*\*|\*\*Fix:\*\*|\*\*Reference:\*\*' terraform-bridge/DOCS.md >= 12 — at
      least 4 fields × 3 entries)
    - DOCS.md § HA backup integration mentions the Phase 9 §10 empirical spike result + lists the three
      files covered (`terraform.tfstate`, `bridge-token`, `bridge-nonce-audit.json`) per CF-13 (verify: grep
      -E 'terraform.tfstate|bridge-token|bridge-nonce-audit.json|addon_config:rw' terraform-bridge/DOCS.md
      produces all expected matches in the § HA backup integration section)
    - DOCS.md keeps the existing Health check, Logs, Add-on network access sections VERBATIM from the
      131-line skeleton (verify: the existing "Health check" prose is present; the existing "Logs" prose is
      present; the existing "Add-on network access" prose is present — `grep -E 'GET /healthz returns HTTP 200
      with body|bridge.token.issued|8124 to be reachable from the OpenTofu client' terraform-bridge/DOCS.md
      produces all expected matches)
    - DOCS.md passes markdownlint-cli2 + prettier (verify: markdownlint-cli2 terraform-bridge/DOCS.md exits 0;
      awk 'length > 120' terraform-bridge/DOCS.md exits 0 with no output OR pre-commit's prettier wraps the
      over-length lines automatically)
    - Final acceptance: `pre-commit run --files terraform-bridge/DOCS.md terraform-bridge/README.md
      internal/verify-bridge-e2e/99-cleanup.sh` exits 0; `bash internal/validate-versions.sh` exits 0
      (the 3-file scheme is preserved)
  </acceptance_criteria>
  <verify>
    <automated>pre-commit run --files terraform-bridge/DOCS.md terraform-bridge/README.md internal/verify-bridge-e2e/99-cleanup.sh && bash internal/validate-versions.sh && markdownlint-cli2 terraform-bridge/DOCS.md && awk 'length > 120' terraform-bridge/DOCS.md && grep -c -E 'troubleshooting-[a-z-]+' terraform-bridge/DOCS.md && grep -c -E '^### Observed issue' terraform-bridge/DOCS.md</automated>
    <fails_when>markdownlint-cli2 reports MD013 (line length) or any other violation against DOCS.md, prettier reformats the file (signals the markdown style doesn't match repo conventions), validate-versions.sh reports TOFU-05 drift, the grep for `troubleshooting-<code>` anchors finds fewer than 12 matches (the cross-link anchor table is incomplete), OR the grep for `^### Observed issue` finds fewer than 3 matches (the SC-5 "three+ observed issues" gate fails)</fails_when>
  </verify>
  <done>
    terraform-bridge/DOCS.md is the full operator reference per D-13 with all 9 sections in order
    (Options, Token issuance, Token rotation, Token recovery, Endpoints reference, Troubleshooting,
    Observed issues, State management, HA backup integration). The Troubleshooting section is a
    1-paragraph pointer to Provider DOCS.md#troubleshooting per D-14 — NO per-error_code Summary
    text is duplicated. The cross-link anchor table covers all 12 error_codes (matching the 12
    Plan 02 scenarios). The Observed issues section has 3+ entries (executor fills them in from the
    captured transcripts). The HA backup integration section cites the Phase 9 §10 empirical spike
    result per CF-13. The pre-commit pipeline + markdownlint + validate-versions ALL pass on the
    expanded DOCS.md.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary                  | Description                                                                                  |
| ------------------------- | -------------------------------------------------------------------------------------------- |
| Operator → DOCS.md        | The operator reads DOCS.md via the HA add-on store page, the GitHub repo, or the GitHub      |
|                           | Pages site. DOCS.md is a STATIC artifact — no runtime security boundary.                     |
| Operator → Provider       | The operator follows DOCS.md's install walkthrough to set up the OpenTofu/Terraform Provider. |
|                           | DOCS.md instructs the operator to set `bearer_token` to the Bridge's current token — same   |
|                           | security boundary as Plan 01's `_lib.sh` token retrieval (T-14-01 from Plan 01 applies).    |
| Operator → Bridge (live)  | Same as Plan 01 + Plan 02 — the operator's workstation reaches the Bridge via HTTPS on port  |
|                           | 8124 from the Tailscale hostname (D-01) or LAN IP when overridden.                          |

## STRIDE Threat Register

| Threat ID    | Category             | Component                                  | Severity | Disposition | Mitigation Plan                                                                                                                                                  |
| ------------ | -------------------- | ------------------------------------------ | -------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-14-09      | Information Disclosure | Bearer token written into DOCS.md examples | high     | mitigate    | DOCS.md's example `*.tf` blocks use the standard OpenTofu variable pattern: `variable "bridge_token" { type = string, sensitive = true }`. The token value is passed via `-var` at apply time, NEVER written into the file. The same `Sensitive: true` marker that Phase 13's Provider schema applies (CF-03) ensures `tofu plan` output masks the value. |
| T-14-10      | Tampering             | DOCS.md anchor table drift from Provider DOCS.md | medium | mitigate    | Bridge DOCS.md's Troubleshooting anchor table is generated from the same source as Provider DOCS.md's troubleshooting table (both reference the kebab-case `<error_code>` anchor pattern from `internal/diagnostics/doc.go:129-135`). When Provider DOCS.md adds a row for a new error_code in a future Phase, the cross-link anchor table in Bridge DOCS.md adds the same row. Per D-09 (fix docs to match observation), Plan 14's empirical run may surface drift; the plan fixes the docs to match the observation. |
| T-14-11      | Denial of Service    | 99-cleanup.sh accidental invocation       | medium   | mitigate    | The cleanup script's header explicitly documents "SEPARATE MANUAL INVOCATION per the agent's Discretion — this script is destructive." The operator runs it explicitly, never as part of the verify suite (scenarios 00-12 are NOT chained to 99 — each scenario exits with its own status code). The Bridge's state survives a botched cleanup via D-16's per-scenario snapshot pattern + the operator can restore via `mv /data/terraform.tfstate.bak.<scenario> /data/terraform.tfstate`. |
| T-14-SC      | Tampering             | Package legitimacy of any new install      | high     | mitigate    | Plan 03 installs ZERO new external packages. README + DOCS + 99-cleanup.sh are pure documentation + shell. No SLOP/SUS packages to audit. |

</threat_model>

<verification>
For overall Phase 14 Plan 03 success (not just this plan's surface):

1. **SC-5 operator documentation** (this plan's surface):
   - README.md is a 1-pager per D-12 with install + first-time setup + Direct Access + Configuration
     pointer (verified: markdownlint + validate-versions pass).
   - DOCS.md is the full operator reference per D-13 with all 9 sections in order (verified:
     markdownlint passes; cross-link anchor table covers all 12 error_codes; Observed issues has
     3+ entries; HA backup integration cites Phase 9 §10 result).
   - 99-cleanup.sh is the separate-manual cleanup script per D-18 + the agent's Discretion
     (verified: shellcheck passes; header documents the manual-invocation contract).

2. **SC-1 + SC-2 + SC-3 + SC-4**: verified by Plans 01 + 02's verify scenarios running against the
   live Bridge. Plan 03 does not modify any verify scenario code.

3. **OPS-04 closure**: the entire Phase-14 requirement surface is covered by this plan's
   deliverables (README + DOCS + cleanup). The single REQUIREMENTS.md line that maps to Phase 14
   is OPS-04 — closed.

4. **3-file versioning scheme**: Plan 03 does NOT bump versions. The README.md badge URL still
   matches `terraform-bridge/build.yaml`'s `VERSION: "0.2.0"`. Validate-versions passes.
</verification>

<success_criteria>
- terraform-bridge/README.md is ≤90 lines, has 5+ ## sections (About, Features, Install, First-time
  setup, Configuration, Direct Access), badge URL contains `version-v0.2.0` matching build.yaml,
  passes markdownlint + prettier, does NOT duplicate per-error_code Summary text.
- terraform-bridge/DOCS.md has 9 ## sections in D-13 order, 12-row Troubleshooting cross-link
  anchor table (one per error_code), Endpoints reference listing 13+ routes with contract type
  references, Observed issues with 3+ entries, HA backup integration citing Phase 9 §10 result,
  Health check / Logs / Add-on network access sections kept verbatim from the existing skeleton,
  passes markdownlint + prettier + validate-versions.
- internal/verify-bridge-e2e/99-cleanup.sh is ≤60 lines, sources _lib.sh, calls preflight +
  snapshot_state + cleanup_scenario_baks, runs `tofu destroy` (with `ha addons uninstall`
  fallback), leaves nonce journal in place, prints green PASS line, passes shellcheck.
- The full pre-commit pipeline (scoped to Task 1 + Task 2 files) exits 0 with zero warnings.
- `bash internal/validate-versions.sh` exits 0 confirming the 3-file versioning scheme is
  preserved across all touched files.
</success_criteria>

<output>
Create `.planning/phases/14-real-ha-end-to-end-verification-operator-documentation/14-03-SUMMARY.md`
when complete — the standard atomic-commit summary for this plan.
</output>
