---
phase: 01-quality-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .pre-commit-config.yaml
  - phone-logger/DOCS.md
autonomous: true
requirements:
  - FIX-01
  - FIX-02

must_haves:
  truths:
    - "Running make validate-versions passes for both fritz-callmonitor2mqtt and phone-logger"
    - "phone-logger/DOCS.md shows type: fritz_callmonitor in the example config block"
  artifacts:
    - path: ".pre-commit-config.yaml"
      provides: "validate-versions hook with files pattern covering both add-ons"
      contains: "phone-logger"
    - path: "phone-logger/DOCS.md"
      provides: "Correct adapter type name in example config"
      contains: "fritz_callmonitor"
  key_links:
    - from: ".pre-commit-config.yaml"
      to: "scripts/validate-versions.sh"
      via: "files: pattern matching both add-on directories"
      pattern: "phone-logger.*config\\.yaml"
---

<objective>
Fix the validate-versions pre-commit hook scope (FIX-01) and correct the wrong adapter type name in DOCS.md (FIX-02).

Purpose: Unblock CI so the version consistency check runs for phone-logger, and correct misleading documentation.
Output: .pre-commit-config.yaml with updated files pattern; phone-logger/DOCS.md with corrected adapter type.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend validate-versions hook to cover phone-logger (FIX-01)</name>
  <files>.pre-commit-config.yaml</files>
  <read_first>
    - .pre-commit-config.yaml (current hook definition — must read before editing)
    - scripts/validate-versions.sh (auto-discovers all add-ons via glob; no script changes needed)
  </read_first>
  <action>
    In `.pre-commit-config.yaml`, find the `validate-versions` hook entry (near line 62).
    Change the `files:` pattern from:
      `^fritz-callmonitor2mqtt/(config\.yaml|build\.yaml|README\.md)$`
    To:
      `^(fritz-callmonitor2mqtt|phone-logger)/(config\.yaml|build\.yaml|README\.md)$`

    No other changes. The validate-versions.sh script already auto-discovers all add-on directories
    containing both config.yaml and build.yaml — extending the trigger pattern is the only change needed.

  </action>
  <verify>
    <automated>grep -n 'phone-logger' /home/akentner/Projects/homeassistant-addons/.pre-commit-config.yaml</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'phone-logger' .pre-commit-config.yaml` returns a match inside the `files:` line of the validate-versions hook
    - The exact files pattern reads: `^(fritz-callmonitor2mqtt|phone-logger)/(config\.yaml|build\.yaml|README\.md)$`
    - `grep -c 'validate-versions' .pre-commit-config.yaml` returns `2` (the id line and the name line still present)
  </acceptance_criteria>
  <done>The validate-versions hook triggers on changes to phone-logger/config.yaml, phone-logger/build.yaml, and phone-logger/README.md</done>
</task>

<task type="auto">
  <name>Task 2: Correct adapter type in phone-logger/DOCS.md (FIX-02)</name>
  <files>phone-logger/DOCS.md</files>
  <read_first>
    - phone-logger/DOCS.md (current content — must read before editing)
    - phone-logger/generate_config.py (source of truth for actual adapter type names)
  </read_first>
  <action>
    In `phone-logger/DOCS.md`, find the YAML example block (around line 52).
    Change the line:
      `  - type: fritz`
    To:
      `  - type: fritz_callmonitor`

    This is the only change. The adapter name `fritz_callmonitor` matches the actual adapter type used in
    generate_config.py and the upstream phone-logger source.

  </action>
  <verify>
    <automated>grep -n 'fritz_callmonitor' /home/akentner/Projects/homeassistant-addons/phone-logger/DOCS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'fritz_callmonitor' phone-logger/DOCS.md` returns a match on the `type:` line
    - `grep '  - type: fritz$' phone-logger/DOCS.md` returns no match (the wrong value is gone)
    - `grep -c 'fritz_callmonitor' phone-logger/DOCS.md` returns `1`
  </acceptance_criteria>
  <done>The DOCS.md example config block shows `type: fritz_callmonitor` for the input adapter</done>
</task>

</tasks>

<verification>
After both tasks complete:

```bash
cd /home/akentner/Projects/homeassistant-addons
make validate-versions
grep 'phone-logger' .pre-commit-config.yaml
grep 'fritz_callmonitor' phone-logger/DOCS.md
```

All three commands must succeed with output. </verification>

<success_criteria>

- .pre-commit-config.yaml validate-versions hook files pattern includes `phone-logger`
- phone-logger/DOCS.md example block contains `type: fritz_callmonitor` (not `type: fritz`)
- `make validate-versions` exits 0 </success_criteria>

<output>
After completion, create `.planning/phases/01-quality-fixes/01-01-SUMMARY.md`
</output>
