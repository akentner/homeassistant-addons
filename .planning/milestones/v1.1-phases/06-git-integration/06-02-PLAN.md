---
phase: 06-git-integration
plan: 02
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - .planning/phases/06-git-integration/verify-git-integration.sh
  - markdown-renderer/DOCS.md
  - markdown-renderer/README.md
autonomous: true
requirements:
  - GIT-01
  - GIT-02
  - GIT-03
  - GIT-04
  - GIT-05
user_setup: []

must_haves:
  truths:
    - "Empirical verifier (verify-git-integration.sh) passes all GIT-01..05 scenarios inside a running container with no
      live HA required"
    - "DOCS.md has a Git Sync section documenting the three new fields, auto-clone behavior, periodic semantics, and
      failure modes"
    - "README.md Manual HA Ingress Test Checklist has new git-sync items"
    - "make check-all exits 0 with all changes in place"
    - "All five GIT-01..05 requirements have an empirical assertion in verify-git-integration.sh"
  artifacts:
    - path: ".planning/phases/06-git-integration/verify-git-integration.sh"
      provides: "5-scenario empirical verifier covering GIT-01..05"
      min_lines: 150
    - path: "markdown-renderer/DOCS.md"
      provides: "Git Sync documentation section"
      contains: "## Git Sync"
    - path: "markdown-renderer/README.md"
      provides: "Extended Manual HA Ingress Test Checklist with git-sync items"
      contains: "git_pull"
  key_links:
    - from: ".planning/phases/06-git-integration/verify-git-integration.sh"
      to: "markdown-renderer/run.sh"
      via: "container startup with git-enabled options.json fixtures"
      pattern: "podman run"
    - from: "markdown-renderer/DOCS.md"
      to: "markdown-renderer/config.yaml"
      via: "documents the git_pull/git_pull_interval/git_url schema fields"
      pattern: "git_pull:"
    - from: "markdown-renderer/README.md"
      to: "verify-git-integration.sh"
      via: "references the verifier in the checklist"
      pattern: "verify-git-integration"
---

<objective>
Empirically verify all five GIT-01..05 requirements end-to-end inside a running `local/markdown-renderer:1.1.0` container using a 5-scenario verifier script (mirroring Phase 5's `verify-multi-namespace.sh` pattern), and document the new git-sync behavior in DOCS.md and README.md.

Purpose: Plan 01 delivered the implementation; this plan proves it works and ships the user-facing docs. The verifier
becomes the canonical empirical record of GIT-01..05 compliance.

Output: New `verify-git-integration.sh` (5 scenarios: startup-pull success, startup-pull-failure graceful, periodic-pull
updates, no-git-invocation-when-disabled, first-time-clone via git_url) + extended DOCS.md `## Git Sync` section +
extended README.md Manual HA Ingress Test Checklist. </objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-git-integration/06-CONTEXT.md
@.planning/phases/06-git-integration/06-01-PLAN.md
@.planning/phases/05-multi-namespace-dynamic-config/05-01-SUMMARY.md
@.planning/phases/05-multi-namespace-dynamic-config/verify-multi-namespace.sh
@markdown-renderer/DOCS.md
@markdown-renderer/README.md
</context>

<interfaces>
<!-- Key contracts the executor needs. -->

From `06-01-PLAN.md` (implementation just completed):

```sh
# run.sh CLI surface (executed inside container):
python3 /app/_git_sync.py              # startup pull (no flags)
python3 /app/_git_sync.py --periodic    # one iteration of periodic pull

# /data/options.json schema (extended in Plan 01):
{
  "directories": [
    {"name": "docs", "path": "/share/docs",
     "git_pull": true, "git_pull_interval": 0, "git_url": ""}
  ],
  "kroki_url": "https://kroki.io"
}
```

From `verify-multi-namespace.sh` (pattern reference for verifier structure):

- Uses `podman run --rm -d -p 8099:8099 -v <fixture>:/data local/markdown-renderer:1.1.0`
- Captures container logs into a shell variable (`MR_LOGS=$(podman logs ...)`) before grepping — avoids
  `podman logs | grep -q` pipe-buffering race documented in `05-01-SUMMARY.md` "Issues Encountered"
- Uses `127.0.0.1` explicitly to dodge IPv6 `::1` resolution race
- Defines `SCENARIO_A`, `SCENARIO_B`, ... blocks with header banners
- Exits 0 on all-pass, non-zero on first failure (set -e)

Image to use in tests: `local/markdown-renderer:1.1.0` (rebuild via `make build-addon ADDON=markdown-renderer` before
running verifier; image tag matches the v1.1 schema extension — do NOT use the existing 1.0.0 image which lacks the git
binary). </interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create verify-git-integration.sh — 5-scenario empirical verifier</name>
  <files>.planning/phases/06-git-integration/verify-git-integration.sh</files>
  <read_first>
    - .planning/phases/05-multi-namespace-dynamic-config/verify-multi-namespace.sh (canonical verifier pattern: scenario blocks, log capture, IPv4 explicit, exit-on-first-fail)
    - .planning/phases/05-multi-namespace-dynamic-config/05-01-SUMMARY.md §"Issues Encountered" (the three known race conditions to design around)
    - .planning/phases/06-git-integration/06-CONTEXT.md D-05, D-07, D-08, D-09 (exact semantic contracts the verifier must check)
    - markdown-renderer/_git_sync.py (created in Plan 01 — know the warning message format it prints)
    - markdown-renderer/run.sh (rewritten in Plan 01 — know the order: safe.directory → generate_nginx.py → _git_sync.py → background loop → exec nginx)
  </read_first>
  <behavior>
    - Scenario A — GIT-01 (startup pull success): container starts with options.json containing one namespace at /tmp/test-repo (a real local git repo initialized in the fixture), git_pull=true, git_pull_interval=0. Commit a new file to the local repo before starting the container. After startup, GET /test-repo/newfile.md returns the new content (proving nginx serves the pulled content). Log contains "Already up to date." or similar git pull success indicator.
    - Scenario B — GIT-05 (startup pull failure graceful): same as A but the git_url points at an unreachable host (https://127.0.0.1:1/nonexistent.git) AND the fixture path is a real git repo so clone won't trigger — instead the pull will fail. Container stays running (exit 0). Log contains "WARNING: git pull failed for <name>". GET /test-repo/ still serves the pre-existing local index.html.
    - Scenario C — GIT-03 (no git invocation when disabled): options.json has git_pull=false, git_pull_interval=0, git_url="". Container starts. Log does NOT contain any "git pull" or "git clone" line. No `git` binary invoked (verify by `podman exec <ctr> ls /proc/*/comm 2>/dev/null` filtered for "git" — should be empty after startup settles).
    - Scenario D — GIT-04 (periodic pull updates): options.json has git_pull=false, git_pull_interval=5. Initial state: fixture repo at commit N. After container start, commit N+1 to the repo. Wait 8 seconds (interval=5 + buffer). Log contains a second pull invocation after the initial startup. GET /test-repo/newfile-from-N+1.md serves the new content.
    - Scenario E — D-02 (first-time clone via git_url): options.json has path=/tmp/not-a-repo, git_pull=true, git_url=file:///tmp/source-repo (a local file:// cloneable repo). Container starts. After startup, /tmp/not-a-repo contains a .git/ directory (clone succeeded). GET /not-a-repo/ serves content cloned from source-repo.
  </behavior>
  <acceptance_criteria>
    - File `.planning/phases/06-git-integration/verify-git-integration.sh` exists and is executable (verify: `test -x .planning/phases/06-git-integration/verify-git-integration.sh`)
    - File starts with `#!/bin/bash` and `set -e` (verify: `head -2 .planning/phases/06-git-integration/verify-git-integration.sh`)
    - File ≥ 150 lines (verify: `test $(wc -l < .planning/phases/06-git-integration/verify-git-integration.sh) -ge 150`)
    - Contains 5 SCENARIO_A..E header banners (verify: `grep -c '^━━━ SCENARIO' .planning/phases/06-git-integration/verify-git-integration.sh` returns 5)
    - Uses `127.0.0.1` for curl (verify: `grep -c '127.0.0.1' .planning/phases/06-git-integration/verify-git-integration.sh` ≥ 5)
    - Captures `podman logs` into a variable before grepping (verify: `grep -c 'MR_LOGS=' .planning/phases/06-git-integration/verify-git-integration.sh` ≥ 3)
    - Uses `local/markdown-renderer:1.1.0` image tag (verify: `grep -q 'local/markdown-renderer:1.1.0' .planning/phases/06-git-integration/verify-git-integration.sh`)
    - Uses `file://` protocol for the local-source-repo clone in Scenario E (verify: `grep -q 'file://' .planning/phases/06-git-integration/verify-git-integration.sh`)
    - Prints `ALL VERIFICATIONS PASSED` final banner on success (verify: `grep -q 'ALL VERIFICATIONS PASSED' .planning/phases/06-git-integration/verify-git-integration.sh`)
    - `shellcheck -e SC1091,SC2034 .planning/phases/06-git-integration/verify-git-integration.sh` exits 0
    - Running the script with a pre-built `local/markdown-renderer:1.1.0` image exits 0 with "ALL VERIFICATIONS PASSED" output
  </acceptance_criteria>
  <action>
    Build `verify-git-integration.sh` mirroring the structure of `.planning/phases/05-multi-namespace-dynamic-config/verify-multi-namespace.sh` (which is 250+ lines and well-tested per Phase 5). Adapt the 5 scenarios from the `<behavior>` block above.

    Implementation requirements:

    1. **Image prerequisite check** — Run `podman images | grep -qE '^(localhost/)?local/markdown-renderer:1\.1\.0$'` first. If missing, print `ERROR: local/markdown-renderer:1.1.0 not found. Run: make build-addon ADDON=markdown-renderer` and exit 1. (Phase 5 had a flake here per its "Issues Encountered" section — document the retry-on-failure behavior inline as a comment but the primary check must succeed.)

    2. **Fixture setup** — Use `mktemp -d` to create a fresh fixture directory for each scenario. Each fixture contains:
       - A `source-repo/` (Scenarios A, D, E) which is a real `git init` + `git commit` repo
       - A `test-repo/` (Scenarios A, D) which is a clone of source-repo so it has a remote to pull from
       - A `not-a-repo/` empty directory (Scenario E)
       - A pre-written `options.json` for that scenario

    3. **Container startup** — `podman run --rm -d --name mr-git-test -p 8099:8099 -v <fixture>:/data local/markdown-renderer:1.1.0`. Wait for nginx to be listening (`curl -fsS http://127.0.0.1:8099/ > /dev/null` with retries).

    4. **Log capture** — `MR_LOGS=$(podman logs mr-git-test 2>&1)` ONCE per scenario, then grep the captured variable. Document inline why (avoids `podman logs | grep -q` pipe-buffering race per Phase 5 §Issues Encountered).

    5. **Cleanup** — `podman stop mr-git-test` then `rm -rf <fixture>` in a trap handler (so failures still clean up).

    6. **Each scenario prints PASS/FAIL per assertion** — Use the same `echo "  PASS ${DESC}"` and `echo "  FAIL ${DESC}"` pattern from verify-multi-namespace.sh. Use `set -e` so the first FAIL exits non-zero.

    7. **Final summary** — At the end of all 5 scenarios, print `ALL VERIFICATIONS PASSED (N assertions)` and exit 0. If any scenario fails, the script exits non-zero at the first `set -e` failure.

    Scenario-specific guidance:

    - **Scenario A** — Commit a file `newfile-A.md` with content `scenario-A-content` to source-repo before cloning. After cloning into test-repo, also commit `newfile-A2.md` with content `scenario-A2-content`. Start container with git_pull=true. Assert GET http://127.0.0.1:8099/test-repo/newfile-A2.md returns "scenario-A2-content".

    - **Scenario B** — Use git_url=`https://127.0.0.1:1/nope.git` (unreachable). Set git_pull=true. Path is already a git repo so clone won't fire. Container should stay running. Log should contain `WARNING: git pull failed for test-repo`. GET http://127.0.0.1:8099/test-repo/ returns 200 with the cached index.html.

    - **Scenario C** — All three git fields at defaults (false, 0, ""). Start container. Assert log does NOT contain `git pull` or `git clone`. Use `podman exec mr-git-test sh -c 'pgrep -af git || true'` immediately after startup and assert it returns nothing (process check).

    - **Scenario D** — git_pull_interval=5. Wait 8 seconds after container start, then push a new commit to source-repo and pull it locally so test-repo's remote-tracking ref advances. Wait another 8 seconds. Assert log contains at least 2 invocations of `git pull` (initial periodic-tick entry + the actual pull that succeeds). GET /test-repo/newfile-D.md returns the new content.

    - **Scenario E** — Path=/tmp/not-a-repo (NOT a git repo). git_pull=true. git_url=`file:///tmp/source-repo`. After container start, assert `podman exec mr-git-test sh -c 'test -d /tmp/not-a-repo/.git'` succeeds. GET /not-a-repo/ serves content from source-repo.

    Use the same fixture-dir cleanup pattern as Phase 5 (trap on EXIT). Do not parallelize scenarios — each one needs its own container + fixture.

  </action>
  <verify>
    <automated>cd /home/akentner/Projects/homeassistant-addons && test -x .planning/phases/06-git-integration/verify-git-integration.sh && test $(wc -l < .planning/phases/06-git-integration/verify-git-integration.sh) -ge 150 && grep -c '^━━━ SCENARIO' .planning/phases/06-git-integration/verify-git-integration.sh && grep -q 'local/markdown-renderer:1.1.0' .planning/phases/06-git-integration/verify-git-integration.sh && grep -q 'file://' .planning/phases/06-git-integration/verify-git-integration.sh && grep -q 'ALL VERIFICATIONS PASSED' .planning/phases/06-git-integration/verify-git-integration.sh && shellcheck -e SC1091,SC2034 .planning/phases/06-git-integration/verify-git-integration.sh</automated>
  </verify>
  <done>verify-git-integration.sh exists, executable, ≥ 150 lines, defines all 5 SCENARIO_A..E blocks, references the 1.1.0 image, uses file:// for local-source-repo, prints ALL VERIFICATIONS PASSED on success, and passes shellcheck.</done>
</task>

<task type="auto">
  <name>Task 2: Run verifier end-to-end and capture pass/fail transcript</name>
  <files>.planning/phases/06-git-integration/06-VERIFICATION-TRANSCRIPT.md</files>
  <read_first>
    - .planning/phases/06-git-integration/verify-git-integration.sh (the verifier created in Task 1)
    - .planning/phases/05-multi-namespace-dynamic-config/05-01-SUMMARY.md §"Verifier Output (verbatim)" (the format for capturing verifier output)
  </read_first>
  <acceptance_criteria>
    - `make build-addon ADDON=markdown-renderer` exits 0 (the 1.1.0 image builds with git installed)
    - `podman images | grep -qE '^(localhost/)?local/markdown-renderer:1\.1\.0$'` succeeds
    - `bash .planning/phases/06-git-integration/verify-git-integration.sh` exits 0 and prints `ALL VERIFICATIONS PASSED` (any number of assertions)
    - File `.planning/phases/06-git-integration/06-VERIFICATION-TRANSCRIPT.md` exists and contains the captured stdout/stderr verbatim from the verifier run
  </acceptance_criteria>
  <action>
    1. Run `cd /home/akentner/Projects/homeassistant-addons && make build-addon ADDON=markdown-renderer 2>&1 | tail -20` and confirm exit 0. If the build fails, STOP and report the build error to the user — DO NOT proceed to the verifier run with a broken image.

    2. Run the verifier and capture output: `bash .planning/phases/06-git-integration/verify-git-integration.sh 2>&1 | tee /tmp/verifier-output.txt`. If exit non-zero, the verifier found a bug — DO NOT proceed silently. Follow the same auto-fix protocol as Phase 5 §"Auto-fixed Issues":
       - Inspect the failing scenario's logs (`podman logs <name>` if container still exists, or re-run with diagnostic logging)
       - Identify the root cause (likely candidates: missing git binary in PATH, subprocess argument quoting, sleep timing in periodic loop, fixture setup bug)
       - Fix the underlying code in markdown-renderer/_git_sync.py or markdown-renderer/run.sh (NOT in the verifier — verifier changes are only justified if the verifier itself has a bug)
       - Rebuild the image and re-run

    3. On success, write `.planning/phases/06-git-integration/06-VERIFICATION-TRANSCRIPT.md` containing the verbatim verifier stdout (including `ALL VERIFICATIONS PASSED (N assertions)` line). Use the same format as `05-01-SUMMARY.md` §"Verifier Output (verbatim)" — fenced code block with no edits.

    4. If bugs were found and fixed during this task, document them in `06-VERIFICATION-TRANSCRIPT.md` as a "Bugs Fixed During Verification" section (mirroring Phase 5's pattern).

  </action>
  <verify>
    <automated>cd /home/akentner/Projects/homeassistant-addons && podman images | grep -qE '^(localhost/)?local/markdown-renderer:1\.1\.0$' && bash .planning/phases/06-git-integration/verify-git-integration.sh 2>&1 | tail -3 | grep -q 'ALL VERIFICATIONS PASSED' && test -f .planning/phases/06-git-integration/06-VERIFICATION-TRANSCRIPT.md</automated>
  </verify>
  <done>markdown-renderer:1.1.0 image builds; verifier exits 0 with ALL VERIFICATIONS PASSED; transcript file captures the verbatim output.</done>
</task>

<task type="auto">
  <name>Task 3: Document git-sync in DOCS.md and extend README.md checklist</name>
  <files>markdown-renderer/DOCS.md, markdown-renderer/README.md</files>
  <read_first>
    - markdown-renderer/DOCS.md (current 105-line doc; Phase 5 added `## Multi-Namespace Behavior` and `## Validation Status` sections at H2 level — match that pattern)
    - markdown-renderer/README.md (current 74-line doc; Phase 5 added 3 new Manual HA Ingress Test Checklist items 8, 9, 10 — match that pattern)
    - markdown-renderer/config.yaml (extended schema from Plan 01 — use the same field names verbatim)
    - .planning/phases/06-git-integration/06-VERIFICATION-TRANSCRIPT.md (the canonical proof that git-sync works — link to it from docs)
    - .planning/codebase/CONVENTIONS.md §Markdown Conventions (120-char limit, ATX headings)
  </read_first>
  <acceptance_criteria>
    - `markdown-renderer/DOCS.md` contains a new `## Git Sync` H2 section (verify: `grep -q '^## Git Sync$' markdown-renderer/DOCS.md`)
    - `## Git Sync` section documents all three schema fields by name: `git_pull`, `git_pull_interval`, `git_url` (verify: `grep -A30 '^## Git Sync$' markdown-renderer/DOCS.md | grep -q 'git_pull:' && grep -A30 '^## Git Sync$' markdown-renderer/DOCS.md | grep -q 'git_pull_interval:' && grep -A30 '^## Git Sync$' markdown-renderer/DOCS.md | grep -q 'git_url:'`)
    - `## Git Sync` section mentions "non-blocking" or "warning" or "graceful" failure semantics (verify: `grep -A50 '^## Git Sync$' markdown-renderer/DOCS.md | grep -qiE 'non-blocking|warning|graceful'`)
    - `markdown-renderer/DOCS.md` references the verifier script (verify: `grep -q 'verify-git-integration.sh' markdown-renderer/DOCS.md`)
    - `markdown-renderer/README.md` mentions `git_pull` in the Manual HA Ingress Test Checklist (verify: `grep -A20 'Manual HA Ingress Test Checklist' markdown-renderer/README.md | grep -q 'git_pull'`)
    - `markdown-renderer/README.md` H1 description mentions git sync (verify: `head -3 markdown-renderer/README.md | grep -qi 'git'`)
    - Both files pass `markdownlint-cli2` (no MD013 line-length violations introduced; Phase 5 doc extended from 49→105 lines, README from 63→74 lines — expect DOCS +30-50 lines, README +10-15 lines)
    - `make check-all` exits 0
  </acceptance_criteria>
  <action>
    Extend `markdown-renderer/DOCS.md` by appending a new H2 section `## Git Sync` AFTER the existing `## Validation Status` section (preserves Phase 5's section order: Configuration, Multi-Namespace Behavior, Validation Status, Git Sync). The new section must document:

    1. **Three schema fields** — A short subsection `### Schema fields` with a table or list of `git_pull` (bool, default false), `git_pull_interval` (int seconds, default 0), `git_url` (optional HTTPS or `file://` URL).

    2. **Startup pull behavior** — `### Startup pull` explaining GIT-01: when `git_pull: true`, `git pull --ff-only` runs at startup before nginx starts.

    3. **Periodic sync behavior** — `### Periodic sync` explaining GIT-04: when `git_pull_interval > 0`, one background loop pulls each namespace whose own interval has elapsed (D-08). State is in-memory only (D-09).

    4. **First-time clone** — `### First-time clone` explaining D-02: when the path is not a git repo AND `git_url` is set, `git clone <git_url> <path>` runs instead of `git pull`.

    5. **Failure semantics** — `### Failure handling` explaining GIT-05: all git errors log as `WARNING:` but never block startup. The add-on serves locally cached Markdown even if the remote is unreachable.

    6. **Verifier reference** — A short note: `### Verifier` linking to `.planning/phases/06-git-integration/verify-git-integration.sh` for the empirical proof that all five GIT-01..05 requirements work.

    7. **Scope notes** — One line: `SSH key authentication for private repositories is planned for v1.2; v1.1 supports HTTPS public repositories and local file:// URLs only.``

    Extend `markdown-renderer/README.md`:
    - Add "and optional Git sync for Markdown sources" to the H1 description (line 1-2 area).
    - Add 3 new numbered items to the `## Manual HA Ingress Test Checklist`:
      11. With `git_pull: true` on a namespace, commit a new file to the source repo; restart the add-on; confirm the new file appears at `http://ha-host:8099/<namespace>/<newfile>.md`.
      12. With `git_pull_interval: 30` on a namespace, commit a new file to the source repo; wait 35 seconds; refresh the browser; confirm the new file is visible.
      13. With all `git_*` fields at defaults, confirm `podman logs <ctr>` shows no `git pull` or `git clone` invocations.

    Wrap prose to 120 chars per `.markdownlint.json` (MD013). Do not modify other sections.

  </action>
  <verify>
    <automated>cd /home/akentner/Projects/homeassistant-addons && grep -q '^## Git Sync$' markdown-renderer/DOCS.md && grep -A30 '^## Git Sync$' markdown-renderer/DOCS.md | grep -q 'git_pull:' && grep -A30 '^## Git Sync$' markdown-renderer/DOCS.md | grep -q 'git_pull_interval:' && grep -A30 '^## Git Sync$' markdown-renderer/DOCS.md | grep -q 'git_url:' && grep -A50 '^## Git Sync$' markdown-renderer/DOCS.md | grep -qiE 'non-blocking|warning|graceful' && grep -q 'verify-git-integration.sh' markdown-renderer/DOCS.md && grep -A20 'Manual HA Ingress Test Checklist' markdown-renderer/README.md | grep -q 'git_pull' && head -3 markdown-renderer/README.md | grep -qi 'git' && make check-all</automated>
  </verify>
  <done>DOCS.md has a `## Git Sync` section documenting all three schema fields with non-blocking failure semantics; DOCS.md references the verifier; README.md H1 mentions git sync and the Manual HA Ingress Test Checklist has new git-sync items; `make check-all` exits 0.</done>
</task>

</tasks>

<verification>
- All three tasks' automated verification commands pass
- `verify-git-integration.sh` exits 0 with `ALL VERIFICATIONS PASSED` against a freshly built `local/markdown-renderer:1.1.0` image
- `06-VERIFICATION-TRANSCRIPT.md` captures the verbatim verifier output
- `markdown-renderer/DOCS.md` has a `## Git Sync` H2 section
- `markdown-renderer/README.md` Manual HA Ingress Test Checklist includes items 11, 12, 13 for git-sync
- `make check-all` exits 0 with all changes in place
- All 5 GIT-01..05 requirements have an empirical assertion in the verifier (Scenario A→GIT-01, B→GIT-05, C→GIT-03, D→GIT-04, E→D-02 first-time-clone which feeds GIT-01 via auto-clone)
</verification>

<success_criteria>

- All 5 GIT-01..05 requirements are empirically verified end-to-end inside a running container with no live HA required
- The verifier script is reusable and re-runnable; it documents its own prerequisites (image tag, mktemp fixtures,
  podman availability)
- DOCS.md and README.md changes match the Phase 5 documentation pattern (H2 section append, checklist item append,
  120-char wrap)
- File modification footprint: 3 files (verify-git-integration.sh, DOCS.md, README.md) plus 1 transcript
  (06-VERIFICATION-TRANSCRIPT.md) that is documentation of the verifier run
- `make check-all` exits 0 — no yamllint, shellcheck, markdownlint, actionlint, or version-consistency regressions
  </success_criteria>

<output>
After completion, create `.planning/phases/06-git-integration/06-02-SUMMARY.md` with the standard summary template, including the verbatim verifier output (mirroring `05-01-SUMMARY.md` §"Verifier Output (verbatim)").
</output>
