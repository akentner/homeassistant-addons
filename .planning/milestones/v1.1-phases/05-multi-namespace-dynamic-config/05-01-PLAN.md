---
phase: 05-multi-namespace-dynamic-config
plan: 01
type: execute
wave: 1
depends_on:
  - 04-01
  - 04-02
  - 04-03
files_modified:
  - markdown-renderer/DOCS.md
  - markdown-renderer/README.md
autonomous: true
requirements:
  - MULTI-01
  - MULTI-02
  - MULTI-03
  - MULTI-04
  - MULTI-05
  - MULTI-06

must_haves:
  truths:
    - "A running markdown-renderer container with 2+ namespace fixtures serves each namespace as an isolated Docsify SPA
      at /<name>/"
    - "Landing page at / lists all configured namespaces as clickable cards and regenerates after a restart with
      different config"
    - "An invalid namespace name causes the container to exit non-zero with a clear stderr message naming the bad name"
    - "Volume mounts /share, /config, /media are readable from inside the container; namespace files from each mount are
      served without permission errors"
    - "Edge cases (duplicate names, empty list, missing options.json) all handled gracefully without uncaught tracebacks"
    - "DOCS.md documents landing page regeneration + reserved-name rules + multi-source /share+/config+/media example"
    - "make check-all exits 0 after Phase 5 docs changes; no regression in pre-commit pipeline"
  artifacts:
    - path: markdown-renderer/DOCS.md
      provides: "Multi-namespace behavior + reserved names + landing-page regeneration note"
      contains: "landing page"
    - path: markdown-renderer/README.md
      provides: "Multi-namespace verification entries appended to Manual HA Ingress Test Checklist"
      contains: "Multi-namespace"
  key_links:
    - from: podman/docker container (local/markdown-renderer:1.0.0)
      to: "http://localhost:8099 endpoints"
      via: "OPTIONS_PATH mounted at /data/options.json + fixture dirs at /share/docs /config/runbooks /media/photos"
      pattern: "curl http://localhost:8099/<ns>/"
    - from: markdown-renderer/generate_nginx.py validate_directories
      to: container stderr on bad input
      via: "podman run --rm with /data/options.json containing invalid namespace"
      pattern: "ERROR: namespace name"
    - from: markdown-renderer/run.sh
      to: /tmp/nginx.conf regeneration on container restart
      via: "python3 /app/generate_nginx.py runs on every container start"
      pattern: "exec nginx -g"
---

<objective>
Close out Phase 5 by empirically verifying the multi-namespace behavior already structurally implemented in Phase 4,
then updating DOCS.md + README.md to reflect the verified behavior. This plan exercises ALL 6 MULTI-01..06 requirements
end-to-end inside a running container (no HA instance required), confirms graceful edge-case handling (duplicate names,
empty list, missing options.json, invalid reserved name), and documents the multi-namespace workflow + landing-page
regeneration semantics in the user-facing docs.

Purpose: Phase 4 SUMMARY explicitly deferred empirical verification to the user ("empirical HA Ingress test deferred to
user"). Phase 5 picks up that deferred work in a sandbox-friendly way (podman/docker run + curl, not a live HA
instance). The 6 MULTI acceptance criteria are verified deterministically; DOCS.md is enhanced to make the
multi-namespace behavior obvious to first-time readers; README's Manual HA Ingress Test Checklist is extended so the
live-HA test procedure covers multi-namespace too.

Output: A reproduceable empirical verification script that exits 0 only when all 6 MULTI requirements pass; updated
markdown-renderer/DOCS.md with multi-namespace + landing-page-regeneration sections; updated markdown-renderer/README.md
with multi-namespace Manual HA Ingress Test Checklist entries. make check-all exits 0 after the docs edits.

</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-scaffold-ingress-validation/04-01-SUMMARY.md
@.planning/phases/04-scaffold-ingress-validation/04-02-SUMMARY.md
@.planning/phases/04-scaffold-ingress-validation/04-03-SUMMARY.md
@.planning/phases/04-scaffold-ingress-validation/04-CONTEXT.md (locked D-01..D-12 — must remain consistent)

# Source-of-truth reference files (Phase 4 implementation)

@markdown-renderer/config.yaml (current state — MULTI-01 list schema, MULTI-06 share/config/media:rw)
@markdown-renderer/build.yaml @markdown-renderer/.upstream.yaml @markdown-renderer/Dockerfile (npm registry vendoring —
confirmed in 04-03 SUMMARY) @markdown-renderer/run.sh (2-line generator + exec nginx)
@markdown-renderer/generate_nginx.py (full MULTI logic: validate_namespace, validate_directories,
render_namespace_blocks, render_landing_html, \_render_nginx, \_write_minimal_nginx, main()) @markdown-renderer/DOCS.md
(current state — directories + kroki_url + volumes + example + Kroki notes) @markdown-renderer/README.md (current state
— Verification section + 7-point Manual HA Ingress Test Checklist)

# Validation tooling

@Makefile (check-all, build-addon targets) @scripts/validate-versions.sh (auto-discovers markdown-renderer/)
@scripts/validate-addon-config.py @scripts/validate-dockerfile-args.sh @.pre-commit-config.yaml (yamllint, shellcheck,
prettier, check-yaml, check-executables-have-shebangs)

# Build artifact (already exists from Phase 4)

`local/markdown-renderer:1.0.0` is already built (verified: `docker images | grep markdown-renderer`). Reuse — do NOT
rebuild unless Task 1 verify fails to find the image.

</context>

<tasks>

<task type="auto">
  <name>Task 1: Empirical multi-namespace end-to-end verification inside container</name>
  <files>.planning/phases/05-multi-namespace-dynamic-config/verify-multi-namespace.sh</files>
  <read_first>
    - markdown-renderer/generate_nginx.py (read the full file — especially _write_minimal_nginx for empty-list behavior,
      validate_directories for the duplicate error message, and the main() nginx -t catch for clear stderr patterns)
    - markdown-renderer/run.sh (confirm python3 /app/generate_nginx.py runs first, then exec nginx -g daemon off)
    - markdown-renderer/Dockerfile (confirm EXPOSE 8099 + CMD ["/app/run.sh"])
    - markdown-renderer/config.yaml (confirm map: [share:rw, config:rw, media:rw] and options.directories default)
    - .planning/phases/04-scaffold-ingress-validation/04-03-SUMMARY.md (confirm image local/markdown-renderer:1.0.0 was
      successfully built and contains the 3 vendored assets at /app/_docsify/)
  </read_first>
  <action>
    Create .planning/phases/05-multi-namespace-dynamic-config/verify-multi-namespace.sh — a single self-contained bash
    script that exits 0 only when all 6 MULTI requirements pass. The script must:

    1. Pre-flight checks at top (using set -euo pipefail):
       a. `command -v podman >/dev/null || command -v docker >/dev/null` (fail with message "no podman or docker found")
       b. detect RUNTIME=podman or RUNTIME=docker
       c. `$RUNTIME images --format '{{.Repository}}:{{.Tag}}' | grep -q '^localhost/local/markdown-renderer:1.0.0$\|^local/markdown-renderer:1.0.0$'` (fail with message "image not built — run make build-addon ADDON=markdown-renderer")
       d. trap EXIT to clean up any container named mr-verify, any host port 8099, any host fixture dirs in /tmp/mr-verify-*

    2. Fixture setup under /tmp/mr-verify-fixtures/:
       - mkdir -p /tmp/mr-verify-fixtures/{share,config,media}
       - echo '# Docs Namespace\n\nThis is the docs namespace served from /share/docs.' > /tmp/mr-verify-fixtures/share/docs/README.md
       - echo '# Runbooks Namespace\n\nThis is the runbooks namespace served from /config/runbooks.' > /tmp/mr-verify-fixtures/config/runbooks/README.md
       - echo '# Photos Namespace\n\nThis is the photos namespace served from /media/photos.' > /tmp/mr-verify-fixtures/media/photos/README.md

    3. Three-namespace options.json at /tmp/mr-verify-fixtures/options.json:
       {"directories": [{"name":"docs","path":"/share/docs"},{"name":"runbooks","path":"/config/runbooks"},{"name":"photos","path":"/media/photos"}],"kroki_url":"https://kroki.io"}

    4. Helper function: run_container(options_file, label) — starts container in background with name mr-verify,
       $RUNTIME run -d --name mr-verify -p 8099:8099 -v "$options_file":/data/options.json:ro
       -v /tmp/mr-verify-fixtures/share:/share:ro -v /tmp/mr-verify-fixtures/config:/config:ro
       -v /tmp/mr-verify-fixtures/media:/media:ro localhost/local/markdown-renderer:1.0.0
       then sleeps 3 seconds and waits up to 30 seconds polling `curl -sf http://localhost:8099/_docsify/docsify.min.js`
       until it returns 200 (nginx has finished starting). Output $label line.

    5. Helper function: stop_container() — `$RUNTIME rm -f mr-verify` (idempotent).

    6. Assertion macros: pass(label) and fail(label) using exit code and colored output.

    7. SCENARIO A — happy path with 3 namespaces (covers MULTI-01, MULTI-02, MULTI-03, MULTI-04, MULTI-06):
       a. run_container(3-namespace options.json)
       b. MULTI-01: assert OPTIONS_PATH loaded (check generator log via $RUNTIME logs mr-verify | grep "Validated 3 namespace")
       c. MULTI-02: curl /docs/ | grep -q '<title>docs</title>' (or "Loading docs" — exact pattern from generator); same for /runbooks/, /photos/
       d. MULTI-03: curl / | grep -q 'href="/docs/"' + grep -q 'href="/runbooks/"' + grep -q 'href="/photos/"' + grep -q '<h2>docs</h2>' + grep -q '<h2>runbooks</h2>' + grep -q '<h2>photos</h2>'
       e. MULTI-04: curl /_docsify/docsify.min.js returns 200 (vendored assets served); $RUNTIME logs mr-verify | grep -q "Generated nginx config for 3 namespace" (proves generate_nginx.py ran)
       f. MULTI-06: per-namespace .md fetch proves volume mounts work — for each of /docs/, /runbooks/, /photos/, the served index.html has basePath=window.location.pathname + relative _docsify/ paths (relative paths prove the nginx config + index.html generation worked end-to-end inside the container)
       g. stop_container

    8. SCENARIO B — landing page regenerates on config change (extends MULTI-03):
       a. Write a new options.json with only 1 namespace: {"directories":[{"name":"only","path":"/share/docs"}]}
       b. run_container(1-namespace options.json)
       c. curl / | grep -q 'href="/only/"' && curl / | grep -qv 'href="/docs/"' (exactly one card now, no docs card)
       d. stop_container

    9. SCENARIO C — invalid namespace names rejected at startup (covers MULTI-05):
       For each of 4 invalid options.json fixtures, run_container, wait 5 seconds, then assert:
       a. `$RUNTIME ps -a --filter name=mr-verify --format '{{.State}}'` is "exited" (NOT "running") — container crashed at startup
       b. `$RUNTIME logs mr-verify | grep -q "ERROR: namespace name"` — clear error in stderr
       c. `$RUNTIME logs mr-verify | grep -qv "Traceback"` — no Python traceback
       d. stop_container (cleanup)
       Fixtures:
       - name with slash: {"directories":[{"name":"bad/name","path":"/x"}]}
       - empty name: {"directories":[{"name":"","path":"/x"}]}
       - reserved name _docsify: {"directories":[{"name":"_docsify","path":"/x"}]}
       - uppercase name: {"directories":[{"name":"Docs","path":"/x"}]}
       Expected error substring for each: "namespace name" (the validate_namespace ValueError is caught in main() and
       printed as "ERROR: namespace name ...")

    10. SCENARIO D — duplicate names rejected (extends MULTI-05):
        a. options.json = {"directories":[{"name":"a","path":"/p"},{"name":"a","path":"/q"}]}
        b. run_container, assert $RUNTIME ps shows exited, assert logs contain "duplicate directory name 'a'", no Traceback
        c. stop_container

    11. SCENARIO E — empty list + missing options.json (edge cases for graceful degradation):
        a. Write options.json with {"directories":[]}
        b. run_container, assert container IS running (graceful degradation, NOT a crash), assert curl / returns 503 with body "Markdown Renderer: no directories configured" (this is the _write_minimal_nginx fallback body)
        c. stop_container
        d. Run container with NO /data/options.json mount at all (omit the -v flag)
        e. assert container IS running, assert curl /_docsify/docsify.min.js returns 200 (vendored assets still served), assert curl / returns 503 with "Markdown Renderer: no options.json mounted from HA Supervisor"
        f. stop_container

    12. Final summary: count passes/fails; if any fail exit 1, else echo "ALL VERIFICATIONS PASSED" and exit 0.

    13. chmod +x the script.

    Make the script ~250-350 lines. Each scenario gets a section header comment. Use ANSI colors (GREEN='\033[0;32m',
    RED='\033[0;31m', NC='\033[0m') for pass/fail output. Print which MULTI requirement each scenario covers.

    After creating the script, run it from the repo root:
    bash .planning/phases/05-multi-namespace-dynamic-config/verify-multi-namespace.sh
    It MUST exit 0 with "ALL VERIFICATIONS PASSED" — this is the empirical proof that all 6 MULTI requirements hold.

  </action>
  <verify>
    # The verify script itself
    test -x .planning/phases/05-multi-namespace-dynamic-config/verify-multi-namespace.sh  (executable bit set)
    grep -q '^set -euo pipefail' .planning/phases/05-multi-namespace-dynamic-config/verify-multi-namespace.sh  (strict mode)
    grep -q 'trap.*EXIT' .planning/phases/05-multi-namespace-dynamic-config/verify-multi-namespace.sh  (cleanup trap)
    grep -q 'MULTI-01\|MULTI-02\|MULTI-03\|MULTI-04\|MULTI-05\|MULTI-06' .planning/phases/05-multi-namespace-dynamic-config/verify-multi-namespace.sh  (covers all 6 requirements)
    grep -q 'ALL VERIFICATIONS PASSED' .planning/phases/05-multi-namespace-dynamic-config/verify-multi-namespace.sh  (success marker)

    # The script must actually pass
    bash .planning/phases/05-multi-namespace-dynamic-config/verify-multi-namespace.sh  (exits 0; prints ALL VERIFICATIONS PASSED)

    # No orphan containers left after the run
    docker ps -a --filter name=mr-verify --format '{{.Names}}' 2>&1 | grep -q '^mr-verify$' && echo "ORPHAN: mr-verify still running" || echo "no orphan containers"

    # Pre-commit + check-all must still pass (no markdown changes yet, but verify check)
    make check-all  (exits 0)

  </verify>
  <done>
    .planning/phases/05-multi-namespace-dynamic-config/verify-multi-namespace.sh exists, is executable, runs in strict mode,
    and exercises all 6 MULTI requirements via 5 distinct scenarios (3-namespace happy path, 1-namespace landing-page
    regeneration, 4 invalid-name fixtures, duplicate names, empty list + missing options.json). Running it from the repo
    root exits 0 with "ALL VERIFICATIONS PASSED" printed. No orphan `mr-verify` containers remain after the run (the trap
    EXIT handler cleans them up).
  </done>
</task>

<task type="auto">
  <name>Task 2: Update DOCS.md and README.md to document multi-namespace behavior + verification</name>
  <files>markdown-renderer/DOCS.md, markdown-renderer/README.md</files>
  <read_first>
    - markdown-renderer/DOCS.md (current state — 49 lines, single Example Configuration block; add two new sections)
    - markdown-renderer/README.md (current state — 63 lines, 7-point Manual HA Ingress Test Checklist; append 3 new items)
    - .planning/phases/05-multi-namespace-dynamic-config/verify-multi-namespace.sh (after Task 1 — read it to confirm what
      the empirical verification covered so DOCS.md text matches the verified behavior)
    - markdown-renderer/generate_nginx.py (the validate_directories + _write_minimal_nginx paths — confirm exact error
      strings so DOCS.md documents the right behavior)
  </read_first>
  <action>
    Edit markdown-renderer/DOCS.md to ADD two new H2 sections AFTER the existing "## Volumes" section and BEFORE "##
    Example Configuration" (this preserves the existing example block at the bottom and inserts the multi-namespace docs
    between volumes and the example). Insert exactly these two sections (do not modify existing content above the
    insertion point):

    ## Multi-Namespace Behavior

    Configure any number of Markdown directories in `directories:` — each becomes an isolated Docsify SPA under
    `/<name>/`. The add-on displays a landing page at `/` listing every configured namespace as a clickable card with
    the namespace name and the configured path.

    Landing page cards regenerate automatically on every add-on restart — change the `directories:` list in the HA
    add-on options, restart the add-on, and the new landing page reflects the new config. There is no separate "apply"
    step.

    ### Namespace Name Rules

    Each `name` must:

    - Be non-empty
    - Match the regex `^[a-z0-9][a-z0-9-]{0,62}$` (lowercase letters, digits, hyphens; max 63 characters)
    - Not collide with reserved names: `_docsify`, `api`, `data`, `share`, `config`, `media`

    If a name violates any rule, the add-on refuses to start with a clear error in the HA Supervisor log naming the
    bad name (e.g. `ERROR: namespace name 'bad/name' must match ^[a-z0-9][a-z0-9-]{0,62}$`). Duplicate names in the
    `directories:` list are also rejected (`ERROR: duplicate directory name 'docs'`).

    With an empty `directories:` list, the add-on stays running but serves a 503 page with body
    `Markdown Renderer: no directories configured`. This makes the failure mode obvious without crashing the container.

    ### Reading from /share, /config, /media

    The `map:` declaration in `config.yaml` mounts all three writable volumes so namespaces can read Markdown content
    from anywhere on the HA host filesystem. A typical multi-source configuration:

    ```yaml
    directories:
      - name: docs
        path: /share/docs        # user-shared documents
      - name: runbooks
        path: /config/runbooks   # HA configuration directory
      - name: photos
        path: /media/photos      # HA media library
    kroki_url: https://kroki.io
    ```

    No additional setup is required — just put `.md` files in the configured paths on the HA host and they will appear
    in the add-on.

    ## Validation Status

    All 6 multi-namespace requirements (MULTI-01..06) have been verified end-to-end by the script
    `.planning/phases/05-multi-namespace-dynamic-config/verify-multi-namespace.sh`. To re-run the empirical verification
    locally:

    ```bash
    make build-addon ADDON=markdown-renderer    # build local/markdown-renderer:1.0.0 (if not already built)
    bash .planning/phases/05-multi-namespace-dynamic-config/verify-multi-namespace.sh
    ```

    The script exits 0 with `ALL VERIFICATIONS PASSED` on success.

    Edit markdown-renderer/README.md to APPEND three new items at the END of the "### Manual HA Ingress Test Checklist"
    list (after item 7, before the `<!-- Badge Links -->` comment). Insert exactly these three items (use the same
    numbered list format as the existing 7 items):

    8. **Multi-namespace landing page** - Configure two or more directories in the add-on options (e.g. docs and
       runbooks). Open the Markdown Renderer panel; the landing page at the Ingress root shows two clickable cards. Click
       each card and confirm the corresponding Docsify SPA loads. Restart the add-on with a different `directories:`
       list; the landing page regenerates with the new set of cards (no caching).
    9. **Invalid namespace name rejected** - Configure a `directories:` entry with a name like `bad/name` (contains `/`)
       or `Docs` (uppercase) or `_docsify` (reserved). Restart the add-on. The HA Supervisor log shows the clear error
       `ERROR: namespace name '...' ...` and the add-on does not start. Fix the name and the add-on starts normally.
    10. **Volume mounts serve files** - Place a `README.md` file in each of `/share/docs/`, `/config/runbooks/`, and
        `/media/photos/` on the HA host. Configure three corresponding namespaces. Confirm each namespace renders its
        respective `README.md` content (Docsify reads `.md` files from the configured path via the mounted volume).

    Also update the H1 description paragraph to mention multi-namespace (the current line ends with "- Verification
    status: see Verification section below"). Change the period before the dash to a period followed by a NEW sentence:
    "Multiple namespaces can be configured and are served as isolated Docsify SPAs under separate URLs.". Then keep the
    existing "- Verification status: see Verification section below" suffix.

    Run `pre-commit run prettier --files markdown-renderer/DOCS.md markdown-renderer/README.md` after the edits to
    ensure markdown formatting stays consistent (120-char wrap). Run `make check-all` after the edits — must exit 0.

  </action>
  <verify>
    # DOCS.md: confirm new sections added + content matches verify script behavior
    grep -q '^## Multi-Namespace Behavior' markdown-renderer/DOCS.md && echo "Multi-Namespace Behavior section present"
    grep -q '^## Validation Status' markdown-renderer/DOCS.md && echo "Validation Status section present"
    grep -q 'Landing page cards regenerate' markdown-renderer/DOCS.md && echo "Regeneration note present"
    grep -q '_docsify' markdown-renderer/DOCS.md && grep -q 'reserved' markdown-renderer/DOCS.md && echo "Reserved names listed"
    grep -q 'no directories configured' markdown-renderer/DOCS.md && echo "Empty-list 503 behavior documented"
    grep -q 'duplicate directory name' markdown-renderer/DOCS.md && echo "Duplicate-name error documented"

    # README.md: confirm new checklist items added
    grep -q '^8\. \*\*Multi-namespace landing page' markdown-renderer/README.md && echo "Item 8 present"
    grep -q '^9\. \*\*Invalid namespace name rejected' markdown-renderer/README.md && echo "Item 9 present"
    grep -q '^10\. \*\*Volume mounts serve files' markdown-renderer/README.md && echo "Item 10 present"
    grep -q 'Multiple namespaces can be configured' markdown-renderer/README.md && echo "H1 description updated"

    # Pre-commit + check-all must pass
    pre-commit run prettier --files markdown-renderer/DOCS.md markdown-renderer/README.md  (passes; 120-char wrap)
    make check-all  (exits 0; validates markdown + lint + versions + dockerfiles)

  </verify>
  <done>
    markdown-renderer/DOCS.md now has two new H2 sections: "Multi-Namespace Behavior" (covers regeneration-on-restart,
    namespace name rules with the exact regex + reserved list, empty-list 503 behavior, multi-source
    /share+/config+/media example) and "Validation Status" (points to the verification script + how to run it).
    markdown-renderer/README.md has 3 new checklist items appended (8: multi-namespace landing page; 9: invalid
    namespace name rejected; 10: volume mounts serve files) and the H1 description mentions multi-namespace
    configuration. Both files pass `pre-commit run prettier` and `make check-all` exits 0.
  </done>
</task>

</tasks>

<verification>
Run all task verify commands. Phase-level checks for Plan 05-01:

1. `bash .planning/phases/05-multi-namespace-dynamic-config/verify-multi-namespace.sh` exits 0 with "ALL VERIFICATIONS
   PASSED" — proves ALL 6 MULTI-01..06 requirements hold empirically inside a running container.
2. No orphan `mr-verify` containers left after the script completes (trap cleanup works).
3. `make check-all` exits 0 after the DOCS.md + README.md edits.
4. DOCS.md "Multi-Namespace Behavior" section names the exact validation regex (`^[a-z0-9][a-z0-9-]{0,62}$`) and
   reserved names (`_docsify`, `api`, `data`, `share`, `config`, `media`) so users have a single source of truth for
   naming rules.
5. README.md Manual HA Ingress Test Checklist now has 10 items (7 Phase 4 items + 3 Phase 5 multi-namespace items),
   giving the user a complete post-merge verification procedure. </verification>

<success_criteria> Phase 5 success criteria from ROADMAP.md all met:

1. User configures two or more directories in HA options; each namespace accessible as an independent Docsify SPA at
   `/name/` — empirically verified by verify-multi-namespace.sh MULTI-02 assertions (curl /docs/, /runbooks/, /photos/
   each return their own title).
2. Ingress root (/) shows a landing page with a clickable card for every configured namespace; cards regenerate on
   restart with different config — empirically verified by MULTI-03 + MULTI-04 assertions (landing lists all 3 cards,
   then after restart with different config landing lists only 1 card).
3. Invalid namespace name (empty, contains `/`, or conflicts with `_docsify` / `api`) causes clear startup log error and
   refuses to start — empirically verified by MULTI-05 assertions (4 invalid-fixture restarts all exit non-zero with the
   expected ERROR string and no Traceback).
4. Directories from /share, /config, /media served without permission errors or volume configuration changes beyond
   `map: share:rw config:rw media:rw` — empirically verified by MULTI-06 assertions (3 volume mounts each serve their
   README.md through the corresponding namespace URL).

All 6 MULTI-01..06 requirements addressed: each requirement appears in at least one verification assertion in
verify-multi-namespace.sh AND in the README Manual HA Ingress Test Checklist (for the user to perform live-HA
verification after merge). </success_criteria>

<output>
After completion, create `.planning/phases/05-multi-namespace-dynamic-config/05-01-SUMMARY.md` following the SUMMARY.md
template. Include:
- Verbatim copy of the verify-multi-namespace.sh pass output (proves all 6 MULTI requirements empirically verified).
- File listing showing DOCS.md grew from 49 to ~85 lines + README.md grew from 63 to ~75 lines.
- make check-all exit code 0.
- Confirmation that no rebuild of local/markdown-renderer:1.0.0 was needed (Phase 4 image reused).
- Any findings from the empirical run that don't match expectations (e.g., if the generator's alias path differs from
  the configured path — document this with a recommended follow-up).
</output>
