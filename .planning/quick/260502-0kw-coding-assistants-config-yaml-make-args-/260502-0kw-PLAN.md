---
phase: quick
plan: 260502-0kw
type: execute
wave: 1
depends_on: []
files_modified:
  - coding-assistants/config.yaml
autonomous: true
requirements: []
must_haves:
  truths:
    - "HA supervisor accepts mcp_server entries without args or env fields"
    - "mcp_server entries with args and/or env still work correctly"
  artifacts:
    - path: "coding-assistants/config.yaml"
      provides: "Updated schema with optional args? and env? fields"
      contains: "args?:"
  key_links:
    - from: "coding-assistants/config.yaml"
      to: "HA supervisor schema validation"
      via: "? suffix on key names marks fields as optional"
      pattern: "args\\?:"
---

<objective>
Make `args` and `env` optional in the `mcp_servers` schema in coding-assistants/config.yaml.

Purpose: HA supervisor rejects add-on config when a user adds an mcp_server entry without `env` (and possibly `args`),
because both are currently required list fields. The fix appends `?` to both key names, making them optional — the same
pattern already used for `command: str?` and `url: str?`. The run.sh already handles missing values with `(.env // [])`
and `(.args // [])`, so no runtime changes are needed.

Output: Updated config.yaml with `args?:` and `env?:` in the mcp_servers schema section. </objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make args and env optional in mcp_servers schema</name>
  <files>coding-assistants/config.yaml</files>
  <action>
    In coding-assistants/config.yaml, update the `schema.mcp_servers` section:

    Change line 64: `      args:` → `      args?:`
    Change line 67: `      env:` → `      env?:`

    The `?` suffix on a key name in HA add-on schemas marks the field as optional (same mechanics as `command: str?`
    on line 62). No other changes needed — the defaults section and run.sh already handle missing values correctly.

    Also update the `options.mcp_servers` default entry for `ha-mcp` (which is an http-type server without args/env)
    to remove the empty `args: []` and `env: []` lines from the `fff` example if desired, but this is cosmetic only
    and NOT required for the fix to work. Focus only on the schema change.

  </action>
  <verify>
    <automated>make validate-addons</automated>
  </verify>
  <done>
    `make validate-addons` passes. The mcp_servers schema contains `args?:` and `env?:` instead of `args:` and `env:`.
    A user can configure an mcp_server entry with only `name`, `type`, and `url` (for http) without triggering
    "Missing option 'env'" errors from the HA supervisor.
  </done>
</task>

</tasks>

<verification>
Run `make validate-addons` and `make validate-versions` — both must pass with no errors.
</verification>

<success_criteria>

- coding-assistants/config.yaml schema has `args?:` and `env?:` in mcp_servers
- `make validate-addons` passes
- No other files modified </success_criteria>

<output>
After completion, create `.planning/quick/260502-0kw-coding-assistants-config-yaml-make-args-/260502-0kw-SUMMARY.md`
</output>
