---
quick_id: "260901-na1"
slug: "fix-6-pre-existing-lint-failures-on-main"
description:
  "Fix 6 pre-existing lint failures on main (end-of-file-fixer, shellcheck SC2029/SC2012 info findings, prettier,
  actionlint SC2086, validate-addon-config app_config gap, verify-bridge-scaffold docker-size regex)"
date: "2026-09-01"
status: planned
type: execute
autonomous: true
files_modified:
  - .planning/phases/08-ci-cd-hardening/08-02-SUMMARY.md
  - .planning/phases/10-auth-layer-structured-logging-healthcheck/10-01-PLAN.md
  - .planning/phases/10-auth-layer-structured-logging-healthcheck/10-02-PLAN.md
  - .planning/phases/10-auth-layer-structured-logging-healthcheck/10-03-PLAN.md
  - .planning/phases/10-auth-layer-structured-logging-healthcheck/10-CONTEXT.md
  - .planning/phases/10-auth-layer-structured-logging-healthcheck/10-DISCUSSION-LOG.md
  - .planning/phases/15-ci-hardening-provider-install-workflow/15-03-SUMMARY.md
  - internal/spike-pitfalls10-backup-addon-config.sh
  - internal/spike-h1-token-rotation.sh
  - terraform-bridge/internal/httpapi/handlers/version.go
  - terraform-provider-homeassistant/main.go
  - tools/test-bridge-fixture/go.mod
  - tools/test-bridge-fixture/main.go
  - .github/RELEASE.md
  - README.md
  - docs/DEVELOPMENT.md
  - docs/WEBHOOK_SETUP.md
  - terraform-bridge/DOCS.md
  - .github/workflows/test-install-provider.yml
  - internal/validate-addon-config.py
  - internal/verify-bridge-scaffold.sh

must_haves:
  truths:
    - "`pre-commit run end-of-file-fixer --all-files` reports zero changes needed (all text files end with exactly one
      trailing newline)"
    - "`pre-commit run prettier --files .github/RELEASE.md README.md docs/DEVELOPMENT.md docs/WEBHOOK_SETUP.md
      terraform-bridge/DOCS.md` passes with zero diffs"
    - "`shellcheck -e SC1091 -e SC2034 internal/spike-h1-token-rotation.sh
      internal/spike-pitfalls10-backup-addon-config.sh` exits 0 (matches the repo's pinned ignore flags in
      `.pre-commit-config.yaml`)"
    - "`actionlint .github/workflows/test-install-provider.yml` exits 0 with no SC2086 finding"
    - "`python3 internal/validate-addon-config.py` passes and accepts both `app_config` (terraform-bridge) and
      `addon_config` (phone-logger, legacy) as valid `map` types"
    - 'The `verify-bridge-scaffold.sh` docker-image-size parser correctly extracts value+unit from both `"55.1 MB"` and
      `"55.1MB"` Docker size formats (verified via standalone Python snippet — docker itself is unavailable in this
      environment)'
    - "No logic/behavioral changes introduced to the two throwaway Phase 9/10 spike scripts beyond inline shellcheck
      suppression comments"
    - "The terraform-bridge `/data` mkdir / no-token-leak bug (item 7 from the original diagnosis) is NOT touched —
      deferred to a separate `/gsd:debug` session"
  artifacts:
    - path: "internal/validate-addon-config.py"
      provides: "VALID_MAP_TYPES includes both app_config and addon_config"
      contains: '"app_config",'
    - path: "internal/verify-bridge-scaffold.sh"
      provides: "regex-based docker image size parsing (handles spaced and unspaced units)"
      contains: "re.match"
    - path: ".github/workflows/test-install-provider.yml"
      provides: "quoted $BRIDGE_PORT in the fixture-start run: block"
      contains: '--port "$BRIDGE_PORT"'
    - path: "internal/spike-h1-token-rotation.sh"
      provides: "inline SC2029 suppressions for intentional client-side var expansion into remote ssh commands"
      contains: "shellcheck disable=SC2029"
    - path: "internal/spike-pitfalls10-backup-addon-config.sh"
      provides: "inline SC2029/SC2012 suppressions for intentional patterns"
      contains: "shellcheck disable=SC2029"
  key_links:
    - from: "internal/validate-addon-config.py"
      to: "terraform-bridge/config.yaml"
      via: "map entry type validation against VALID_MAP_TYPES"
      pattern: "app_config"
    - from: ".pre-commit-config.yaml (shellcheck hook args: -e SC1091 -e SC2034)"
      to: "internal/spike-h1-token-rotation.sh, internal/spike-pitfalls10-backup-addon-config.sh"
      via:
        "inline # shellcheck disable comments (no severity-floor exists, so info-level findings must be suppressed
        per-line)"
      pattern: "shellcheck disable=SC(2029|2012)"
---

<objective>
Fix the 6 pre-existing lint failures on `main` that were diagnosed (against real CI logs + source) in a prior session
while investigating why open Renovate PRs #42-45 show red CI — confirmed unrelated to those PRs, purely pre-existing
debt on `main`. No further investigation needed; this plan only implements the already-verified fixes.

The 6 failures: (1) end-of-file-fixer trailing-newline violations, (2) shellcheck info-level SC2029/SC2012 findings in
two throwaway spike scripts (no severity floor configured, so info-level findings fail the hook), (3) prettier
formatting drift in 5 markdown files, (4) actionlint SC2086 (unquoted shell var) in a workflow `run:` block, (5) a real
bug in `internal/validate-addon-config.py` — `VALID_MAP_TYPES` never got the `app_config` type added when
terraform-bridge migrated off the deprecated `addon_config` map type, and (6) a real bug in
`internal/verify-bridge-scaffold.sh` — its docker-image-size parser assumes a space between value and unit (`"55.1 MB"`)
but current Docker emits no space (`"55.1MB"`), so `.split()` + `float()` throws.

Out of scope: the terraform-bridge `/data` mkdir / no-token-leak bug (item 7 from the original diagnosis) is explicitly
deferred to a separate `/gsd:debug` session — do NOT touch it here.

Purpose: restore a green `main` branch so the open Renovate PRs (and all future PRs) show accurate CI status instead of
pre-existing red noise.

Output: 3 atomic commits — mechanical formatting fixes, CI-config lint fixes (shellcheck/actionlint), and the 2 real bug
fixes — each independently verifiable with the repo's own tooling. </objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@.pre-commit-config.yaml
@.actionlint.yml

All findings below were independently re-verified against the actual repo state in this planning session (not just
trusted from the prior diagnosis): `npx prettier@3.9.6 --check` confirmed exactly the 5 listed markdown files fail and
no others; a locally-built `shellcheck 0.11.0` confirmed the exact SC2029/SC2012 lines in both spike scripts and found
no other shellcheck findings repo-wide; a locally-built `actionlint 1.7.7` (with shellcheck on PATH) confirmed the
SC2086 finding at `.github/workflows/test-install-provider.yml:58` and confirmed the fix (quoting) clears it;
`python3 internal/validate-addon-config.py` reproduced the real `app_config` validation failure and confirmed the fix;
the regex-based docker-size parse was tested standalone against both `"55.1 MB"` and `"55.1MB"` and produces identical
byte counts. </context>

<tasks>

<task type="auto">
  <name>Task 1: Mechanical auto-fixes — trailing newlines + markdown formatting (items 1, 3)</name>
  <files>
    .planning/phases/08-ci-cd-hardening/08-02-SUMMARY.md,
    .planning/phases/10-auth-layer-structured-logging-healthcheck/10-01-PLAN.md,
    .planning/phases/10-auth-layer-structured-logging-healthcheck/10-02-PLAN.md,
    .planning/phases/10-auth-layer-structured-logging-healthcheck/10-03-PLAN.md,
    .planning/phases/10-auth-layer-structured-logging-healthcheck/10-CONTEXT.md,
    .planning/phases/10-auth-layer-structured-logging-healthcheck/10-DISCUSSION-LOG.md,
    .planning/phases/15-ci-hardening-provider-install-workflow/15-03-SUMMARY.md,
    internal/spike-pitfalls10-backup-addon-config.sh,
    terraform-bridge/internal/httpapi/handlers/version.go,
    terraform-provider-homeassistant/main.go,
    tools/test-bridge-fixture/go.mod,
    tools/test-bridge-fixture/main.go,
    .github/RELEASE.md,
    README.md,
    docs/DEVELOPMENT.md,
    docs/WEBHOOK_SETUP.md,
    terraform-bridge/DOCS.md
  </files>
  <action>
    These are purely mechanical, tool-driven fixes — do not hand-edit content. If `pre-commit` is not yet installed
    locally, run `make init` first (installs `pre-commit`, `yamllint`, `shellcheck-py` via `uv tool install`;
    `pre-commit` itself bootstraps the `prettier`/`actionlint`/`markdownlint-cli2` hook environments on first run).

    Step 1 — trailing newlines: run `pre-commit run end-of-file-fixer --all-files`. This adds a single trailing
    newline to every text file currently missing one (confirmed via a manual `tail -c1` scan: the 12 files listed
    above, excluding binaries like `*.png` which the hook's `types: [text]` filter already skips automatically).

    Step 2 — markdown formatting: run
    `pre-commit run prettier --files .github/RELEASE.md README.md docs/DEVELOPMENT.md docs/WEBHOOK_SETUP.md terraform-bridge/DOCS.md`.
    This reformats exactly these 5 files per `.prettierrc.yaml` (confirmed via `npx prettier@3.9.6 --check` — no other
    markdown files in the repo currently fail formatting).

    Both hooks are "fixer" hooks: the first invocation exits non-zero after modifying files (expected pre-commit
    behavior). Re-run each command a second time to confirm a clean (exit 0) pass — that second, clean run is the
    verification step below.

  </action>
  <verify>
    <automated>pre-commit run end-of-file-fixer --all-files && pre-commit run prettier --files .github/RELEASE.md README.md docs/DEVELOPMENT.md docs/WEBHOOK_SETUP.md terraform-bridge/DOCS.md</automated>
  </verify>
  <done>Both `pre-commit` hook runs exit 0 with no further changes reported. `git diff --stat` shows only whitespace-at-EOF additions and markdown reformatting — no content/logic changes.</done>
</task>

<task type="auto">
  <name>Task 2: CI-config lint fixes — shellcheck suppressions + actionlint quoting (items 2, 4)</name>
  <files>
    internal/spike-h1-token-rotation.sh,
    internal/spike-pitfalls10-backup-addon-config.sh,
    .github/workflows/test-install-provider.yml
  </files>
  <action>
    These are throwaway Phase 9/10 spike scripts (per their own header comments) — do NOT rewrite any ssh/ls logic,
    only add inline shellcheck suppression comments directly above each flagged line, each with a short reason.

    In `internal/spike-h1-token-rotation.sh`, insert `# shellcheck disable=SC2029 # intentional: local var expands
    client-side into remote ssh command, by design` immediately above:
    - line 57: `if ! ssh "${HOST}" "sudo docker ps --format '{{.Names}}' | grep -qx '${CONTAINER_NAME}'"; then`
    - line 71 (inside `CAPTURE_TOKEN()`, 4-space indent): `raw="$(ssh "${HOST}" "sudo docker exec ${CONTAINER_NAME} env" 2>/dev/null \`

    In `internal/spike-pitfalls10-backup-addon-config.sh`, insert the same SC2029 disable comment (matching each
    line's indentation) immediately above:
    - line 48 (inside `cleanup()`, 4-space indent): `ssh "${HOST}" "rm -f /addon_configs/${ADDON_SLUG}/${SENTINEL_NAME}" >/dev/null 2>&1 || true`
    - line 69: `ADDON_INFO="$(ssh "${HOST}" sudo ha apps info "${ADDON_SLUG}" 2>&1)" || {`
    - line 89: `ssh "${HOST}" "echo '${SENTINEL_DATA}' > ${SENTINEL_HOST_DIR}/${SENTINEL_NAME}"`
    - line 91: `ssh "${HOST}" "ls -la ${SENTINEL_HOST_DIR}/${SENTINEL_NAME}"`
    - line 92: `ssh "${HOST}" "cat ${SENTINEL_HOST_DIR}/${SENTINEL_NAME}"`

    Then insert `# shellcheck disable=SC2012 # intentional: informational listing of our own generated filenames, not
    machine-parsed` immediately above line 140: `ls "${BACKUP_DIR}" | head -30`.

    In `.github/workflows/test-install-provider.yml`, quote the unquoted variable at line 58 inside the "Start test
    Bridge fixture (background)" step's `run: |` block. Change `            --port $BRIDGE_PORT \` to
    `            --port "$BRIDGE_PORT" \` (preserve the existing 12-space indentation). This is the sole change —
    verified locally that quoting alone clears the actionlint SC2086 finding at this exact line.

  </action>
  <verify>
    <automated>shellcheck -e SC1091 -e SC2034 internal/spike-h1-token-rotation.sh internal/spike-pitfalls10-backup-addon-config.sh && actionlint .github/workflows/test-install-provider.yml</automated>
  </verify>
  <done>Both spike scripts produce zero shellcheck findings with the repo's standard `-e SC1091 -e SC2034` ignores (no other suppressions added). `actionlint` reports zero findings for `test-install-provider.yml`. Neither script's ssh/backup/restart logic changed — only comments and one added pair of quotes.</done>
</task>

<task type="auto">
  <name>Task 3: Real bugs — validator map-type gap + docker-size regex parsing (items 5, 6)</name>
  <files>internal/validate-addon-config.py, internal/verify-bridge-scaffold.sh</files>
  <action>
    In `internal/validate-addon-config.py`, add `"app_config"` to the `VALID_MAP_TYPES` set (currently: `addon_config`,
    `backup`, `config`, `data`, `media`, `share`, `ssl`). Insert it right after `"addon_config",` to preserve
    alphabetical order. Keep `"addon_config"` in the set — `phone-logger/config.yaml` still declares
    `map: addon_config:rw` (legacy type name, per this repo's CLAUDE.md), while `terraform-bridge/config.yaml` now
    declares `map: app_config:rw` (migrated in a prior commit). Both must validate.

    In `internal/verify-bridge-scaffold.sh` line 66, replace the `.split()`-based python3 one-liner with a
    regex-based parse so it handles both `"55.1 MB"` (space-separated, older Docker) and `"55.1MB"` (no space,
    current Docker). Replace:
    `import sys; s=sys.stdin.read().strip(); v=float(s.split()[0]); u=s.split()[1].upper(); mult={"B":1,"KB":1024,"MB":1024**2,"GB":1024**3}; print(int(v*mult[u]))`
    with:
    `import sys, re; s=sys.stdin.read().strip(); m=re.match(r"([\d.]+)\s*([A-Za-z]+)", s); v=float(m.group(1)); u=m.group(2).upper(); mult={"B":1,"KB":1024,"MB":1024**2,"GB":1024**3}; print(int(v*mult[u]))`
    Keep the same `mult` unit table and final `print(int(v*mult[u]))` — only the value/unit extraction changes. Do not
    modify any other part of the script (stages 2/3, cleanup, size cap logic).

  </action>
  <verify>
    <automated>python3 internal/validate-addon-config.py && for s in "55.1 MB" "55.1MB"; do printf '%s' "$s" | python3 -c 'import sys, re; s=sys.stdin.read().strip(); m=re.match(r"([\d.]+)\s*([A-Za-z]+)", s); v=float(m.group(1)); u=m.group(2).upper(); mult={"B":1,"KB":1024,"MB":1024**2,"GB":1024**3}; print(int(v*mult[u]))'; done</automated>
  </verify>
  <done>`python3 internal/validate-addon-config.py` prints "Add-on config validation passed." (exit 0). Both size-string test inputs above produce identical byte count (57776537), confirming the regex handles spaced and unspaced Docker size formats correctly. No other line in `verify-bridge-scaffold.sh` changed.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                          | Description                                                                                                                                               |
| ------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------- |
| operator CLI args -> ssh remote command string    | `HOST`/`CONTAINER_NAME`/`ADDON_SLUG`/`SENTINEL_*` values are shell-expanded client-side into a string passed to a remote `ssh` invocation (spike scripts) |
| GitHub Actions workflow env -> shell `run:` block | `BRIDGE_PORT` is a workflow-defined `env:` value expanded unquoted into a `run:` step                                                                     |

## STRIDE Threat Register

| Threat ID       | Category  | Component                                                                                                                                 | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                                    |
| --------------- | --------- | ----------------------------------------------------------------------------------------------------------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-quick-01      | Tampering | `internal/spike-h1-token-rotation.sh`, `internal/spike-pitfalls10-backup-addon-config.sh` (SC2029: var expansion into ssh command string) | accept      | Throwaway operator-run spike scripts; `HOST`/`ADDON_SLUG`/`SENTINEL_*` come from the same operator invoking the script (CLI args or hardcoded defaults), not untrusted network input -- suppressed inline with `# shellcheck disable=SC2029` plus a reason comment per this plan, no logic rewrite |
| T-quick-02      | Tampering | `.github/workflows/test-install-provider.yml` (SC2086: unquoted `$BRIDGE_PORT`)                                                           | mitigate    | `BRIDGE_PORT` is a hardcoded `env: 18224` in the workflow itself (not attacker-controlled), quoted anyway per this plan to close the word-splitting/globbing class defensively                                                                                                                     |
| T-quick-03      | Tampering | `internal/validate-addon-config.py` (map-type allowlist)                                                                                  | mitigate    | `VALID_MAP_TYPES` is an explicit allowlist (not a denylist) -- adding `app_config` keeps the validator fail-closed against any other unrecognized type string                                                                                                                                      |
| T-quick-SC      | Tampering | npm/pip/cargo installs                                                                                                                    | n/a         | this plan installs no new packages; only `pre-commit`/`shellcheck`/`prettier`/`actionlint` invoked, all already pinned in `.pre-commit-config.yaml`                                                                                                                                                |
| </threat_model> |

<verification>
Run, from repo root, in order:
1. `pre-commit run end-of-file-fixer --all-files && pre-commit run prettier --files .github/RELEASE.md README.md docs/DEVELOPMENT.md docs/WEBHOOK_SETUP.md terraform-bridge/DOCS.md`
2. `shellcheck -e SC1091 -e SC2034 internal/spike-h1-token-rotation.sh internal/spike-pitfalls10-backup-addon-config.sh && actionlint .github/workflows/test-install-provider.yml`
3. `python3 internal/validate-addon-config.py`
4. Standalone docker-size regex check (no docker binary needed): `for s in "55.1 MB" "55.1MB"; do printf '%s' "$s" | python3 -c 'import sys, re; s=sys.stdin.read().strip(); m=re.match(r"([\d.]+)\s*([A-Za-z]+)", s); v=float(m.group(1)); u=m.group(2).upper(); mult={"B":1,"KB":1024,"MB":1024**2,"GB":1024**3}; print(int(v*mult[u]))'; done` -- both lines must print the same number (57776537).
5. `make check-all` should now pass, except the `verify-bridge-scaffold` local hook itself, which needs `docker` (unavailable in this environment) -- step 4 stands in for it per the task's explicit verification constraint.

Confirm no diff touches the terraform-bridge `/data` mkdir / token-leak logic (item 7, out of scope):
`git diff -- terraform-bridge/` should show only `DOCS.md` formatting, nothing else. </verification>

<success_criteria>

- All 6 diagnosed lint failures fixed and locally verified (pre-commit/shellcheck/actionlint/python re-runs all exit 0)
- Two real bugs fixed: `validate-addon-config.py` accepts both `app_config` and `addon_config`;
  `verify-bridge-scaffold.sh` size parser regex-verified against both Docker size formats
- Zero logic changes to the two throwaway spike scripts beyond inline shellcheck disable comments
- Item 7 (terraform-bridge `/data` mkdir / token leak) untouched
- 3 atomic commits created, one per task </success_criteria>

<output>
Create `.planning/quick/260901-na1-fix-6-pre-existing-lint-failures-on-main/260901-na1-SUMMARY.md` when done.
</output>
