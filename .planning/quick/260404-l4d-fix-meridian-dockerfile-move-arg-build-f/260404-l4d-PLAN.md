---
phase: quick-260404-l4d
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - meridian/Dockerfile
autonomous: true
requirements:
  - QUICK-260404-l4d
must_haves:
  truths:
    - "Docker build does not fail with 'base name ($BUILD_FROM) should not be blank'"
    - "ARG BUILD_FROM is declared before the first FROM instruction"
    - "Stage 2 FROM $BUILD_FROM resolves to the HA base image"
  artifacts:
    - path: "meridian/Dockerfile"
      provides: "Fixed multi-stage Dockerfile with correct ARG placement"
      contains: "ARG BUILD_FROM"
  key_links:
    - from: "ARG BUILD_FROM=ghcr.io/home-assistant/amd64-base:3.23"
      to: "FROM $BUILD_FROM"
      via: "Docker global ARG scope (before first FROM)"
      pattern: "^ARG BUILD_FROM"
---

<objective>
Fix the Meridian Dockerfile so the multi-stage build resolves `$BUILD_FROM` correctly.

Purpose: Docker ARGs used in FROM instructions must be declared before the first FROM in the file. Currently
`ARG BUILD_FROM` is on line 17, inside the first build stage scope, so it is empty when the second `FROM $BUILD_FROM` is
evaluated. This causes the build to fail with "base name ($BUILD_FROM) should not be blank".

Output: `meridian/Dockerfile` with `ARG BUILD_FROM=ghcr.io/home-assistant/amd64-base:3.23` moved to line 1, before
`FROM oven/bun:1 AS build`. </objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@meridian/Dockerfile
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move ARG BUILD_FROM to before the first FROM</name>
  <files>meridian/Dockerfile</files>
  <action>
    Rewrite `meridian/Dockerfile` so that `ARG BUILD_FROM=ghcr.io/home-assistant/amd64-base:3.23` is the very first
    line of the file, before `FROM oven/bun:1 AS build`.

    Remove the existing `ARG BUILD_FROM=ghcr.io/home-assistant/amd64-base:3.23` line from its current position
    (between the comment "# Stage 2: Runtime" and `FROM $BUILD_FROM`).

    The comment "# Stage 2: Runtime — HA base image with Node.js" should remain directly above `FROM $BUILD_FROM`.

    Target structure (line order):
    ```
    ARG BUILD_FROM=ghcr.io/home-assistant/amd64-base:3.23
    # Stage 1: Build — compile TypeScript with bun
    FROM oven/bun:1 AS build
    ...
    # Stage 2: Runtime — HA base image with Node.js
    FROM $BUILD_FROM
    ...
    ```

    All other lines remain unchanged. Do not reformat, reorder, or alter any other content.

  </action>
  <verify>
    <automated>
      grep -n "ARG BUILD_FROM" /home/akentner/Projects/homeassistant-addons/meridian/Dockerfile
      # Must show line 1 only. Line 17 (old position) must be gone.
      head -3 /home/akentner/Projects/homeassistant-addons/meridian/Dockerfile
      # Must show: ARG BUILD_FROM=... as line 1
    </automated>
  </verify>
  <done>
    `ARG BUILD_FROM=ghcr.io/home-assistant/amd64-base:3.23` appears exactly once, on line 1 of the file, before any
    FROM instruction. `FROM $BUILD_FROM` on the second stage is unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify lint passes</name>
  <files></files>
  <action>
    Run the pre-commit hooks on the changed file to confirm no lint regressions:

    ```bash
    cd /home/akentner/Projects/homeassistant-addons && pre-commit run --files meridian/Dockerfile
    ```

    If any hook fails due to the Dockerfile change, fix accordingly. Hadolint is disabled (commented out in
    `.pre-commit-config.yaml`), so only generic hooks (trailing whitespace, end-of-file, line endings) apply.

  </action>
  <verify>
    <automated>cd /home/akentner/Projects/homeassistant-addons && pre-commit run --files meridian/Dockerfile</automated>
  </verify>
  <done>All pre-commit hooks pass for `meridian/Dockerfile`.</done>
</task>

</tasks>

<verification>
After both tasks complete:
- `grep -n "ARG BUILD_FROM" meridian/Dockerfile` returns a single match on line 1
- `head -1 meridian/Dockerfile` outputs `ARG BUILD_FROM=ghcr.io/home-assistant/amd64-base:3.23`
- `pre-commit run --files meridian/Dockerfile` exits 0
</verification>

<success_criteria> The Dockerfile passes Docker's build-time ARG scoping rules: `ARG BUILD_FROM` is declared before the
first `FROM`, making `$BUILD_FROM` resolvable in all subsequent FROM instructions. The build no longer fails with "base
name ($BUILD_FROM) should not be blank". </success_criteria>

<output>
After completion, create `.planning/quick/260404-l4d-fix-meridian-dockerfile-move-arg-build-f/260404-l4d-SUMMARY.md`
documenting what was changed, which lines moved, and the verification result.
</output>
