---
phase: quick
plan: 260404-n7n
type: execute
wave: 1
depends_on: []
files_modified:
  - meridian/Dockerfile
autonomous: true
requirements: []
must_haves:
  truths:
    - "Docker build stage 1 completes without hanging"
    - "dist/cli.js and dist/server.js are present after build"
  artifacts:
    - path: "meridian/Dockerfile"
      provides: "Build stage without hanging server start"
      contains: "bun build bin/cli.ts src/proxy/server.ts"
  key_links:
    - from: "meridian/Dockerfile"
      to: "dist/cli.js"
      via: "bun build + tsc compilation"
      pattern: "bun build bin/cli\\.ts"
---

<objective>
Replace `bun run build` on line 15 of meridian/Dockerfile with the raw bun + tsc compilation commands.

Purpose: The `bun run build` npm script in the upstream package.json includes a post-build server-start step that causes
the Docker build to hang forever waiting for a server process that never exits.

Output: Updated Dockerfile where Stage 1 runs only the compilation commands directly, skipping any server-start step.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@meridian/Dockerfile
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace bun run build with raw compilation commands</name>
  <files>meridian/Dockerfile</files>
  <action>
    Replace line 15 in meridian/Dockerfile:

    FROM:
      RUN bun run build

    TO:
      RUN bun build bin/cli.ts src/proxy/server.ts --outdir dist --target node --splitting --external @anthropic-ai/claude-agent-sdk --entry-naming '[name].js' && \
          tsc -p tsconfig.build.json

    This runs only the compilation step (bun build + tsc) from the upstream build script.
    It deliberately omits the `node --check` verification step (optional, not needed in CI) and any server-start step
    that would cause `docker build` to hang.

    Do not change anything else in the Dockerfile.

  </action>
  <verify>
    <automated>grep -n "bun build bin/cli.ts" /home/akentner/Projects/homeassistant-addons/meridian/Dockerfile && ! grep -n "bun run build" /home/akentner/Projects/homeassistant-addons/meridian/Dockerfile</automated>
  </verify>
  <done>
    Line 15 contains the raw bun build + tsc command. No reference to `bun run build` remains in the Dockerfile.
  </done>
</task>

</tasks>

<verification>
Run lint to confirm the Dockerfile still passes hadolint-equivalent checks (pre-commit):

    cd /home/akentner/Projects/homeassistant-addons && make lint

</verification>

<success_criteria>

- meridian/Dockerfile no longer contains `bun run build`
- The replacement line calls `bun build bin/cli.ts ...` and `tsc -p tsconfig.build.json` directly
- Pre-commit hooks pass (YAML, shell, markdown lint unaffected; Dockerfile change is cosmetic to linters)
  </success_criteria>

<output>
After completion, create `.planning/quick/260404-n7n-replace-bun-run-build-in-meridian-docker/260404-n7n-SUMMARY.md`
</output>
